// TASK 1.1.1: minimal, real page metadata — grows into BaseLayout's config
// in TASK 1.2.1. Not user-facing copy (so not yet i18n's `t()` concern,
// TASK 1.1.2), just the `<title>`/description this task's own page needs.

export const siteName = 'Nourish the Nerve';
export const siteDescription =
  'UK neuro-rehabilitation clinic — patient care, clinician-led, online.';
