# ADR 0014 — CI/CD

**Decision:** GitHub Actions + OIDC, ephemeral PR envs, canary + auto-rollback
**Options rejected:** CodePipeline (per-pipeline cost); manual deploy (fails C-06/C-07)
**£/mo:** £0
**Reversal cost:** Low
