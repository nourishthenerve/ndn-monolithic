# 1. Decision log

Decisions D-01…D-05 were answered directly. D-06 onward are the Stage A recommended defaults, adopted so work can proceed — **each is individually overridable and none is load-bearing on the others unless noted.**

| ID | Decision | Consequence |
|---|---|---|
| **D-01** | Build in a **new AWS member account** inside an Organization under the existing payer | Existing card, one consolidated invoice, access by role-switch, one new root email with MFA then unused. Gains **its own always-free allowances** (islamicmaps currently consumes 803129122420's). No signup credits (org members forfeit them). **SES production access must be requested in the new account** → long-lead, Phase 0 |
| **D-02** | Legacy `nourishthenerve-api` Lambda is **decommissioned**; build is greenfield | No port, no reuse. Live brochure site may call it, so removal happens at G1 cutover. Interim non-destructive containment still lands in Phase 0 |
| **D-03** | `clients/` (361 MB) is **disposable test/demo material** | Excluded from the new platform. **No code we write will delete it** (§6.8 — erasure stays a manual human action). Versioning still enabled as cheap insurance; you remove it by hand if you want it gone |
| **D-04** | **English at launch**, i18n framework built for N languages, **RTL-safe primitives from day one** | No hard-coded copy anywhere, ever. Translation procurement is a long-lead item triggered when you name languages; it does not block launch |
| **D-05** | **Chatbot deferred past launch** | FR-WEB-03 and M1.6 leave launch scope. LLM spend line = £0. Medical-device qualification leaves the critical path but **stays open** in the risk register |
| D-06 | Serverless-first: Lambda **arm64** + API Gateway **HTTP API** | arm64 is 20% cheaper/GB-s; HTTP API ~⅓ the price of REST |
| D-07 | **DynamoDB on-demand**, single-table design | 25 GB always-free; every §7 query proven servable in ADR-004 or we switch and cost an instance honestly |
| D-08 | Static web on **S3 + CloudFront PriceClass_100**, API on same domain | Same-origin cookies/CORS; 1,000 free invalidations/month covers deploys |
| D-09 | **Cognito Essentials** — TOTP for clinicians, email-OTP for patients | £0 at our scale (10,000 free MAU verified); email-OTP requires Essentials; avoids self-rolled auth on health data |
| D-10 | **Email-primary notifications** via SES; SMS only for the 1-hour appointment reminder | Arithmetic, not preference — see Risk R-01 |
| D-11 | SMS: app-level hard block + provider spend limit backstop + **+44 allow-list** + per-principal rate limit | C-02 demands a block, not an alert |
| D-12 | Video: **P2P first, Cloudflare TURN fallback** (1 TB/mo free), concurrency-capped | Verified worst case ≈80–90 GB/mo. Self-hosted coturn ≈£6/mo — rejected on cost |
| D-13 | **Stripe Checkout**, GBP, webhook-driven, idempotent | No monthly fee; per-transaction fees netted from revenue, outside C-01 |
| D-14 | **SSM Parameter Store SecureString** for secrets, not Secrets Manager | Secrets Manager at ~6 secrets ≈ £2/mo = 15% of target envelope for no benefit we need |
| D-15 | **AWS CDK (TypeScript)** for all infrastructure | One language across web, API, IaC, mobile (NFR-08) |
| D-16 | **TypeScript end-to-end**, pnpm workspaces monorepo | Single toolchain; shared types are the contract |
| D-17 | GitHub Actions with **OIDC federation** to a deploy role; no long-lived AWS keys | NFR-03; constrained to 2,000 free CI minutes/month |
| D-18 | Observability: UptimeRobot free + ~8 CloudWatch alarms + **14-day log retention**, no dashboards/WAF/Synthetics | Log ingestion at $0.5985/GB is the sleeper cost |
| D-19 | Pre-production confidence: local emulation + **ephemeral per-PR environments** + contract tests + **feature flags** + canary alias + post-deploy smoke with auto-rollback | Standing cost £0 — the answer to §10 |
| D-20 | **SLO 99.9%/month**, measured by 5-minute external checks + post-deploy smoke | 43 min/month budget. Stated honestly: 5-min polling detects outages, it cannot *prove* 99.9% |
| D-21 | **RPO ≤1h** (DynamoDB PITR), **RTO ≤4 working hours**, restore drill actually executed at M5.4 | PITR on a sub-1 GB clinical dataset costs pennies |
| D-22 | Backups: PITR **plus** periodic export to a separate object-locked prefix | §6.6 wants protection from account compromise, not just bad code |
| D-23 | Feature flags: **homegrown, config-driven** | Avoids a recurring SaaS line item |
| D-24 | Add the **missing DMARC record**; keep Zoho Mail EU for inbound | Deliverability is a launch blocker under an email-primary strategy |
| D-25 | Legacy brochure site stays live untouched until **G1 DNS cutover** | We hold DNS; we never need the other account |
| D-26 | Mobile: **Expo / React Native**, not before G5 | C-10. Store fees ($99/yr + $25) reported, outside C-01 |
| D-27 | **§0.6 elaboration policy adopted as written** | Full detail Phases 0–1; stubs beyond; elaborated at each gate |
| D-28 | **Root access keys deleted by you** once an admin identity exists | I flag it; I will not touch credentials |
| D-29 | **Patient auth reverts to staff-issued passwords** (2026-08-29): no self-registration, no OTP of any kind (email, SMS or WhatsApp). A patient contacts the clinic's WhatsApp Business number — a human-staffed channel, not a technical integration; staff verify who they are (trained, not automated) and create the account on their behalf via `POST /patients`, setting a permanent, machine-generated password the patient does not choose. Forgetting it is the same WhatsApp conversation again (`POST /patients/{id}/reset-password`), never a self-service reset. Supersedes D-09's patient half ("email-OTP for patients") — D-09's clinician half (TOTP) is untouched | Removes SMS/WhatsApp-OTP regulatory dependencies (India TRAI/DLT, Meta template approval) and the SES-production-access blocker entirely for patients, at the cost of a new, code-unmitigated social-engineering surface (`docs/plan/02-risk-register.md`'s new risk) the owner accepts and addresses with staff training rather than automation. **The clinician-approval step (D-onward, TASK 2.5.1) is explicitly unchanged** — an owner instruction, not an oversight. DPIA/solicitor review (LL-05/LL-06) is deliberately deferred until the mechanism is proven with synthetic test patients; TASK 5.5.3's own go-live gate for real patient data is unaffected. Full detail: [docs/runbooks/patient-account-provisioning.md](../runbooks/patient-account-provisioning.md) |
| D-30 | **Clinician onboarding reverts to staff-issued credentials** (2026-08-29): no Cognito invite email, ever. The principal clinician creates a colleague's account via `POST /clinicians` exactly as today, but this platform now generates a permanent password *and* completes the clinician pool's mandatory TOTP enrolment on the new clinician's behalf, returning both the password and the TOTP setup secret in the same API response, once. The principal relays both to the new clinician however they already communicate — in person, phone, WhatsApp — never automatically emailed, never a Cognito-sent temporary-password email. Deactivation/reactivation and the principal/sub role split are unchanged. Supersedes 2.4.1's own "Cognito's own invite email, a deliberate deviation" note (`clinician-admin.ts`'s header) and closes the clinician half of D-09 the same way D-29 already closed the patient half | Removes the last email dependency from account creation of any kind — clinician onboarding no longer needs SES production access (LL-01) any more than patient onboarding does after D-29, and a stalled SES appeal no longer blocks hiring a colleague. Trades the new clinician's own exclusive possession of their TOTP secret (ordinary enrolment: only the enrolling person ever sees it) for the principal also knowing it at creation time — the same *category* of trade-off D-29 already accepted for patient identity verification, now on the clinician side, and named as a new risk rather than left implicit (`docs/plan/02-risk-register.md`). Mitigated by the audit trail (every create is attributed to the acting principal) and, not built in this pass, a real mitigation named rather than silently deferred: a signed-in clinician replacing their own admin-provisioned TOTP secret with one only they have ever seen. Full detail: [docs/runbooks/clinician-accounts.md](../runbooks/clinician-accounts.md) |
| D-31 | **Workshops go announcement-only; no Stripe, ever** (2026-08-29): D-13/ADR-0010's Stripe Checkout is abandoned before its first real use — not deferred pending LL-03, **decided against**. A public workshop page states what, when, and (if the owner chooses) the informational cost, with no capacity limit enforced, no online payment, and no registration flow of any kind on the website. The owner's own words: "if I ever want to announce a webinar it will be a generic one." Anything requiring an actual booking — a targeted invitation, a paid place, a 1:1 session — is arranged over WhatsApp directly, the identical human-mediated channel D-29/D-30 already established for patient and clinician onboarding. Supersedes D-13 in full; ADR-0010 is amended, not rewritten, so the rejected-options record stays intact | Removes LL-03 (Stripe KYC/verification) from the critical path entirely — no longer "gates M1.5," moot. TASK 1.5.2's own code (`stripe-checkout.ts`, `stripe-webhook.ts`, `registration-repository.ts`, the `Registration` entity) is built, tested, and was never wired into any page `apps/web` actually serves (confirmed by search — zero references to Stripe/checkout anywhere in `apps/web/src`); it stays in place, unused, flag permanently off, not deleted — the same "dark, unreachable, not removed" posture this codebase already holds for `patient-registration.md`'s own retired self-registration path. `workshop-confirmation-sms.md`'s own SMS template (`workshopRegistrationConfirmation`) is equally moot — nothing will ever create a `Registration` row for it to confirm. `Workshop.capacity`/`priceMinorUnits` and the detail page's own capacity/price display (TASK 1.5.1) are not removed by this decision alone — a separate, smaller follow-up task, not assumed included here |
| D-32 | **No automated notification channel of any kind, anywhere — WhatsApp is the sole out-of-band channel** (2026-08-30): the owner's own words, verbatim, in response to being asked how far D-29's "no automated channel" principle extends: "remove this form, the patient/visit will contact to the whatsapp business account" (the public contact form); "remove this reminder thing, the clinician handling whatsapp will send a reminder manually" (the 1-hour appointment-reminder sweep); "remove this, any notification will go via whatsapp" (every remaining automated notice: assignment approve/decline/reassign, clinician caseload add/remove, new-message, clinician deactivation). This is D-29/D-30/D-31's own logic taken to its conclusion rather than a new principle: this system will never send an automated email or SMS to a patient or clinician about anything, ever — every one of those moments is now either silent (the API mutation still happens; nothing tells anyone it happened over any channel this codebase controls) or a human's own WhatsApp message, exactly as D-29 already established for account creation/reset and D-31 for workshops | **Contact form**: `contact-form.ts`/`contact-form-handler.ts` (and the Turnstile→rate-limit→SES chain built for them), the `/contact` route and Lambda (`infra/src/web-stack.ts`), `createSesContactEmailSender` (`ses.ts`), and `contact.form.enabled` (`flags.ts`) are deleted outright — not darkened, deleted, the same "genuinely unreachable code is removed" precedent D-29 set for the post-confirmation trigger. The contact page becomes a direct link to the clinic's WhatsApp Business number (a placeholder pending the owner naming the real one, the identical "`[Owner action]`" convention `site-config.ts`'s own `turnstileSiteKey` already uses). **Reminder sweep**: `reminder-sweep.ts`/`reminder-sweep-handler.ts`, its EventBridge rule, and GSI4 (`docs/adr/0002-database.md` confirms it exists for exactly this one query and no other) are deleted outright, along with `appointments.reminders.enabled`. Its removal has one unplanned further consequence: GSI4's reminder sweep was the *only* template ever marked `smsEligible` (`packages/i18n/src/notifications/index.ts`) and therefore the only real caller SMS sending could ever have had — `sms.ts`, `sms-provider.ts`, `sms-rate-limiter.ts`, `sms-spend-cap.ts`, TASK 0.5.3's own hard-cap mechanism, ADR-0008, and LL-02 (UK SMS provider onboarding) all become unreachable by the same removal, without themselves being touched. Per D-31's own precedent for genuinely-dead-but-harmless code (no live number was ever leased against LL-02, $0 spent, nothing to unwind), these are left in place, unused, rather than torn out as a separate effort — the same posture Stripe already holds. **Everything else**: `assignment.ts`'s `notifyBestEffort`/`notifyClinicianBestEffort` and their three call sites, `message.ts`'s `notifyOtherParty` and its one call site, and `clinician-admin.ts`'s deactivation notice are all removed; each file's core business logic (the actual approve/decline/reassign/send/deactivate mutation and its audit row) is untouched — only the "then tell someone" step goes. `assignment-handler.ts`/`message-handler.ts`/`clinician-admin-handler.ts` drop their now-unneeded `Notifier`/SES/Cognito-`AdminGetUser`-for-email wiring entirely. `notifications.ts` (the `Notifier` abstraction itself), `ses.ts`'s `createSesGenericEmailSender`, and the full `NOTIFICATION_TEMPLATES` registry are **not** touched — `stripe-webhook-handler.ts` (D-31's own dark, unremoved Stripe code) is still wired against all three, and leaving a generic, currently-uncalled abstraction in place costs nothing, the same reasoning that already governs every other piece of dead-but-kept infrastructure in this codebase. Full detail: [docs/runbooks/contact-form.md](../runbooks/contact-form.md), [docs/runbooks/appointment-reminders.md](../runbooks/appointment-reminders.md), [docs/runbooks/notifications.md](../runbooks/notifications.md) |
| D-33 | **Track B goes live for real patients before formal Indian legal review — the owner's own explicit, informed risk acceptance** (2026-08-30). Context established over two exchanges: closing LL-04 (ICO registration) surfaced the underlying fact — "it has no UK establishment, all indian customer, by indian clinicians. it just happened so that it's being build by a developer in the UK" — which applies equally to LL-05 (DPIA)/LL-06 (solicitor sign-off on R-04), both scoped to UK GDPR and moot in that specific form for the identical reason. That does not make the underlying obligation disappear: this is a clinical platform holding real diagnoses, care plans and assessments, and India's own **Digital Personal Data Protection Act, 2023 (DPDPA)** — plus whatever telemedicine/professional-conduct rules apply to the clinicians involved — governs it instead, unassessed as of this decision. The owner's own words, asked directly whether LL-05/LL-06 were still needed: **"I will get it done legally later on but I want to go live now for real patients. go with it."** This is not LL-04's kind of closure (a registration requirement that doesn't apply) — it is a conscious choice to proceed *before* a review that does still need to happen eventually, made knowingly rather than as an oversight, the same "deliberate, informed sequencing" posture D-29 already established for deferring DPIA/solicitor review past the synthetic-patient proof point, now extended by the owner's own direction past that point too, for real patients | LL-05/LL-06 are **reframed, not closed** — retitled to name Indian counsel and DPDPA rather than a UK DPO/solicitor, and left explicitly open in `08-long-lead.md`; nothing about this decision represents completed legal work. `docs/compliance/dpia-skeleton.md` is amended into a generic, clearly-labelled India-facing placeholder — every design decision already recorded there (schema separation, audit-log identifiers-only, the `visible{}`/`private{}` split, content-free notifications, no-stored-media video) stays accurate and jurisdiction-agnostic; a new section names DPDPA as the operative framework and states plainly that no completed legal review has occurred. `02-risk-register.md`'s R-04 (erasure vs C-03's never-delete) is reframed the same way — DPDPA gives its own data-principal correction/erasure rights, so the identical tension recurs under Indian law, still owner/counsel-only to resolve, never something this codebase decides or implements unilaterally. Track B's ten remaining flags (`patients.profile.enabled` through `video.turn.enabled`) are turned on immediately following this decision, with no code change: every Track B feature was already built and proven against a synthetic patient (D-29's own status update); this is the first time any of it runs against a real one. Full detail: [docs/runbooks/go-live.md](../runbooks/go-live.md), [docs/compliance/dpia-skeleton.md](../compliance/dpia-skeleton.md), [08-long-lead.md](08-long-lead.md) |
