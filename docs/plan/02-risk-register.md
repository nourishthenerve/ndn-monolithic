# 2. Risk register

| ID | Risk | L | I | Mitigation | Task |
|---|---|---|---|---|---|
| **R-01** | ~~§5 asks for ~150 SMS/month; C-02's £5 bought ~108 at Twilio's re-verified rate, ~172 at AWS End User Messaging's (chosen 2.3.2, ADR-0008) — closing the modelled shortfall, though reminder volume running ahead of the ~150/month estimate still exceeds the cap. workshop-confirmation-sms task: a second `smsEligible` template (`workshopRegistrationConfirmation`) now shares the same cap and provider.~~ **Fully moot as of D-32 (2026-08-30):** the appointment reminder — the sole remaining real SMS volume driver after D-31 made the workshop-confirmation template moot — is itself deleted; the clinician now reminds a patient over WhatsApp, by hand. No `smsEligible` template exists that anything still calls, so this risk's own arithmetic no longer describes a scenario that can occur. Kept struck-through, not deleted, so the reasoning stays legible | ~~Med~~ N/A | ~~High~~ N/A | ~~Email-primary (D-10); SMS reserved for the 1-hour reminder and (new) the workshop confirmation; defined degradation to email+in-app when capped; push notifications in Phase 6 relieve it permanently. Never silently drop a reminder — built as a property of the notification abstraction at 2.3.1 (every failure path writes a `degraded` delivery record naming the reason), not left to the SMS provider.~~ Moot, D-32 | ~~0.5.3, 2.3.1, 2.3.2, 3.4.4, workshop-confirmation-sms~~ |
| **R-02** | ~~SMS pumping fraud (NFR-09) burns the month's budget in minutes~~ **Moot as of D-32 (2026-08-30):** the reminder sweep was SMS's only real caller (any template); with it deleted, no code path can send an SMS at all, so no volume exists to pump. `sms.ts`/`sms-provider.ts`/the rate limiter and spend cap are unreachable, not deleted (same posture Stripe holds under D-31) — a real future SMS use would find this mitigation already built, not absent | ~~Med~~ N/A | ~~High~~ N/A | ~~+44-only destination allow-list, per-principal rate limit, hard block at cap, anomalous-velocity alarm, SMS only behind authentication. 2.3.2 is the task that first makes a real send possible and must prove the cap against the real provider, not a simulation.~~ Moot, D-32 | ~~0.5.3, 0.5.4, 2.3.2~~ |
| **R-03** | **TURN relay cost blow-out** (FR-VID-04) | Low | High | Cloudflare free tier covers ~11× worst case; concurrent-relay cap; egress telemetry + alarm; kill switch degrades to audio-only | 4.4.1, 4.4.2 |
| **R-04** | **GDPR erasure vs C-03 never-delete** | High | High | **Unresolved by design** — goes to your DPO/solicitor. Schema separates clinical (retention basis) from non-clinical PII so a future human-authorised field-level erasure needs no rewrite | 0.3.4, LL-06 |
| **R-05** | **Chatbot medical-device qualification** (FR-WEB-03) | Med | High | Deferred by D-05, **not resolved**. Reopens only after solicitor sign-off on scope | LL-07 |
| **R-06** | Public unauthenticated Lambda with delete rights over unversioned bucket | High | High | Phase 0 containment: strip Delete/Put, enable versioning, remove public URL; full decommission at G1 | 0.0.2 |
| **R-07** | Root access keys in use for CLI | High | High | IAM Identity Center + OIDC deploy role; you delete root keys | 0.1.1, D-28 |
| **R-08** | Merge to master deploys straight to production (C-06) | High | High | CI is the only gate: contract tests, ephemeral PR envs, canary alias, smoke test, auto-rollback | 0.6.x |
| **R-09** | Clinician-private data leaks to a patient (FR-DP-05) | Med | Critical | Field-level projection at the repository layer, not the handler; 100% coverage on the boundary; negative test per endpoint, forever. **Amended at Gate G1:** the chokepoint is built at **2.1.2**, one phase earlier than originally planned, so it exists before anything authenticated does — 3.2.x remains the task that wires the first real entity (assessments) through it. **Built 2026-08-21** — `services/api/src/projection.ts`, the `Projected<T>` type that no serialiser accepts without it, and the "negative test per endpoint, forever" convention: `docs/runbooks/private-field-boundary.md` | **2.1.2** (chokepoint), 3.2.x (wiring) |
| **R-10** | Cold starts breach p95 < 500ms (NFR-05) | Med | **High** | arm64, small bundles, no VPC on request path; measured at M5.1. If missed, provisioned-concurrency cost shown to you rather than absorbed. **Measured 2026-08-28 — missed.** 26 of ~32 functions check a feature flag via a live `SSMClient` call before anything else; every one of six sampled (across a range of bundle sizes) exceeded 500ms cold (656ms–1492ms), root-caused to first-connection TLS setup, not code size. Provisioned concurrency priced live (arm64, `eu-west-2`): ~$1.25/mo per always-on instance, ~$32–33/mo for all 26 — **not enabled**, no real traffic exists yet to justify it. See `docs/runbooks/load-testing.md`'s own TASK 5.1.2 section for the full finding and three named alternatives; owner decision still open | 5.1.2 |
| **R-11** | Log ingestion at $0.5985/GB silently eats the envelope | Med | Med | 14-day retention, sampled request logs, no debug logging in prod, log-volume alarm | 0.5.2 |
| **R-12** | SES stuck in sandbox at launch | Med | High | Production-access case raised in **Phase 0**, before it can block | LL-01 |
| **R-13** | CI exceeds 2,000 free GitHub minutes | Med | Low | Path-filtered workflows, cached installs, minute-usage check at each gate | 0.2.4 |
| **R-14** | Data residency vs global CDN edges (NFR-04) | Low | Med | PriceClass_100; static assets only at edge; **no patient data traverses CloudFront** — API responses are no-store | ADR-003 |
| **R-15** | G1 cutover blocked: CloudFront's global alias-uniqueness constraint prevents adding apex/`www` to `NdnWebStack` while another distribution still holds those aliases; confirmed by a live attempt (2026-08-15, ~72s error-page window, reverted). Believed until 2026-08-21 to be held by an unreachable third account | High | High | **Downgraded to scheduling, 2026-08-21.** The holder is the `ndn-frontend` Amplify app in our own `803129122420` (AWS Support case; verified directly) — not a third party. Release is one `amplify delete-domain-association`. No cooperation or queue involved. What remains is an owner-chosen window for the ~10–20 min apex/`www` outage the release-then-claim ordering forces | 1.6.1, `docs/runbooks/g1-cutover.md` |
| **R-16** | **D-29 (2026-08-29): WhatsApp-mediated account creation/recovery is social-engineering-able.** Moving identity verification from "prove you can read this mailbox" (Cognito's own OTP) to "a human decides who they're talking to on WhatsApp" trades a weak, automatable check for a stronger but entirely human, code-unaudited one. Failure mode: someone convincing staff they are a patient they are not (account creation), or that they are a specific existing patient (password reset, handing an attacker a live credential) | Med | High | **Mitigated by staff training, explicitly not by code** — the owner's own stated approach, and explicitly out of this codebase's scope to build or verify. Every creation and reset is audited with the acting principal (`audit.ts`'s `'create'`/`'reset-password'` actions), so a compromise is reconstructable after the fact even though it cannot be prevented by the platform itself. No automated identity-proofing (document upload, video call, knowledge-based verification) exists or is planned | D-29, `docs/runbooks/patient-account-provisioning.md` |
| **R-17** | **D-30 (2026-08-29): the principal clinician knows a new colleague's TOTP secret at enrolment time.** Ordinary Cognito TOTP enrolment guarantees the secret is seen by exactly one person — the account holder, scanning their own QR code. D-30's admin-side enrolment (built so no invite email is ever sent) necessarily generates and momentarily holds that same secret in `ClinicianAdminFunction` before handing it to the principal to relay, the same "weakened automated check, human trust instead" trade D-29 already made for patients, now on the clinician side. Failure mode: a principal (or anyone who saw the relay) could compute valid codes for that clinician's account indefinitely, undetected, unless the clinician later replaces the secret | Med | High | Every `create` is audited with the acting principal (`audit.ts`), so a misuse is reconstructable after the fact, same shape as R-16. The secret is returned once in the API response, never logged, never stored beyond the clinician pool itself (`clinician-admin.ts`'s own discipline, mirroring `patient-account-provisioning.md`'s password handling). **Not built in this pass, named rather than left implicit:** a self-service "replace my own MFA device" endpoint, callable only by the signed-in clinician themselves, would let them invalidate the admin-known secret the first time they sign in — the real fix, deferred as a follow-up task rather than silently assumed covered | D-30, `docs/runbooks/clinician-accounts.md` |
