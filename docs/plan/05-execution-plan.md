# 7. Execution plan

## Phase 0 — Foundations (nothing user-facing)

### TASK 0.0.1 — Commit the plan

**Milestone:** M0.0 · **Requirements:** — · **Decisions:** D-01…D-28 · **Depends on:** none · **Blocks:** all · **Size:** S · **Cost:** £0.00
**Context.** The plan must be versioned and reviewed like code before any of it is executed.
**Files:** create `docs/plan/00-index.md`, `00-conventions.md`, `01-decisions.md`, `02-risk-register.md`, `03-cost-model.md`, `04-data-model-rbac.md`, `05-execution-plan.md`, `06-gate-checklists.md`, `07-traceability.md`, `08-long-lead.md`, `09-self-audit.md`, `docs/adr/0001..0016-*.md`, `docs/compliance/dpia-skeleton.md`.
**Steps.** 1. `git init` state already exists; create branch `docs/0-0-1-commit-plan`. 2. Write each manifest document verbatim from this plan. 3. Open PR.
**Tests.** Markdown lint + link check in CI (added by 0.2.1; until then, manual link check documented in the PR).
**Verification:** `pnpm docs:lint` (once CI exists) · reviewer confirms every §0.5 file present.
**Flag:** none. **DoD:** all manifest files present; PR opened on a feature branch with the required body. **Rollback:** revert the commit. **Do NOT:** write application code.

### TASK 0.0.2 — Contain the legacy public delete path

**Milestone:** M0.0 · **Requirements:** C-03, C-11, §6.7 · **Decisions:** D-02, D-03 · **Risks:** R-06 · **Depends on:** 0.0.1 · **Blocks:** 0.3.x · **Size:** S · **Cost:** £0.00
**Context.** A publicly invokable Lambda in account 803129122420 holds `s3:DeleteObject` over an unversioned bucket. Every data-protection guard we build later is theatre while this exists. This task is **non-destructive**: it removes capability and adds recoverability, and deletes nothing.
**Steps.** 1. Enable **versioning** on bucket `nourishthenerve` (eu-west-2). 2. Replace `LambdaS3AccessPolicy` with a read-only statement (`s3:GetObject`, `s3:ListBucket`) — **remove `PutObject` and `DeleteObject`**. 3. Delete the Function URL (`AuthType: NONE`) after confirming whether the live brochure site calls it; if it does, keep the URL until G1 and instead restrict CORS to the site origin. 4. Record findings in `docs/runbooks/legacy-estate.md`.
**Interfaces:** IAM policy document; S3 versioning configuration.
**Tests.** Integration: assert the role can `GetObject` and **cannot** `PutObject`/`DeleteObject` (IAM policy simulator, asserted in CI). Negative: unauthenticated call to the Function URL fails or is gone. Regression: brochure site still renders (synthetic fetch of `/` returns 200).
**Verification:** `aws s3api get-bucket-versioning --bucket nourishthenerve` → `Enabled`; `aws iam simulate-principal-policy` returns `implicitDeny` for `s3:DeleteObject`.
**Flag:** none (infrastructure). **DoD:** versioning on; delete/put denied; documented; cost delta £0. **Rollback:** re-attach the previous policy version (versioning stays — it is never harmful). **Do NOT:** delete any object, the bucket, or the `clients/` prefix. **Do NOT** delete the Lambda yet — that is task 1.6.1 at cutover.

### TASK 0.1.1 — Provision the AWS account and identity baseline

**Milestone:** M0.1 · **Requirements:** NFR-03 · **Decisions:** D-01, D-28 · **Risks:** R-07 · **Depends on:** 0.0.1 · **Size:** M · **Cost:** £0.00
**Context.** All subsequent infrastructure lands in a dedicated member account with no human long-lived credentials.
**Steps.** 1. Create the Organization (if absent) in 803129122420; create member account `ndn-prod` with a unique root email; enable MFA on its root user and stop using it. 2. Enable IAM Identity Center; create `NDNAdmin` permission set. 3. Create the GitHub OIDC provider and `ndn-deploy` role with a trust policy scoped to `repo:nourishthenerve/ndn-monolithic:ref:refs/heads/main` **and** pull-request contexts for ephemeral envs. 4. Enable CloudTrail (management events, free tier) and Cost Explorer. 5. Document root-key deletion as **your** action.
**Tests.** Integration: assume `ndn-deploy` from a CI dry-run and confirm it can `cloudformation:DescribeStacks` and **cannot** `iam:CreateUser`. Negative: OIDC trust rejects a token from another repository.
**Verification:** `aws sts get-caller-identity` from CI shows the assumed role, not a user.
**DoD:** no IAM users with access keys exist in the new account. **Rollback:** delete the role; the account remains harmless and free.
**Do NOT:** copy root access keys anywhere. **Do NOT** delete the existing account's keys yourself — that is the owner's action.

### TASK 0.1.2 — Monorepo scaffolding, linting, formatting, type checking

**Milestone:** M0.1 · **Requirements:** NFR-08 · **Decisions:** D-15, D-16 · **Depends on:** 0.0.1 · **Size:** M · **Cost:** £0.00
**Files:** `package.json`, `pnpm-workspace.yaml`, `tsconfig.base.json`, `eslint.config.js`, `.prettierrc`, `vitest.config.ts`, plus empty workspace packages per §4.
**Steps.** 1. pnpm workspaces with the §4 layout. 2. Strict TS (`strict`, `noUncheckedIndexedAccess`). 3. ESLint + Prettier + import ordering. 4. Vitest with coverage thresholds configured but not yet enforced (0.2.3 enforces).
**Tests.** Unit: one trivial test per package proving the harness runs. **Verification:** `pnpm -r lint && pnpm -r typecheck && pnpm -r test`.
**DoD:** clean run on a fresh clone. **Rollback:** revert. **Do NOT:** add runtime dependencies not needed by a shipped feature.

### TASK 0.2.1 — CI pipeline with quality gates

**Milestone:** M0.2 · **Requirements:** C-06, C-09, NFR-03, NFR-06 · **Risks:** R-08, R-13 · **Depends on:** 0.1.1, 0.1.2 · **Size:** M · **Cost:** £0.00
**Steps.** 1. `.github/workflows/ci.yml`: install (cached) → lint → typecheck → unit → integration → coverage thresholds → dependency audit → secret scan. 2. Path filters so docs-only PRs skip heavy jobs (protects the 2,000-minute budget). 3. Branch protection requiring CI green. 4. Job summary prints CI minutes used.
**Tests.** Meta: a deliberately failing lint rule on a scratch branch fails the build (evidence in PR).
**Verification:** `gh run list` shows required checks. **DoD:** cannot merge without green CI. **Do NOT:** add `continue-on-error` to any quality gate.

### TASK 0.3.1 — Destructive-primitive lint rule

**Milestone:** M0.3 · **Requirements:** C-03, §6.7 · **Risks:** R-06 · **Depends on:** 0.2.1 · **Size:** M · **Cost:** £0.00
**Context.** Code-layer half of the two-layer guard. Must exist before anything writes data.
**Steps.** 1. Custom ESLint rule banning `DeleteItemCommand`, `DeleteObjectCommand`, `DeleteObjectsCommand`, `BatchWriteItem` with `DeleteRequest`, raw `DELETE`/`TRUNCATE`/`DROP` SQL, and `s3:DeleteObject*` strings in IaC — across `services/`, `apps/`, `packages/`, `infra/` **and** `tests/`. 2. Allowlist only `infra/` constructs that *deny* the action. 3. Wire into CI as a blocking check.
**Tests.** **The guard's own test is the deliverable:** a fixture file containing `DeleteObjectCommand` must fail lint; CI proves the failure. Negative: allowlisted deny-policy file passes.
**Verification:** `pnpm lint:no-destructive` exits non-zero on the fixture.
**DoD:** a PR introducing a destructive primitive cannot merge. **Do NOT:** allow per-file disable comments for this rule.

### TASK 0.3.2 — IAM deny guardrails

**Milestone:** M0.3 · **Requirements:** §6.7, NFR-03 · **Depends on:** 0.1.1, 0.3.1 · **Size:** M · **Cost:** £0.00
**Steps.** 1. Runtime role policy with explicit `Deny` on `s3:DeleteObject`, `s3:DeleteObjectVersion`, `dynamodb:DeleteItem`, `dynamodb:DeleteTable` against protected resources. 2. S3 bucket policy denying the same to the runtime principal. 3. Separate, unused **break-glass role** requiring MFA, documented in a runbook and *not* implemented in application code (§6.8).
**Tests.** Integration against emulated/ephemeral AWS: runtime role `DeleteObject` → `AccessDenied`; `PutObject` to a media prefix → succeeds; policy simulator assertions in CI.
**DoD:** deletion denied at code **and** IAM layers, both proven by tests. **Do NOT:** implement any break-glass deletion code path.

### TASK 0.3.3 — Soft-delete + audit primitives

**Milestone:** M0.3 · **Requirements:** §6.2–6.4, FR-X-03 · **Depends on:** 0.3.1 · **Size:** M · **Cost:** £0.00
**Steps.** Repository base class enforcing `created_at`, `updated_at`, `status` on every write; append-only audit writer; versioned-record helper (new version, never overwrite).
**Tests.** Unit: "delete" sets a status flag and the record remains readable by ID; audit entry written for every mutation; version N+1 never mutates version N. Negative: attempting an in-place overwrite of a clinical record throws.
**DoD:** no repository method exists that removes a row. **Do NOT:** add one "for tests".

### TASK 0.3.4 — Schema separation for future lawful erasure

**Milestone:** M0.3 · **Requirements:** §6 note, NFR-04 · **Risks:** R-04 · **Depends on:** 0.3.3 · **Size:** S · **Cost:** £0.00
**Steps.** Split every person record into `clinical{}` (retention basis) and `personal{}` (name, contact, marketing prefs) attributes so a future human-authorised erasure of specific non-clinical fields needs no migration. Document in the DPIA skeleton.
**Tests.** Unit: projection helpers prove the two sets are independently addressable.
**DoD:** DPIA skeleton records the split. **Do NOT:** implement erasure.

### TASK 0.4.1 — IaC baseline: DNS, certificate, CDN, storage, health check

**Milestone:** M0.4 · **Requirements:** C-07, C-08, NFR-01 · **Depends on:** 0.2.1, 0.3.2 · **Size:** M · **Cost:** +£0.42/mo
**Steps.** 1. CDK app: S3 site bucket (versioned, private, OAC), CloudFront PriceClass_100, ACM cert in us-east-1, HTTP API + `GET /health` Lambda (arm64). 2. Route 53 records for a **staging hostname only** (`next.nourishthenerve.com`) — the apex stays on the legacy site until G1. 3. Security headers + CSP via CloudFront response-headers policy. 4. First production deploy: an "it's alive" page.
**Tests.** Integration: `/health` returns 200 with a version string; CloudFront serves the page over TLS; security headers present. Negative: direct S3 URL is denied (OAC enforced).
**Verification:** `curl -sI https://next.nourishthenerve.com` → 200 + HSTS/CSP.
**Flag:** none. **DoD:** production deploy succeeded from CI via OIDC; apex site untouched. **Rollback:** `cdk destroy` of the new stack leaves the legacy site unaffected. **Do NOT:** change the apex or `www` DNS records in this task.

### TASK 0.5.1 — Budgets and cost alarms

**Milestone:** M0.5 · **Requirements:** C-01, NFR-02 · **Depends on:** 0.4.1 · **Size:** S · **Cost:** £0.00 (≤2 budgets free)
**Steps.** Budget at £20 with alerts at 50/75/90%; anomaly detection; alarm → email. Cost allocation tags on every resource.
**Tests.** Integration: simulate a threshold breach via a forced budget notification and assert the alert fires (evidence in PR — the brief requires alarms *proven* at G0).
**DoD:** alarms demonstrably fire in a test. **Do NOT:** rely on Cost Explorer API polling (it bills per request).

### TASK 0.5.2 — Log retention and volume control

**Milestone:** M0.5 · **Requirements:** FR-X-05 · **Risks:** R-11 · **Depends on:** 0.4.1 · **Size:** S · **Cost:** ~£1.00/mo
**Steps.** 14-day retention on every log group by CDK default; sampled request logging; alarm on ingestion GB/day.
**Tests.** Integration: new log groups are created with 14-day retention (assert in CDK snapshot test).
**DoD:** no log group has infinite retention.

### TASK 0.5.3 — SMS hard-cap mechanism

**Milestone:** M0.5 · **Requirements:** C-02, C-11, NFR-09 · **Risks:** R-01, R-02 · **Depends on:** 0.5.1 · **Size:** M · **Cost:** £0.00 (no SMS sent yet)
**Context.** Built **before** any SMS can be sent, so the cap can never be breached even once.
**Steps.** 1. Atomic monthly counter in DynamoDB (conditional update) holding spend in pence. 2. `canSend()` returns false at the £5 cap — **a block, not an alert**. 3. `+44`-only destination allow-list with E.164 normalisation. 4. Per-principal rate limit. 5. Provider-side monthly spend limit set as an independent backstop. 6. Kill switch parameter in SSM.
**Interfaces:** `sendSms(to: E164, template, vars): Result<Sent, Blocked|Capped|NotUk|RateLimited>`.
**Tests.** Unit: cap boundary at £4.99/£5.00/£5.01; non-UK numbers rejected (+1, +33, +44 spoofs like `+4401`); E.164 normalisation. Integration (emulated): 200 concurrent sends stop at the cap with no overshoot; counter is atomic under contention. **Cost-abuse:** allow-list rejects non-UK; rate limit returns the correct error; kill switch blocks everything.
**Flag:** `sms.enabled` — default **off**, you flip it. **DoD:** tests prove the cap blocks. **Do NOT:** send a real SMS in any test.

### TASK 0.6.1 — Feature flags

**Milestone:** M0.6 · **Requirements:** §10 · **Decisions:** D-23 · **Depends on:** 0.4.1 · **Size:** S · **Cost:** £0.00
**Steps.** SSM-backed flag store, cached in-process with short TTL; typed accessor; default-off for every new flag.
**Tests.** Unit: unknown flag returns false; cache honours TTL. **DoD:** incomplete work can merge dark.

### TASK 0.6.2 — Canary deploy, smoke test, auto-rollback

**Milestone:** M0.6 · **Requirements:** C-06, C-07, NFR-01 · **Risks:** R-08 · **Depends on:** 0.6.1 · **Size:** M · **Cost:** £0.00
**Steps.** 1. Lambda alias with weighted routing (10% → 100% over 5 min). 2. CloudWatch alarm on 5xx/latency wired to the deployment. 3. Post-deploy smoke test hitting `/health` and a real page. 4. Failure → automatic alias rollback. 5. `docs/runbooks/rollback.md`.
**Tests.** Integration: deliberately deploy a failing build to an ephemeral environment; assert automatic rollback and that the previous version still serves.
**DoD:** rollback demonstrated, not described. **Do NOT:** allow a deploy path that bypasses the alias.

### TASK 0.6.3 — Ephemeral per-PR environments

**Milestone:** M0.6 · **Requirements:** §10 · **Depends on:** 0.6.2 · **Size:** M · **Cost:** £0.00 standing
**Steps.** CI job deploys a uniquely-named stack per PR, runs integration + contract + a11y tests, then **destroys it in the same run** (including on failure, via `always()`).
**Tests.** Meta: a PR leaves no residual stack — CI asserts stack count returns to baseline.
**DoD:** zero standing cost proven by a stack-count assertion. **Do NOT:** leave orphaned stacks on failure.

**Gate G0:** production deploys and rolls back safely · guards demonstrably block a destructive change · budget alarms fire in a test · no long-lived credentials exist.

## Phase 1 — Public website

### TASK 1.1.1 — Design system + tokens, WCAG 2.2 AA primitives, reduced-motion support

**Milestone:** M1.1 · **Requirements:** FR-X-02 · **Decisions:** ADR-0017 · **Depends on:** 0.1.2, 0.4.1, 0.6.2 · **Blocks:** 1.1.2, 1.1.3, 1.2.1 onward · **Size:** M · **Cost:** £0.00
**Context.** `packages/ui` and `apps/web` are still the empty scaffolds TASK 0.1.2 created; every later Phase 1 page depends on this task's tokens/primitives. No ADR pinned a frontend rendering framework (ADR-0003 covers hosting/delivery only) — this task makes that call, recorded as [ADR-0017](../adr/0017-frontend-framework.md).
**Files:** `packages/ui/package.json`, `packages/ui/src/tokens/{color,space,type,motion}.ts`, `packages/ui/src/components/{Button,Link,Input,Heading,Card,SkipLink,VisuallyHidden}.tsx`, `apps/web/package.json`, `apps/web/astro.config.mjs`, `apps/web/src/pages/index.astro`.
**Steps.** 1. Adopt ADR-0017: **Astro with React islands, TypeScript, static output** (`astro build` → `dist/`) — zero-JS-by-default (helps perf/reduced-motion budgets), first-class file-based i18n routing (feeds 1.1.2), and its static output drops straight into the existing `BucketDeployment` in `infra/src/web-stack.ts` (TASK 0.4.1) with no infra change beyond the source path. 2. `packages/ui/src/tokens/`: CSS custom properties + typed TS export; colour pairs pre-checked for ≥4.5:1 (body text) / ≥3:1 (large text/UI). 3. `packages/ui/src/components/`: primitives built from semantic HTML, every interactive target ≥24×24 CSS px (WCAG 2.2 SC 2.5.8), a token-driven visible `:focus-visible` state (never `outline: none` without a replacement). 4. `motion.ts` + a global rule: every transition wrapped so `@media (prefers-reduced-motion: reduce)` collapses duration to ~0 (SC 2.3.3). 5. Wire `packages/ui` as an `apps/web` dependency; replace `infra/assets/site/index.html`'s placeholder with the real build output as the `BucketDeployment` source.
**Interfaces:**

```ts
// packages/ui/src/tokens/color.ts
export interface ColorToken { css: string; contrastOnLight: number; contrastOnDark?: number }
export const colorTokens: Record<'text' | 'textMuted' | 'brand' | 'focusRing' | 'error', ColorToken>;
```

**Tests.** Unit: a contrast-ratio calculator run against every token pair asserts ≥4.5:1/≥3:1, failing the build if a token regresses. Unit: `Button`/`Input`/`Link` tests assert computed tap-target ≥24px and a non-empty `:focus-visible` style. Integration (Playwright): `page.emulateMedia({ reducedMotion: 'reduce' })` on an animated component asserts effectively-zero duration. Negative: a token deliberately set to 3.9:1 fails the contrast test (proves the guard fires, not just exists).
**Verification:** `pnpm --filter @ndn/ui test && pnpm --filter @ndn/web run build` — build succeeds, `apps/web/dist/index.html` exists.
**Flag:** none. **DoD:** `pnpm --filter @ndn/ui test` passes including contrast/target-size/reduced-motion assertions; the 0.4.1 placeholder page is replaced by the real build. **Rollback:** revert the branch; `web-stack.ts`'s `BucketDeployment` source reverts to `infra/assets/site`, redeploy. **Do NOT:** hand-pick colours without running the contrast checker; ship a component whose only focus indicator was removed via `outline: none`; add a UI framework beyond what ADR-0017 records.

### TASK 1.1.2 — i18n framework: ICU catalogues, locale routing, RTL-safe logical CSS, missing-translation fallback, lint rule banning hard-coded strings

**Milestone:** M1.1 · **Requirements:** FR-X-01 · **Decisions:** D-04, ADR-0012 · **Depends on:** 1.1.1 · **Blocks:** 1.2.1 onward (every user-facing string) · **Size:** M · **Cost:** £0.00
**Context.** D-04 requires "no hard-coded copy anywhere, ever," with RTL-safe primitives from day one even though only English ships at launch — ADR-0012 notes reversal cost is low if done now, high if retrofitted.
**Files:** `packages/i18n/src/index.ts`, `packages/i18n/src/locales/en.json`, `packages/i18n/src/format.ts`, `packages/eslint-plugin-i18n/` (new), `eslint.config.js`, `apps/web/astro.config.mjs`.
**Steps.** 1. `packages/i18n`: ICU MessageFormat catalogues via `intl-messageformat` (new dependency) — one JSON file per locale, keyed by dotted message ID. 2. `t(key, vars?, locale?)`: falls back to the `en` catalogue and logs a structured, non-PII warning — never throws, never renders a raw key. 3. Locale routing: Astro's file-based `[locale]` routing, URL-prefixed (`/en/...`) from day one so a second language is additive routing later, not a URL-scheme change. 4. Audit and convert `packages/ui`'s tokens/components (1.1.1) to CSS logical properties (`margin-inline-start`, `padding-block`, `inset-inline-end`); `dir` driven by locale metadata, default `ltr`. 5. New rule `ndn/no-hardcoded-strings` (`packages/eslint-plugin-i18n`, same plain-ESM+`checkJs` pattern as `packages/eslint-plugin-no-destructive`) banning literal user-facing text in JSX/Astro output not wrapped by `t()`; registered `'error'` for `apps/web/**` in root `eslint.config.js`.
**Interfaces:**

```ts
// packages/i18n/src/index.ts
export type Locale = 'en'; // extended additively when a language is named (LL-08)
export function t(key: string, vars?: Record<string, string | number>, locale?: Locale): string;
```

**Tests.** Unit: `t()` against a key missing from a hypothetical non-`en` locale falls back to `en`, emits one warning, never throws. Unit: ICU plural/date formatting round-trips correctly. Unit (RuleTester): a raw JSX string literal fails `ndn/no-hardcoded-strings`; the same text wrapped in `t()` passes. Integration: a fixture rendered `dir="rtl"` shows no broken logical-property fallback. Negative: a missing catalogue key never crashes the render.
**Verification:** `pnpm --filter @ndn/i18n test && pnpm run lint` (new rule runs inside the existing required lint gate).
**Flag:** none. **DoD:** no user-facing string in `apps/web` bypasses `t()`, enforced by lint not review; `/en/...` routing works; RTL logical-property audit is documented. **Rollback:** revert the branch (avoid reverting after go-live — would change live URLs). **Do NOT:** hard-code date/plural formatting outside the ICU catalogue; add a runtime machine-translation dependency (ADR-0012 rejects this).

### TASK 1.1.3 — CI accessibility checks (axe on every page, keyboard-path tests)

**Milestone:** M1.1 · **Requirements:** FR-X-02, NFR-06 · **Depends on:** 1.1.1, 1.1.2, 0.6.3 · **Blocks:** 1.2.1 · **Size:** S · **Cost:** £0.00
**Context.** TASK 0.6.3's `tests/pr-env/a11y.test.ts` (axe-core + jsdom, one static page) was explicitly built as a lightweight stand-in for the real-browser a11y suite this task adds across every page, now that real routes exist.
**Files:** `tests/pr-env/a11y-full.test.ts`, `tests/pr-env/keyboard.test.ts`, `apps/web/src/routes.ts` (new route manifest), `.github/workflows/ci.yml`.
**Steps.** 1. `apps/web/src/routes.ts`: a checked-in list of every public route (grows through 1.2.x–1.5.x) — the single source both the a11y suite and any future sitemap read from, so a new page can't silently skip the gate. 2. `a11y-full.test.ts`: Playwright + `@axe-core/playwright` (new dependencies), navigates every route against the live ephemeral PR URL (`PR_ENV_BASE_URL`, from 0.6.3), asserts zero `serious`/`critical` violations per page. 3. `keyboard.test.ts`: keyboard-only walkthroughs — tab order matches visual order, a skip-link is first-focusable and jumps to `<main>`, no unintended focus trap, `Enter`/`Space` activate custom widgets. 4. Wire both into the existing `pr-environment` CI job (0.6.3) rather than a new deploy. 5. Promote into `ci-summary`'s required gate once the first live run is green (same pattern already used twice in this repo).
**Tests.** This task's own deliverable is the suite (Steps 2–3). Meta: a scratch page missing a `<main>` landmark is caught by the axe run, then removed.
**Verification:** `pnpm run test:pr-env` against a real ephemeral stack; `gh run view` shows the job green.
**Flag:** none. **DoD:** every route in `routes.ts` is exercised by the axe check on every PR; a keyboard-only pass reaches every route's primary actions. **Rollback:** revert the branch — additive CI only. **Do NOT:** exempt a route from the axe check without a written, reviewed justification next to its `routes.ts` entry; downgrade a `serious`/`critical` finding to non-gating to make CI pass.

### TASK 1.2.1 — Public pages + navigation + footer with configurable social links

**Milestone:** M1.2 · **Requirements:** FR-WEB-05 · **Depends on:** 1.1.1, 1.1.2, 1.1.3 · **Blocks:** 1.2.2, 1.2.3, 1.3.2, 1.4.1, 1.4.2, 1.5.1 · **Size:** M · **Cost:** £0.00
**Context.** First real page content on the new site — replaces the 0.4.1 "it's alive" placeholder and gives every later Phase 1 task a shell to plug into.
**Files:** `apps/web/src/layouts/BaseLayout.astro`, `apps/web/src/pages/[locale]/{index,about,services}.astro`, `apps/web/src/components/{Nav,Footer}.astro`, `apps/web/src/config/social-links.ts`.
**Steps.** 1. `BaseLayout.astro`: `<html lang>`/`dir` from locale metadata, skip-link, `<main>` landmark, imports `packages/ui` tokens. 2. `Nav.astro`/`Footer.astro` built from `packages/ui` primitives only; nav links to every route in `apps/web/src/routes.ts`. 3. `social-links.ts`: a single typed, checked-in config array the footer renders from — "configurable" means editing this file, not markup or a redeploy pipeline of its own. 4. Home/About/Services/Contact-stub pages, all copy `t()`-wrapped (1.1.2's lint rule enforces this). 5. Add every new route to `routes.ts`; point `web-stack.ts`'s `BucketDeployment` at the real build if not already done.
**Tests.** Unit: `social-links.ts` validated by a Zod schema — a malformed entry fails the build, not a runtime 500. Integration (Playwright): every nav link resolves 200; footer renders the configured links. Integration: 1.1.3's a11y/keyboard suite passes against every new route.
**Verification:** `pnpm --filter @ndn/web run build`; 1.1.3's pr-environment suite green against these routes.
**Flag:** none. **DoD:** nav/footer present on every page; every link resolves; social links change via one config file. **Rollback:** revert the branch; `BucketDeployment` reverts to the placeholder. **Do NOT:** hard-code social URLs in `Footer.astro`; add a page not listed in `routes.ts`.

### TASK 1.2.2 — Legal pages (privacy, cookies, terms, accessibility statement, clinical disclaimer) as i18n placeholders

**Milestone:** M1.2 · **Requirements:** FR-WEB-07 · **Depends on:** 1.2.1, 1.1.2 · **Size:** S · **Cost:** £0.00
**Context.** These must exist and be linked before Gate G1, while LL-05 (DPIA) and LL-06 (solicitor sign-off on the erasure tension) remain open per `08-long-lead.md` — this task ships page shells and marked placeholder copy, not final legal text.
**Files:** `apps/web/src/pages/[locale]/legal/{privacy,cookies,terms,accessibility-statement,clinical-disclaimer}.astro`, `packages/i18n/src/locales/en.json` (new `legal.*` keys).
**Steps.** 1. One route per document, added to `routes.ts` and linked from the footer. 2. Each body is an explicit, visibly-marked placeholder banner ("This page is a placeholder pending legal review — do not rely on it."), not just a code comment. 3. Accessibility statement states this project's actual a11y posture (WCAG 2.2 AA target, 1.1.3's automated checks), not generic boilerplate. 4. Clinical disclaimer: placeholder noting the public site provides no diagnosis/treatment; real wording awaits LL-06.
**Tests.** Unit: every legal route's rendered output contains the placeholder-notice banner (fails if someone quietly removes it). Integration: all five pages return 200 and pass the 1.1.3 a11y/keyboard suite.
**Verification:** `pnpm --filter @ndn/web run build`; reviewer confirms all five pages are reachable from the footer.
**Flag:** none. **DoD:** five pages live, linked, each visibly marked as a placeholder pending review. **Rollback:** revert the branch. **Do NOT:** write real privacy/terms/clinical claims as if reviewed; remove the placeholder banner without a recorded LL-05/LL-06 sign-off.

### TASK 1.2.3 — Cookie consent mechanics; remove the http:// Google Fonts dependency, self-host fonts

**Milestone:** M1.2 · **Requirements:** FR-WEB-07, NFR-04 · **Depends on:** 1.2.1, 1.2.2 · **Size:** M · **Cost:** £0.00
**Context.** The legacy site loads Google Fonts over plain `http://` — mixed content and an uncontrolled third-party data flow. The new site is greenfield so this isn't a migration: TASK 0.4.1's CloudFront `ResponseHeadersPolicy` CSP (`default-src 'self'`, no `font-src` override) would already silently block a Google Fonts `<link>` — this task makes self-hosting explicit and adds the consent mechanism FR-WEB-07 needs.
**Files:** `apps/web/src/assets/fonts/*.woff2`, `apps/web/src/styles/fonts.css`, `packages/ui/src/components/CookieBanner.tsx`, `apps/web/src/scripts/consent.ts`.
**Steps.** 1. Self-host the chosen typeface(s) as `.woff2` (Latin subset at launch), `@font-face` in `fonts.css`; no external font `<link>`/`@import` anywhere. 2. `CookieBanner` (built from 1.1.1 primitives): shown on first visit, links to `/legal/cookies` (1.2.2), stores the accept/reject choice in a first-party cookie; no non-essential cookie set before consent. 3. `consent.ts`: a typed gate any future non-essential script must check first. 4. Confirm `web-stack.ts`'s CSP adds no `font-src` exception — document this as deliberate, not an oversight.
**Interfaces:**

```ts
// apps/web/src/scripts/consent.ts
export type ConsentCategory = 'essential' | 'analytics';
export function hasConsent(category: ConsentCategory): boolean;
export function setConsent(categories: ConsentCategory[]): void;
```

**Tests.** Unit: `hasConsent('essential')` always `true`; `hasConsent('analytics')` `false` until set. Integration (Playwright): no font request leaves the page to a non-`self` origin; banner is keyboard-operable (1.1.3); rejecting still lets the site function. Negative: with no consent cookie set, no non-essential script runs.
**Verification:** `curl -sI https://next.nourishthenerve.com/ | grep -i content-security-policy` shows no external `font-src`; the Playwright network-request assertion above.
**Flag:** none. **DoD:** zero third-party font/network requests on first paint; the banner gates every non-essential category; the cookie policy page is linked and accurate. **Rollback:** revert the branch. **Do NOT:** add a Google Fonts (or any third-party font CDN) reference; set a non-essential cookie before consent.

### TASK 1.3.1 — Blog model + per-language content + keyword tagging

**Milestone:** M1.3 · **Requirements:** FR-WEB-01 · **Decisions:** D-07 · **Depends on:** 0.3.1, 0.3.2, 0.3.3, 0.3.4, 0.4.1, 1.1.2 · **Blocks:** 1.3.2, 1.4.2, 1.5.1 · **Size:** M · **Cost:** £0.00 net-new — this is the task that actually incurs the DynamoDB/API Gateway line items `03-cost-model.md`'s M1 column already modelled (~$0.05 DynamoDB + part of the API Gateway line), not a new unmodelled cost.
**Context.** No DynamoDB table is deployed anywhere yet — every Phase 0 data-protection primitive (0.3.1–0.3.4) was built against an in-memory seam specifically so a real table could be dropped in later. Blog content is the first entity to actually deploy `04-data-model-rbac.md`'s single-table design (`CONTENT#<id>`/`META`, GSI2 keyword→content) for real, in `ndn-prod`.
**Files:** `infra/src/data-stack.ts` (new), `infra/bin/app.ts`, `services/api/src/dynamo-store.ts` (new; adds `@aws-sdk/client-dynamodb`/`@aws-sdk/lib-dynamodb`), `services/api/src/content-repository.ts`, `packages/shared-types/src/content.ts`.
**Steps.** 1. `NdnDataStack`: one DynamoDB table, `PAY_PER_REQUEST` (D-07), PITR enabled (D-21, cheap even unused until the Phase 5 restore drill), `RemovalPolicy.RETAIN`. Only **GSI2** (keyword→content) is created now — GSI1/3/4 land with the Phase 2/3 tasks that first need them; adding a GSI later is additive, not a migration. 2. Call `attachDestructiveActionGuardrail` (`infra/src/guardrails.ts`, built in 0.3.2, never yet attached to a real resource) against the new table and the new runtime Lambda role — its first real exercise. 3. `dynamo-store.ts`: a `KeyValueStore<T>` implementation (the 0.3.3 interface) backed by `DynamoDBDocumentClient` — `Repository`/`VersionedRepository` are unchanged; only the store swaps from `InMemoryStore`. 4. `content-repository.ts`: `PK = CONTENT#<id>` / `SK = META`, per-language body on one row (`translations: Record<Locale, {...}>`, not one row per language, so GSI2's keyword query stays single-hop); `keywords: string[]` projected onto GSI2 via `TransactWriteItems` so an item with N keywords is discoverable by all N. 5. Minimal read API only: `GET /content?keyword=` returns published content — authoring is 1.3.2's job.
**Interfaces:**

```ts
// packages/shared-types/src/content.ts
export interface ContentItem extends BaseRecord {
  id: string;
  contentType: 'blog';
  status: 'draft' | 'published' | 'unpublished'; // never 'deleted'
  keywords: string[];
  translations: Record<Locale, { title: string; body: string; excerpt: string }>;
}
```

**Tests.** Unit: `content-repository.test.ts` against `InMemoryStore` proves keyword-projection and status transitions. Integration: an item tagged with 3 keywords is retrievable via all 3 GSI2 queries; `status: 'unpublished'` is excluded from the public read API but still `GetItem`-able by id. Negative: the guardrail denies `DeleteItem` against the real table for the runtime role (policy simulator, real resource this time, not an illustrative ARN).
**Verification:** `pnpm --filter @ndn/infra run synth`; `aws dynamodb describe-table` shows PITR + GSI2; `aws iam simulate-principal-policy` on the new runtime role → `dynamodb:DeleteItem` `explicitDeny`.
**Flag:** `content.readApi.enabled` — default off until 1.3.2 can populate real content. **DoD:** the table and GSI2 exist in `ndn-prod`; the guardrail is attached and proven against it; a content item is queryable by keyword. **Rollback:** `cdk destroy NdnDataStack` — `RemovalPolicy.RETAIN` keeps the table even if the stack is torn down. **Do NOT:** create GSI1/3/4 speculatively; add a `deleteContent`/`removeContent` method anywhere.

### TASK 1.3.2 — Blog authoring/editing, publish/unpublish (never delete), SEO metadata + hreflang

**Milestone:** M1.3 · **Requirements:** FR-WEB-01 · **Depends on:** 1.3.1, 0.3.3, 0.6.1 · **Blocks:** 1.4.2, 1.5.1 (reuse the admin authorizer this task introduces) · **Size:** M · **Cost:** £0.00
**Context.** Phase 2 (Cognito/RBAC) doesn't exist yet — `09-self-audit.md` itself flags this ordering tension and recommends (not yet actioned — the plan's author left it as the account owner's call at G1) pulling M2.3 forward. Authoring needs *some* access control before G1, so this task adds one narrow, explicitly-temporary admin-token gate rather than waiting on Phase 2.
**Files:** `services/api/src/admin-auth.ts` (new), `services/api/src/content-authoring.ts`, `apps/web/src/pages/[locale]/blog/{index,[slug]}.astro`, `packages/shared-types/src/content.ts`.
**Steps.** 1. `admin-auth.ts`: single bearer-token check (`ADMIN_API_TOKEN`, SSM SecureString, D-14) — commented explicitly as a Phase-1-only bridge superseded by Phase 2's Cognito RBAC; reused by 1.4.2/1.5.1, not reimplemented. 2. Authoring endpoints (`POST /content`, `PATCH /content/:id`, `POST /content/:id/publish`, `.../unpublish`) — all admin-token-gated, all via `Repository.update` (0.3.3). No endpoint calls anything delete-shaped — `unpublish` only ever transitions `status`. 3. Per-language `title`/`description`/canonical URL on `translations[locale]`, rendered as `<title>`/`<meta description>`/`<link canonical>`. 4. hreflang: emit `<link rel="alternate" hreflang="{locale}">` + `x-default` per published translation actually present — a single-locale post emits exactly one tag. 5. Public pages read only `status: 'published'` via 1.3.1's read API; an unpublished post's URL resolves to a "no longer available" page, not a raw 404.
**Interfaces:**

```ts
// services/api/src/admin-auth.ts
export function verifyAdminToken(header: string | undefined): boolean; // constant-time compare
```

**Tests.** Unit: missing/malformed/wrong token rejected, correct token accepted, comparison is constant-time. Unit: `unpublish` never removes the row (same `getOwnPropertyNames` assertion pattern 0.3.3 uses). Integration: publish → visible publicly; unpublish → excluded publicly, still `GetItem`-able, still visible to an authenticated admin. Integration: hreflang set matches exactly the populated locales. Negative: an authoring request without the token → 401, mutates nothing (re-read before/after).
**Verification:** `curl -X POST .../content -H "Authorization: Bearer wrong"` → 401; correct token → 201; view-source on a published post shows the expected hreflang set.
**Flag:** `content.authoring.enabled` — default off. **DoD:** a post can be created, edited, published and unpublished end-to-end; unpublished content is never deleted; SEO/hreflang are correct. **Rollback:** revert the branch; rotate/delete the SSM `ADMIN_API_TOKEN` independently if needed. **Do NOT:** add a `DELETE /content/:id` endpoint or any raw `UpdateItem` that empties a row; treat `admin-auth.ts` as permanent.

### TASK 1.4.1 — Contact form → SES → contact@, Turnstile spam protection, rate limited

**Milestone:** M1.4 · **Requirements:** FR-WEB-04, C-11 · **Decisions:** ADR-0009 · **Depends on:** 1.2.1, 1.1.2, 0.6.1 · **Blocks:** 1.5.2 (reuses this task's SES sending identity) · **Size:** M · **Cost:** £0.00 — SES volume already appears in `03-cost-model.md`'s M1 line (~$0.05); Turnstile is free tier. **LL-01 (SES production access) must be resolved before this task's real email send can be proven end-to-end** — flagged as unresolved in the Gate G0 report; raise it now, don't wait for this task to start.
**Context.** The legacy site's own contact form is silently broken today (`legacy-estate.md`: `POST /form` 404s, no route wired) — this is the first working contact form the platform has ever had, built greenfield rather than porting the broken (and unauthenticated, unrate-limited) legacy shape.
**Files:** `services/api/src/rate-limiter.ts` (extracted, generic), `services/api/src/sms-rate-limiter.ts` (updated to depend on it), `services/api/src/contact-form.ts`, `services/api/src/turnstile.ts`, `services/api/src/ses.ts` (new; adds `@aws-sdk/client-sesv2`), `apps/web/src/pages/[locale]/contact.astro`, `infra/src/web-stack.ts`.
**Steps.** 1. Extract the already-principal-generic `RateLimiter` (`tryConsume(principal)`) out of `sms-rate-limiter.ts` into `rate-limiter.ts`; `sms-rate-limiter.ts` re-exports it — no behaviour change, proven by its existing tests passing unmodified. 2. `turnstile.ts`: server-side verify against `https://challenges.cloudflare.com/turnstile/v0/siteverify` (secret key from SSM SecureString), plain `fetch` behind an injected `Fetcher` (matches `smoke-test.ts`'s pattern) so no test calls Cloudflare. 3. `ses.ts`: `SendEmailCommand`, `From` a verified sending identity, `To: contact@nourishthenerve.com` (existing Zoho mailbox, ADR-0009), `ReplyTo` the submitter's address. 4. `contact-form.ts`: same gate-in-order shape as 0.5.3's `sendSms` — Turnstile fail → rejected → rate limit (3/hour per hashed principal) → 429 → SES send → 200; never logs the message body or raw IP, only a hashed key and outcome. 5. Extend the CloudFront `ResponseHeadersPolicy` CSP for the Turnstile origin (`script-src`/`frame-src https://challenges.cloudflare.com`) — the current `default-src 'self'` would otherwise block it. 6. Confirm SES sending identity verification and LL-01's production-access status before relying on delivery to non-verified addresses in a real end-to-end test.
**Interfaces:**

```ts
// services/api/src/contact-form.ts
export type ContactResult = { kind: 'sent' } | { kind: 'blocked'; reason: 'turnstile' | 'rateLimited' };
export function createContactFormHandler(deps: ContactFormDeps): (req: ContactRequest) => Promise<ContactResult>;
```

**Tests.** Unit: Turnstile failure → `blocked/turnstile`, no SES call attempted. Unit: a 4th submission within an hour → `blocked/rateLimited`; a Turnstile-rejected attempt doesn't consume a rate-limit slot. Integration (emulated SES): a valid submission produces exactly one `SendEmailCommand` with the expected `To`/`ReplyTo`. Negative: an oversized/missing field is rejected by Zod before Turnstile is even checked. Negative: no test calls the real Turnstile or SES endpoints.
**Verification:** a deliberately invalid Turnstile token against the ephemeral PR env → rejected; a real submission on the PR env arrives at `contact@nourishthenerve.com`.
**Flag:** `contact.form.enabled` — default off until the PR-env end-to-end send is confirmed. **DoD:** a real message sent through the form arrives at `contact@`; Turnstile and the rate limit both demonstrably block abuse in a test. **Rollback:** revert the branch; flip the flag off via SSM without a deploy. **Do NOT:** log the submitter's raw IP or message body; widen the CSP beyond the one Turnstile origin; reuse the legacy Function URL or its broken `/form` route as a reference.

**Deleted, D-32 (2026-08-30).** The owner's own words: "remove this form, the patient/visit will contact to the whatsapp business account." Every file this task built (`contact-form.ts`, `contact-form-handler.ts`, `ses.ts`'s `createSesContactEmailSender`, the contact page's own script, `ContactFormFunction`, the `/contact` route, and `contact.form.enabled` itself) is removed from the codebase, not merely flagged off — `POST /contact` no longer exists at any status code. `apps/web/src/pages/[locale]/contact.astro` is now a static WhatsApp Business link. Turnstile and `rate-limiter.ts` survive: testimonial submission (TASK 1.4.2) is their other live caller. Full reasoning: [01-decisions.md](01-decisions.md)'s D-32, [docs/runbooks/contact-form.md](../runbooks/contact-form.md).

### TASK 1.4.2 — Testimonials with moderation queue + documented consent record per testimonial

**Milestone:** M1.4 · **Requirements:** FR-WEB-06 · **Risks:** R-04 · **Depends on:** 1.3.1, 1.3.2, 1.4.1 · **Size:** M · **Cost:** £0.00 — same table, no new resource type.
**Context.** FR-WEB-06 requires a documented consent record per testimonial, not just a moderation flag — this applies 0.3.4's clinical/non-clinical schema-separation discipline to a public-facing entity so a future consent withdrawal is a targeted field-level action, not a rewrite (R-04).
**Files:** `services/api/src/testimonial-repository.ts`, `services/api/src/testimonial-moderation.ts`, `packages/shared-types/src/testimonial.ts`, `apps/web/src/pages/[locale]/testimonials/index.astro`.
**Steps.** 1. `PK = TESTIMONIAL#<id>` / `SK = META`, `status: 'pending_review' | 'published' | 'rejected'` — same never-delete `Repository` pattern as content; rejected testimonials stay stored and readable by id. 2. `consent: { textVersion, consentedAt, submitterContactHash }` — a distinct sub-object stamped once at submission; a second write attempt to an existing `consent` object throws rather than overwriting (mirrors 0.3.3's versioned-record guarantee). `submitterContactHash`, not raw contact detail. 3. Public submission form reuses 1.4.1's Turnstile + `rate-limiter.ts` directly — no second anti-abuse implementation. 4. Moderation queue: admin-token-gated (1.3.2) `GET /testimonials?status=pending_review`, `POST /testimonials/:id/{publish,reject}` — both status-only, both audited (0.3.3's `AuditWriter`). 5. Public page renders only `published` testimonials, attribution per the consent record's stated terms (full name / first-name-only / anonymous), not assumed.
**Interfaces:**

```ts
// packages/shared-types/src/testimonial.ts
export interface Testimonial extends BaseRecord {
  id: string;
  status: 'pending_review' | 'published' | 'rejected';
  quote: Record<Locale, string>;
  attribution: { display: 'full' | 'firstNameOnly' | 'anonymous'; name?: string };
  consent: { textVersion: string; consentedAt: string; submitterContactHash: string };
}
```

**Tests.** Unit: a second write to an existing `consent` object throws. Unit: `reject` never removes the row. Integration: submission → `pending_review` → admin `publish` → appears publicly; admin `reject` → never public, still queryable by id and audit log. Negative: Turnstile/rate-limit failure behaves identically to 1.4.1 (shared code path).
**Verification:** `pnpm --filter @ndn/api test`; manual moderation-queue walkthrough against the ephemeral PR env.
**Flag:** `testimonials.submission.enabled`, `testimonials.moderationQueue.enabled` — both default off. **DoD:** every published testimonial has a non-null, immutable consent record; rejected/pending testimonials are never public and never deleted. **Rollback:** revert the branch; flip both flags off. **Do NOT:** publish a testimonial with no consent record (schema-level requirement, not UI-only); allow moderation to mutate `consent`; store the submitter's raw contact detail on the testimonial row.

### TASK 1.5.1 — Workshops: model, posters, details, per-language

**Milestone:** M1.5 · **Requirements:** FR-WEB-02 · **Decisions:** ADR-0005 · **Depends on:** 1.3.1, 1.3.2, 1.2.1 · **Blocks:** 1.5.2 · **Size:** M · **Cost:** £0.00 net-new — poster-image volumes fall within `03-cost-model.md`'s existing S3/CloudFront M1 lines; re-verify at the next gate if poster files turn out large.
**Context.** The first Phase 1 entity needing real media — no media bucket exists yet; ADR-0005 already decided the general shape (S3 + CloudFront, versioned, signed URLs for private per-client media). Workshop posters are deliberately public marketing collateral, not private clinical media, so this task serves them via CloudFront + Origin Access Control **without** signed URLs — the bucket itself stays exactly as locked-down as ADR-0005 requires (no public bucket, no public listing), only the poster *objects* are meant to be publicly viewable once published. Note this distinction explicitly in review; ADR-0005 is not being weakened, just applied to a public-content prefix for the first time.
**Files:** `infra/src/web-stack.ts` (media bucket + `/media` behavior), `services/api/src/workshop-repository.ts`, `services/api/src/media-upload.ts`, `packages/shared-types/src/workshop.ts`, `apps/web/src/pages/[locale]/workshops/{index,[slug]}.astro`.
**Steps.** 1. A second S3 bucket (`MediaBucket`), versioned, `BLOCK_ALL` public access, `RemovalPolicy.RETAIN` — same shape as the site bucket. A `/media/*` CloudFront behavior via Origin Access Control, same pattern already proven for the site bucket. Attach 0.3.2's `attachDestructiveActionGuardrail` to the runtime role against this bucket immediately (deny `DeleteObject`/`DeleteObjectVersion`). 2. `PK = WORKSHOP#<id>` / `SK = META`: per-language `title`/`description`, `dateTimeUtc`, `capacity`, `priceMinorUnits` (GBP pence), `posterKey`. 3. `media-upload.ts`: admin-token-gated endpoint issuing a presigned S3 `PutObject` URL scoped to `workshops/` — the runtime role gets `PutObject` only, never `DeleteObject`. 4. Workshop CRUD mirrors `content-authoring.ts`'s shape — a cancelled workshop transitions `status: 'cancelled'`, poster and details remain retrievable. 5. Public listing/detail pages render published, non-past workshops, poster served via `/media/*`.
**Interfaces:**

```ts
// packages/shared-types/src/workshop.ts
export interface Workshop extends BaseRecord {
  id: string;
  status: 'draft' | 'published' | 'cancelled';
  dateTimeUtc: string;
  capacity: number;
  priceMinorUnits: number; // GBP pence, per D-13
  posterKey?: string;
  details: Record<Locale, { title: string; description: string }>;
}
```

**Tests.** Unit: status transitions never remove a row; `priceMinorUnits` rejected if negative/non-integer (Zod). Integration: a presigned upload URL is scoped to `workshops/` only and expires; the guardrail denies `DeleteObject` against the real media bucket (policy simulator). Integration: an uploaded poster is retrievable at its `/media/...` URL; direct S3 access is denied (OAC, same regression check 0.4.1 runs).
**Verification:** `aws iam simulate-principal-policy` on the runtime role vs. the media bucket → `s3:DeleteObject` `explicitDeny`, `s3:PutObject` `allowed`; `curl -sI` a direct S3 URL → 403.
**Flag:** `workshops.enabled` — default off. **DoD:** a workshop with a poster can be created, published, and rendered publicly; cancelling never deletes it or its media. **Rollback:** `cdk destroy` reverts the bucket/behavior (`RETAIN` keeps uploaded posters). **Do NOT:** make the media bucket or any prefix publicly writable/listable; grant the runtime role `DeleteObject` on it; delete a cancelled workshop's row or poster.

**Amended by D-31, 2026-08-30.** `capacity`/`priceMinorUnits` (the Interfaces block above, as originally built) moved from required to optional and are no longer rendered on the public detail page — workshops are announcement-only, no registration or payment exists on the site to enforce a capacity or charge a price against. Full reasoning: [01-decisions.md](01-decisions.md)'s D-31, [docs/runbooks/workshops.md](../runbooks/workshops.md).

### TASK 1.5.2 — Stripe Checkout + idempotent webhooks + registration confirmation email

**Milestone:** M1.5 · **Requirements:** FR-WEB-02 · **Decisions:** D-13, ADR-0010 · **Depends on:** 1.5.1, 1.4.1, 0.6.1 · **Size:** M · **Cost:** £0.00 recurring infra (Stripe per-transaction fees excluded from C-01, netted from workshop revenue).
**Context.** LL-03 (Stripe account verification/KYC) is an owner action explicitly gating M1.5 in `08-long-lead.md` — this task's code can be built and tested against Stripe test-mode keys regardless, but real payment processing needs LL-03 resolved first.
**Files:** `services/api/src/registration-repository.ts`, `services/api/src/stripe-checkout.ts`, `services/api/src/stripe-webhook.ts` (adds `stripe` dependency), `packages/shared-types/src/registration.ts`, `infra/src/web-stack.ts` (CSP + webhook route).
**Steps.** 1. `PK = WORKSHOP#<id>` / `SK = REGISTRATION#<id>`, `status: 'pending' | 'confirmed' | 'cancelled'` — created `pending` at Checkout Session creation, never deleted. 2. Capacity reservation: an atomic conditional increment on `registeredCount` (`ConditionExpression: registeredCount < capacity`, same shape as 0.5.3's `SpendCounterStore.tryAdd`) at Checkout Session creation; a `checkout.session.expired` webhook releases the reservation. 3. `stripe-checkout.ts`: `POST /workshops/:id/checkout`, GBP Checkout Session, `client_reference_id` = the new `REGISTRATION#<id>`; secret key from SSM SecureString. 4. `stripe-webhook.ts`: verifies the Stripe signature before touching anything. **Idempotency:** a conditional put of `STRIPE_EVENT#<event.id>` (`attribute_not_exists`) — a re-delivered webhook is detected and short-circuited to 200 without reprocessing. 5. On first-seen `checkout.session.completed`: registration → `confirmed`, confirmation email via `ses.ts` (1.4.1's sending identity). 6. Extend the CSP for `https://js.stripe.com`/`https://checkout.stripe.com`.
**Interfaces:**

```ts
// services/api/src/stripe-webhook.ts
export function createStripeWebhookHandler(deps: WebhookDeps): (rawBody: string, signatureHeader: string) => Promise<{ statusCode: 200 | 400 }>;
```

**Tests.** Unit: an invalid signature → 400, no state mutated. Unit: the same `event.id` delivered twice → second delivery is a no-op (200, no second email, no double `confirmed` transition — proven via an email-spy call count of 1). Integration: 50 concurrent checkout-session creations against `capacity: 10` reserve exactly 10, reject the rest (mirrors 0.5.3's 200-concurrent-adds proof). Integration: `checkout.session.expired` releases a reservation. Negative: no test calls the real Stripe API — a fake client satisfies the same interface, Stripe's documented test-mode payloads used as fixtures.
**Verification:** Stripe CLI (`stripe trigger checkout.session.completed`) against the ephemeral PR env's webhook URL, replayed twice, produces exactly one confirmed registration and one email.
**Flag:** `payments.stripeCheckout.enabled` — default off until LL-03 is resolved and the webhook is proven idempotent against real Stripe test-mode events. **DoD:** a real (test-mode) payment produces exactly one confirmed registration and one email, even under a duplicated webhook; capacity is never oversold under concurrency. **Rollback:** revert the branch; flip the flag off via SSM; disable the webhook endpoint in the Stripe dashboard independently of a deploy. **Do NOT:** trust `client_reference_id` without verifying the signature first; process a webhook before the idempotency check; delete a `cancelled` registration or workshop row.

**Abandoned before its first real use — D-31 (2026-08-29).** The owner decided against any online payment or registration on the public site, ever — not a deferral pending LL-03, a decision against. This task's own code is built and tested exactly as specified above and was never wired into any page `apps/web` serves (confirmed by search: zero references to Stripe/checkout anywhere in `apps/web/src`). It stays in place, unused; `payments.stripeCheckout.enabled` is never flipped, in `go-live.md`'s own sequence or anywhere else. LL-03 is closed as moot. Full reasoning: [01-decisions.md](01-decisions.md)'s D-31, [docs/runbooks/stripe-checkout-registration.md](../runbooks/stripe-checkout-registration.md).

### TASK 1.6.1 — G1 cutover: point apex + www at the new distribution, decommission the legacy Lambda, verify the legacy account no longer serves traffic

**Milestone:** M1.6 · **Decisions:** D-02, D-25, D-08 · **Risks:** R-06 · **Depends on:** 0.0.2, 1.1.1–1.5.2 · **Size:** M · **Cost:** £0.00 net delta to the C-01 envelope — re-confirmed as this task's own Verification step; legacy-account spend decreases, outside the C-01 envelope anyway.
**Context.** The highest-risk task in Phase 1 — the actual DNS cutover away from the legacy site and the final decommission of the R-06 public-delete-capable Lambda that TASK 0.0.2 only *contained*, never removed ("Do NOT delete the Lambda yet — that is task 1.6.1 at cutover"). D-25 held the legacy site untouched until this exact point; D-02 executes here. Two legacy surfaces must not be conflated: the Route 53 zone and the `nourishthenerve-api` Lambda/S3 bucket live in `803129122420`, and the CloudFront distribution currently serving apex/`www` (`d2z3fclxq13w3z.cloudfront.net`, confirmed live at Gate G0) was believed to be in a third, unidentified account. **Corrected 2026-08-21:** it is Amplify-managed, fronted by the `ndn-frontend` app in `803129122420` — so releasing the apex/`www` aliases is a self-service step in an account we control, not a cross-account request. See `docs/runbooks/g1-cutover.md`. The distribution itself still cannot be modified directly (Amplify owns it); the app's domain association is what this task removes.
**Files:** `docs/runbooks/g1-cutover.md` (new), `infra/src/config.ts`, `infra/src/web-stack.ts` (`domainNames`), `docs/plan/03-cost-model.md` (post-cutover reconciliation).
**Steps.** 1. **Pre-flight, no DNS/account changes:** confirm every Phase 1 task (1.1.1–1.5.2) is deployed and green at `next.nourishthenerve.com`, including a full a11y/keyboard pass (1.1.3) and a Core Web Vitals check (Gate G1's own criterion). 2. **Certificate:** request/extend an ACM cert in `us-east-1` covering `nourishthenerve.com` + `www.nourishthenerve.com` (keep `next.` too), DNS-validated via a manual CNAME in the `803129122420` zone (`Z09601252VHSWVDDK2RH4`) — same cross-account manual step 0.4.1 used. 3. **CloudFront:** add `nourishthenerve.com`/`www.nourishthenerve.com` as alternate domain names on the **existing** `NdnWebStack` distribution (not a second one — reuses the already-proven canary/rollback/security-headers/OAC shape), deployed via the ordinary CI `deploy` job (OIDC, `ndn-deploy`) — DNS-invisible so far. 4. **DNS cutover, last, one record type at a time:** in the `803129122420` zone (manual, `default` profile — `ndn-deploy` has no access there), repoint the apex `A`/`ALIAS` and `www` `CNAME`/`ALIAS` from `d2z3fclxq13w3z.cloudfront.net` to `NdnWebStack`'s distribution. Lower the TTL beforehand if currently high, so rollback propagates fast. 5. **Verify immediately:** `curl -sI` both apex and `www` serve the new site (200, expected headers, `/health` version); watch the 0.6.2 canary/smoke-test machinery on this deploy specifically — its first time serving the apex. 6. **Observe before decommissioning:** for 24–48h, monitor `nourishthenerve-api`'s CloudWatch invocation metrics (`803129122420`) to confirm invocations drop to zero (excluding this task's own probes). 7. **Decommission, `803129122420` only, manual:** delete the Function URL first, then the Lambda function itself (satisfies D-02). **Leave the S3 bucket `nourishthenerve` exactly as 0.0.2 configured it** — versioned, read-only, `clients/`/`posts/` untouched; D-03 forbids any code or task here from deleting it. 8. Update `03-cost-model.md`'s reconciliation note with actual vs. modelled M1 spend, per Gate G1's checklist. 9. Before step 7, fix or formally accept the two pre-existing legacy issues `legacy-estate.md` flagged and this gate re-confirmed still live: the unauthenticated `/client/{id}/report` enumeration exposure and the broken `/form` route — decommissioning the Lambda resolves both by removal, but if step 7 is delayed for any reason past this task's completion, that exposure remains live and should be called out, not silently carried forward again.
**Interfaces:** none (infrastructure/DNS task).
**Tests.** Integration: post-cutover, both apex and `www` serve `NdnWebStack` content over TLS with the expected security headers (same assertions 0.4.1 runs against `next.`). Integration: the canary/auto-rollback machinery is exercised for real on this deploy. Regression: every Phase 1 feature verified against the apex domain specifically, not only `next.` (a same-origin/absolute-URL assumption would otherwise surface only now). Negative: after step 7, invoking the deleted legacy Function URL fails, and `aws lambda get-function --function-name nourishthenerve-api` returns `ResourceNotFoundException`. Negative: the S3 bucket's objects remain read-only accessible exactly as 0.0.2 left them — proving the Lambda decommission left the data untouched.
**Verification:** `dig` on both apex and `www` resolve to `NdnWebStack`'s distribution; `curl -sI` both return 200 with the full security-header set; a Core Web Vitals run against the live apex passes Gate G1's bar.
**Flag:** none — this is a DNS cutover, not application behaviour; every feature flag it depends on should already be flipped on and proven in staging before this task runs, not flipped as part of it.
**DoD:** apex and `www` serve the new site exclusively; `nourishthenerve-api` and its Function URL no longer exist in `803129122420`; legacy invocation metrics show zero real post-cutover traffic; Gate G1's checklist (apex serving new site, legacy retired, Core Web Vitals pass) is met and recorded.
**Rollback:** **DNS-only, fast, rehearsed before use:** revert the apex/`www` records in `803129122420` to `d2z3fclxq13w3z.cloudfront.net` (documented ahead of time, not guessed) — restores the legacy experience with zero AWS resource changes, provided step 7 hasn't run. **Once step 7 has run, the Lambda cannot be undeleted** — this is why the observation window and step ordering (DNS first, confirmed stable, *then* delete) exist. A post-step-7 issue is fixed forward on the new stack or rolled back via DNS to legacy CloudFront (untouched throughout), never by resurrecting the deleted Lambda.
**Do NOT:** delete the legacy Lambda, Function URL, or IAM policy before DNS has served the new site through the full observation window; delete the `ndn-frontend` Amplify app or anything else in `803129122420` beyond the one domain association the cutover removes (the account is shared with the unrelated `islamicmaps` estate); delete, empty, or version-purge the S3 bucket `nourishthenerve` or its prefixes under any circumstance (D-03); skip the TTL-lowering step; run this task before every Phase 1 dependency is proven at `next.nourishthenerve.com` first.

### TASK 1.6.2 — SSM-backed feature-flag source, so a flag can actually be turned on

**Milestone:** M1.6 · **Requirements:** §10 · **Decisions:** D-14, D-23 · **Depends on:** 0.6.1, 1.3.1–1.5.2 · **Blocks:** every Phase 2 flag-gated task · **Size:** S · **Cost:** £0.00
**Context.** Added at the Gate G1 review (`gate-g1-report.md` §3a, on its own branch), which found that nine production handlers each wire a `CachedFlagReader` over an `InMemoryFlagSource` that nothing ever writes to. Every flag therefore reads `false` forever and no operator action can change it: blog, blog authoring, contact form, testimonials, workshops, media upload and Stripe checkout are all unreachable in production, verified live. TASK 0.6.1 deferred the SSM implementation correctly — nothing read a flag then — but the deferral was re-noted verbatim in each of the eight tasks after 1.3.1 and never became work. D-23 chose homegrown config-driven flags precisely so a flag could be flipped **without** a deploy; as shipped, only the dark-launch half exists.
**Files:** `services/api/src/ssm-flag-source.ts` (new), `infra/src/flag-parameters.ts` (new), `infra/src/config.ts`, `infra/src/web-stack.ts`, `infra/src/data-stack.ts`, the nine `services/api/src/*-handler.ts`, `docs/runbooks/feature-flags.md`.
**Steps.** 1. `SsmFlagSource implements FlagSource` reading `/ndn/flags/<FlagName>` via `GetParameter` — plain `String`, no `WithDecryption` (a flag's state is not a secret). 2. Fail closed on every path: only the literal `'true'` is on; a missing parameter is the documented steady state and resolves `undefined` quietly; an unrecognised value or an SSM error warns and resolves `undefined`, never throws — a config read must not 500 a working page. 3. `createSsmFlagReader()` collapses the five-line wiring block the nine handlers each repeated into one call, so the TTL, clock and source cannot drift between them. 4. `grantFlagReads` grants `ssm:GetParameter` on `parameter/ndn/flags/*` only — a prefix wildcard, since naming flags individually would mean a deploy before every flip. 5. Set `FLAG_PARAMETER_NAME_PREFIX` on exactly the nine flag-reading functions. 6. Document the operator procedure in the runbook.
**Interfaces:** no change — `FlagSource` and `FlagReader` are unchanged from TASK 0.6.1; this is the implementation that interface was written for.
**Tests.** Unit: prefix construction; `WithDecryption` never sent; `'true'`/`'false'` parsed, everything else refused; missing parameter is quiet; an SSM error resolves off rather than throwing; a parameter's value is never logged. Integration (synth): all nine functions carry the prefix env var and a grant that is exactly `ssm:GetParameter` on `parameter/ndn/flags/*`. **Negative: the grant cannot reach `/ndn/admin-api-token`, `/ndn/stripe-secret-key`, `/ndn/stripe-webhook-secret` or `/ndn/turnstile-secret-key`** — a `parameter/ndn/*` grant would pass the positive assertion and must fail this one. Negative: the health and smoke-test functions hold no flag access.
**Verification:** `pnpm -r lint && pnpm -r typecheck && pnpm test` green; after deploy, `aws ssm put-parameter --name /ndn/flags/<name> --value true` makes the gated route respond within 30s (`FLAG_CACHE_TTL_MS`) with no deploy.
**Flag:** none — this *is* the flag mechanism. **DoD:** an operator can turn any `FlagName` on and off from the CLI without a deploy; every flag is still off at the end of this task. **Rollback:** revert the branch — flags return to permanently-off, the state this task found them in. **Do NOT:** turn any flag on as part of this task; grant `ssm:GetParameter` on `/ndn/*`; use `SecureString` or `GetParametersByPath`; make a failed flag read throw.

**Gate G1:** public site live at the apex, accessible, within cost, legacy estate retired.

## Phase 2 — Identity, authorisation and roles

**Ordering, taken at Gate G1 (2026-08-21).** [09-self-audit.md](09-self-audit.md)'s one pushback on the brief recommended pulling M2.3 (RBAC spine + audit) forward "to sit alongside M1.1 … Your call at G1." Phase 1 shipped before that call was made, so *alongside M1.1* is no longer on the table; what was decided is the surviving half of it — **the spine goes first inside Phase 2, ahead of authentication rather than after it.** The milestones are renumbered against the brief accordingly: **M2.1 is the authorisation spine and audit log** (the brief's M2.3), **M2.2 is authentication** (the brief's M2.1), **M2.3 is notifications** (the brief's M2.2). M2.4 (clinician accounts) and M2.5 keep their numbers; M2.5 is stated as **assignment, reassignment and caseload oversight**, because the principal's cross-caseload view (2.5.3) can only be built once assignments exist and belongs on the assignment side of that line rather than the directory side. The reorder is possible at all because every piece of the spine — a `Principal`, a policy-decision function, a field projection, an append-only audit writer — is pure domain code that needs no Cognito, exactly as 0.3.3/0.3.4 built the soft-delete and person-record primitives against in-memory seams long before a real table existed. Nothing authenticated is built before the boundary it has to respect.

**Phase-wide notes every task below inherits.**

- **Never-delete now covers people.** C-03 and §6.7 were written about clinical rows; in this phase they extend to identity. A patient is `declined` or `suspended`, a clinician is `deactivated`, a Cognito user is disabled — never `AdminDeleteUser`, never a removed row. The user pools carry `deletionProtection` and `RemovalPolicy.RETAIN` for the same reason the table does: deleting a pool destroys every credential in it and cannot be undone.
- **The log-alarm budget is full.** `MONITORED_LOG_GROUP_NAMES` holds 10 entries against a hard AWS ceiling of 10 metrics per alarm (`infra/src/config.ts`, probed against the real API at Gate G1), and `log-retention.test.ts` fails the build if a new `/ndn/*` group appears in neither that list nor `UNMONITORED_LOG_GROUP_NAMES`. Phase 2 adds roughly eight functions. Every task that adds one **names which list it goes in and, if monitored, which group it displaces** — that is a required line in the PR body, not an afterthought.
- **Flags are real now.** TASK 1.6.2 made `SsmFlagSource` the live source, so "default off" in this phase means an operator can turn it on with `aws ssm put-parameter` and no deploy. Every flag below is still off at the end of its own task.
- **Requirement IDs, and a gap this elaboration found.** The tasks below cite `FR-DP-02` and `FR-DP-05` — the only two FR IDs any committed document actually pins to a requirement (`04-data-model-rbac.md` and `02-risk-register.md` respectively) — plus `FR-X-*`/`FR-WEB-*` where Phase 1 already established the mapping, and brief section references elsewhere. **The full FR/NFR matrix is not in the repo:** `07-traceability.md` holds a coverage summary that points at itself for the matrix, and the source requirements brief (`ndn-planning-brief.md`, named in `00-index.md`) was never committed. Nothing in Phase 2 is blocked by this, but "every FR maps to ≥1 task and ≥1 test" is currently unverifiable from the repo alone. Reconstructing the matrix into `07-traceability.md` is an action item on the Gate G1 report and should be closed before G2 audits coverage.
- **No clinical or personal data enters Cognito.** Pools hold credentials, group membership and an email address. Names, contact details, diagnoses and everything else live in the DynamoDB table under `04-data-model-rbac.md`'s key shapes, keyed by the pool's `sub`. This keeps NFR-04's residency story to one region and one store, and keeps `person-record.ts`'s clinical/personal split (0.3.4, R-04) the only place personal data is held.

### TASK 2.1.1 — Principal model and the RBAC policy-decision layer

**Milestone:** M2.1 · **Requirements:** FR-DP-05, NFR-06 · **Decisions:** D-09 · **Risks:** R-09 · **Depends on:** 0.3.3 · **Blocks:** every remaining task in Phases 2–4 · **Size:** M · **Cost:** £0.00 — pure domain code, no AWS resource.
**Context.** `04-data-model-rbac.md` states the authorisation matrix in prose and a table; nothing in the repo implements it. The only access control that exists today is `services/api/src/admin-auth.ts`, whose own header calls itself "one narrow, explicitly-temporary bearer-token gate, not a real auth system: no user identity, no session, no scopes." This task turns the table into code **before** there is anything to authenticate, so that every handler written from here on is built against it rather than retrofitted onto it — the whole reason the milestone was reordered.
**Files:** `services/api/src/principal.ts` (new), `services/api/src/authz.ts` (new), `services/api/src/authz-matrix.ts` (new), `packages/shared-types/src/principal.ts` (new), `packages/shared-types/src/index.ts`.
**Steps.** 1. `Principal`: a `subjectId` (the Cognito `sub`, opaque here), a `role` of `'patient' | 'sub-clinician' | 'principal-clinician'`, an `accountStatus`, and exactly one identity link — `patientId` for a patient, `clinicianId` for either clinician role. No other field; a principal is not a user profile. 2. `authz-matrix.ts` transcribes `04-data-model-rbac.md`'s table as one exported const, cell for cell, with the doc's own row and column labels as the keys — so a reviewer can diff the code against the document line by line rather than reading intent out of `if` statements. 3. `can(principal, action, resource): Decision` reads that const and nothing else. Deny by default: an unlisted entity, an unrecognised role or a missing relationship is a denial, never a fallthrough. 4. **`Action` has no delete member.** `type Action = 'create' | 'read' | 'update'` — the matrix's `D = never` column becomes unrepresentable, so "authorise a delete" is a compile error rather than a policy decision, the same discipline `Repository` uses by having no method that removes a row. 5. Relationship, not just role: a sub-clinician's `read` on a patient resource is allowed only when `resource.assignedClinicianId === principal.clinicianId`, and a patient's only when `resource.ownerPatientId === principal.patientId`. Unassigned and other-patient are the matrix's two `—` columns and are the cases the tests hammer. 6. Assessment resources carry a `fieldSet: 'visible' | 'private'` so the matrix's two assessment rows are two distinct lookups, not one lookup plus a later filter. 7. `accountStatus` gates before role does: anything other than `approved`/`active` can read its own profile and nothing else.
**Interfaces:**

```ts
// packages/shared-types/src/principal.ts
export type Role = 'patient' | 'sub-clinician' | 'principal-clinician';
export type Action = 'create' | 'read' | 'update'; // deliberately no 'delete'

export interface Principal {
  readonly subjectId: string;
  readonly role: Role;
  readonly accountStatus: 'pending' | 'approved' | 'declined' | 'suspended' | 'active' | 'deactivated';
  readonly patientId?: string;
  readonly clinicianId?: string;
}

export interface Resource {
  readonly entityType: string;
  readonly ownerPatientId?: string;
  readonly assignedClinicianId?: string;
  readonly fieldSet?: 'visible' | 'private';
}
```

**Tests.** Unit: the matrix is asserted **exhaustively, not by sample** — a generated table-driven suite over the full cross-product of role × entity × action × relationship, one assertion per cell, so a future edit to `authz-matrix.ts` that widens a cell fails a named test rather than passing silently. Unit: deny-by-default for an unknown entity type, an unknown role, and a principal with neither `patientId` nor `clinicianId`. Unit: `accountStatus: 'pending'` is denied everything except its own profile read. Negative: a patient principal is denied on every `private` assessment lookup, in every relationship, including their own. Negative: a sub-clinician is denied on every unassigned-patient resource. Type-level: a test asserting `can(p, 'delete' as never, r)` does not compile (`// @ts-expect-error`).
**Verification:** `pnpm -r lint && pnpm -r typecheck && pnpm test` green; a reviewer can hold `04-data-model-rbac.md`'s table next to `authz-matrix.ts` and match every cell.
**Flag:** none — nothing calls it yet; a policy module that is off is worse than one that is unused. **DoD:** every cell of `04-data-model-rbac.md`'s RBAC matrix is implemented and individually asserted; no code path in the repo can authorise a delete. **Rollback:** revert the branch — nothing consumes it yet. **Do NOT:** add a `'delete'` action or an `isAdmin` escape hatch; scatter authorisation checks into handlers instead of calling `can()`; let the matrix live anywhere but the one const.

### TASK 2.1.2 — The clinician-private field boundary, at the repository layer

**Milestone:** M2.1 · **Requirements:** FR-DP-05, NFR-06 · **Risks:** **R-09** · **Depends on:** 2.1.1, 0.3.3, 0.3.4 · **Blocks:** 3.2.x (which wires the first real entity through it) · **Size:** M · **Cost:** £0.00
**Context.** R-09 — a clinician's `private{}` notes reaching a patient — is the one risk in the register rated **Critical**, and `09-self-audit.md`'s red-team names it as the second-likeliest way this plan fails: "an export, a log line, an error message, a cache." The register currently points R-09 at 3.2.x. This task builds the chokepoint one phase earlier, for the same reason 0.3.4 built `person-record.ts`'s clinical/personal split before any patient existed: the boundary is cheap to build against an empty system and expensive to retrofit onto a populated one. 3.2.x remains the task that wires assessments through it; **R-09's register entry is amended to `2.1.2` (chokepoint), `3.2.x` (wiring)** as part of this task.
**Files:** `services/api/src/projection.ts` (new), `services/api/src/repository.ts`, `services/api/src/versioned-repository.ts`, `docs/plan/02-risk-register.md`, `docs/runbooks/private-field-boundary.md` (new).
**Steps.** 1. `projection.ts`: `projectFor(principal, record)` strips every `private{}` attribute unless `can(principal, 'read', {…, fieldSet: 'private'})` allows it. One function, one place. 2. Make it structurally unavoidable rather than merely available: the repository read methods return an opaque `Projected<T>` that no serialiser accepts until it has been through `projectFor`, so "forgot to project" is a type error at the call site, not a missing test. 3. Every path out of the process goes through it — the JSON response serialiser, `logger.ts`'s structured line, `errors.ts`'s message construction, and any future export. Named individually because the red-team named them individually. 4. `logger.ts` gains an assertion in its own tests that a record containing a `private` key cannot be logged, on any level. 5. 100% branch coverage on `projection.ts` is a CI condition, not an aspiration — the coverage config gets a per-file threshold for this file alone. 6. The runbook records the invariant, the negative-test convention every future endpoint owes ("negative test per endpoint, forever" — NFR-06), and how to add one.
**Interfaces:**

```ts
// services/api/src/projection.ts
export type Projected<T> = T & { readonly __projected: unique symbol };
export function projectFor<T>(principal: Principal, record: T): Projected<Partial<T>>;
```

**Tests.** Unit: a record with `visible{}` and `private{}` projects to both for an assigned sub-clinician and the principal clinician, and to `visible{}` alone for the owning patient, an unassigned clinician and any other patient. Unit: projection is applied to nested structures and to arrays of records, not just top-level objects. Negative: `logger` refuses a payload containing a `private` key. Negative: an `AppError` message built from a record carries no `private` content. Type-level: serialising an unprojected record does not compile. Coverage: `projection.ts` at 100% branches or CI fails.
**Verification:** `pnpm test:coverage` shows `projection.ts` at 100% branches; the type-level negatives are in the suite as `// @ts-expect-error` assertions.
**Flag:** none. **DoD:** no patient-reachable code path can emit a `private{}` field, and the compiler — not review — is what enforces it. **Rollback:** revert the branch. **Do NOT:** add a `skipProjection` / `raw` option "for admin"; project in a handler or a view; widen `Projected<T>` into a plain cast.

### TASK 2.1.3 — Persistent append-only audit log, and the principal's read of it

**Milestone:** M2.1 · **Requirements:** §6.2–6.4, NFR-06 · **Decisions:** D-07, D-21 · **Depends on:** 2.1.1, 1.3.1 · **Blocks:** 2.2.3, 2.4.1, 2.5.1 · **Size:** M · **Cost:** £0.00 net-new — audit rows are a few hundred bytes each and fall inside `03-cost-model.md`'s existing DynamoDB line; re-check at G2 against real write volume.
**Context.** `services/api/src/audit.ts` defines `AuditWriter` and exactly one implementation — `InMemoryAuditLog`, an array in a Lambda's memory. Every `create`, `update`, `soft-delete`, `publish`, `unpublish`, `reject`, `cancel` and `confirm` the platform has ever performed has been written to it and discarded when the invocation ended. Its header says why ("no DynamoDB table exists yet"); `NdnDataStack` shipped at 1.3.1 and that sentence stopped being true. This is the same shape of finding as Gate G1 §3a's flag source: a correctly-deferred seam that nothing came back for. Nothing in Phase 2 can be reviewed after the fact until it is fixed, and audit is the only mechanism that makes an authorisation boundary reviewable at all.
**Files:** `services/api/src/audit.ts`, `services/api/src/dynamo-audit-log.ts` (new), `services/api/src/audit-read-handler.ts` (new), `infra/src/data-stack.ts`, `infra/src/config.ts`, `docs/runbooks/audit-log.md` (new).
**Steps.** 1. `DynamoAuditLog implements AuditWriter` writing `PK = AUDIT#<yyyy-mm-dd>` / `SK = <iso-instant>#<ulid>`, per `04-data-model-rbac.md`. Date-partitioned so a day's events are one query and no partition grows unbounded. 2. **`AuditEvent` gains the `where` the data model asks for and the type never had** — today it is who/what/when only (`at`, `actor`, `action`, `entityType`, `entityId`). Add `sourceIp` and `requestId` from the API Gateway event, plus the actor's `role`, so "who" is a principal rather than a bare string. 3. Append-only, enforced three ways and not by convention: the interface exposes no update or delete; the write is a `PutCommand` conditioned on `attribute_not_exists(pk)` so a colliding key fails rather than overwrites; and `attachDestructiveActionGuardrail` already denies `DeleteItem` to the role. 4. The writer's IAM grant is `dynamodb:PutItem` **only** — no read, no update — so a compromised writer cannot read the log it appends to. 5. **No PII and no clinical content in an audit row.** Identifiers only, per `00-conventions.md`; the row records that patient `PAT#123`'s care plan was updated, never what it now says. 6. Audit rows never expire — no TTL attribute anywhere near this partition. 7. `GET /audit?date=` behind `can(principal, 'read', { entityType: 'audit' })`, which the matrix allows to the principal clinician alone. 8. A write failure is **not** swallowed: if the audit write fails, the operation that triggered it fails too. An unauditable change to clinical data is worse than a rejected one. 9. Log group `/ndn/audit-read-function` → `UNMONITORED_LOG_GROUP_NAMES` (a principal-only endpoint, the lowest-volume class in the estate, and the alarm's ten slots are full).
**Interfaces:**

```ts
// services/api/src/audit.ts — added fields, existing ones unchanged
export interface AuditEvent {
  readonly at: string;
  readonly actor: string;        // subjectId, not a name
  readonly actorRole: Role;
  readonly action: AuditAction;
  readonly entityType: string;
  readonly entityId: string;
  readonly requestId: string;
  readonly sourceIp: string;
}
```

**Tests.** Unit: every existing `AuditAction` round-trips through the Dynamo writer. Unit: a duplicate `<ts>#<id>` throws rather than overwriting. Unit: a failed audit write propagates and the caller's operation fails. Integration: a day's events come back in `<ts>` order from one query. Integration (synth): the writer role holds `dynamodb:PutItem` and no `GetItem`/`Query`/`UpdateItem` on the table. Negative: `GET /audit` as a patient and as a sub-clinician → `403`, both asserted. Negative: no audit row in any test fixture contains a value from a record's `personal{}` or `clinical{}` half — a repo-wide assertion, not a spot check. Negative: `InMemoryAuditLog` is no longer imported by any production handler (a lint-level assertion, the same shape as Gate G1 §3a's fix).
**Verification:** `pnpm -r lint && pnpm -r typecheck && pnpm test` green; after deploy, an authoring action produces a queryable `AUDIT#<today>` row and `aws iam simulate-principal-policy` returns `implicitDeny` for `dynamodb:Query` on the writer role.
**Flag:** `audit.readApi.enabled` — default off; the *writer* is not flagged, because an audit log that can be switched off is not an audit log. **DoD:** every repository write lands a durable audit row carrying who, what, when and where; nothing can amend or remove one; the principal clinician can read a day's events and nobody else can. **Rollback:** revert the branch — the writer reverts to in-memory, which is the state this task found. Rows already written are retained (the table is `RETAIN`). **Do NOT:** put a TTL on an audit row; grant the writer read access; add an `updateAuditEvent` for any reason; write a patient name, an email address or clinical text into an audit row; let an audit-write failure be caught and ignored.

### TASK 2.2.1 — Cognito: two user pools, patient passwordless, clinician TOTP

**Milestone:** M2.2 · **Requirements:** NFR-03, NFR-04 · **Decisions:** **D-09, ADR-0004 (amended by this task)** · **Risks:** R-07 · **Depends on:** 2.1.1 · **Blocks:** 2.2.2, 2.2.3, 2.4.1 · **Size:** M · **Cost:** £0.00 — `03-cost-model.md` carries Cognito Essentials at $0 on 509 MAU against 10,000 free. **Re-verify the tier's free allowance and price as this task's first step** — D-09's figure was verified 2026-08-07 and the gate checklist requires re-verification past 90 days; this task is the first that actually depends on it.
**Context.** ADR-0004 records "Cognito Essentials" and nothing else, and there is a constraint underneath it that the one-line ADR does not survive contact with. D-09 wants **TOTP for clinicians and email-OTP for patients**, and Cognito's MFA policy is *pool-wide*: `REQUIRED` would stack a second factor on top of a patient's passwordless email OTP, and `OPTIONAL` cannot compel a clinician to enrol. One pool cannot hold both policies.
**Decision this task takes, and records as an ADR-0004 amendment: two user pools.** `ndn-patients` — self sign-up on, passwordless email OTP, no MFA. `ndn-clinicians` — self sign-up **disabled entirely**, password plus `REQUIRED` TOTP. The gain beyond the MFA policy is structural: a patient credential cannot become a clinician credential, because they are not in the same directory. Group membership inside one pool is a softer boundary than that, and on a clinical system the harder one is worth its cost. **The cost is real and named:** API Gateway's built-in JWT authorizer binds one issuer per authorizer and one authorizer per route, so two issuers mean the Lambda authorizer TASK 2.2.2 builds instead. **Rejected:** one pool with groups (cheaper, lower latency, and the built-in authorizer would do — rejected on the MFA policy conflict above, which has no workaround that does not involve a Pre-Authentication trigger reimplementing MFA enforcement that Cognito already does properly one pool boundary away).
**Files:** `infra/src/auth-stack.ts` (new), `infra/bin/app.ts`, `infra/src/config.ts`, `docs/adr/0004-auth.md`, `docs/runbooks/cognito-user-pools.md` (new).
**Steps.** 1. `NdnAuthStack`, `eu-west-2` (NFR-04 — the pools hold email addresses, so they stay in-region like everything else). 2. Both pools: `deletionProtection: ACTIVE`, `RemovalPolicy.RETAIN`, `selfSignUpEnabled` true for patients and false for clinicians, `signInAliases: { email: true }`, `standardAttributes` limited to a required, mutable email — nothing else, per this phase's "no personal data in Cognito" note. 3. Patients: Essentials-tier passwordless email OTP as the first factor; no password; account recovery by email only. 4. Clinicians: password policy at CDK's strong default, `mfa: REQUIRED`, `mfaSecondFactor: { otp: true, sms: false }` — SMS as a second factor is off deliberately, both for R-02 (it is a spendable path) and because a phone-number factor is a poorer one. 5. One app client per pool, `authFlows` limited to what 2.2.4 uses, **no client secret** (a public browser client cannot hold one), PKCE required, callback/logout URLs on `SITE_ORIGIN` only. 6. Refresh-token validity 30 days, access/ID tokens 60 minutes; `enableTokenRevocation` on, so 2.4.1's deactivation is immediate rather than eventual. 7. Advanced/threat-protection features are **not** enabled — they are a paid tier, and the honest position is that £0 buys the directory and the MFA, not the risk engine. Recorded in the ADR rather than assumed. 8. Both pool IDs and client IDs become `config.ts` constants (they are identifiers, not secrets). 9. `attachDestructiveActionGuardrail`'s pattern extends to identity: the deploy role gets an explicit `Deny` on `cognito-idp:DeleteUserPool`, `DeleteUserPoolClient` and `AdminDeleteUser`, the same shape 0.3.2 uses for the table and the buckets.
**Interfaces:** none in application code — this task deploys infrastructure and exports identifiers.
**Tests.** Unit (synth): both pools carry `DeletionProtection: ACTIVE` and `RemovalPolicy.RETAIN`; the clinician pool has `MfaConfiguration: ON` with `SOFTWARE_TOKEN_MFA` and **not** `SMS_MFA`; the patient pool has self sign-up enabled and the clinician pool has it disabled. Unit (synth): neither app client has a generated secret; both require PKCE; callback URLs contain no host other than `SITE_ORIGIN`. Negative (synth): the deploy role's policy contains the three `Deny` statements. Negative: no standard or custom attribute beyond email is configured on either pool.
**Verification:** `pnpm --filter @ndn/infra run synth`; after deploy, `aws cognito-idp describe-user-pool` on both confirms the MFA and sign-up settings; `aws iam simulate-principal-policy` on `ndn-deploy` returns `explicitDeny` for `cognito-idp:AdminDeleteUser`; the re-verified Cognito price is recorded in `03-cost-model.md` whether or not it changed.
**Flag:** none — a deployed, empty user pool with no authorizer in front of it is inert. **DoD:** two pools exist in `ndn-prod` with the stated policies; no clinician can exist without TOTP; no patient is asked for a second factor; neither pool can be deleted by the deploy role; ADR-0004 records the two-pool decision and what it costs. **Rollback:** `cdk destroy NdnAuthStack` before any real user exists — after that, **rollback is forward only**: `RETAIN` and deletion protection are there precisely so a bad deploy cannot take the directory with it. **Do NOT:** enable SMS MFA; put a name, phone number or any clinical attribute on a pool; give a browser client a secret; enable self sign-up on the clinician pool; use `AdminDeleteUser` anywhere, in code, scripts or by hand.

### TASK 2.2.2 — Lambda authorizer: verify the token, resolve the `Principal`, fail closed

**Milestone:** M2.2 · **Requirements:** NFR-03, NFR-05, NFR-06 · **Decisions:** D-06 · **Risks:** R-09 · **Depends on:** 2.2.1, 2.1.1, 2.1.3 · **Blocks:** 2.2.3, 2.2.4, 2.4.x, 2.5.x · **Size:** M · **Cost:** £0.00 — one more 128 MB arm64 function inside the always-free Lambda allowance; authorizer results are cached by API Gateway so it is not invoked per request.
**Context.** 2.2.1's two issuers rule out API Gateway's built-in JWT authorizer. This is the function that takes its place, and it is the single most security-sensitive piece of code in the repo: everything downstream trusts the `Principal` it produces. It is also the only place a JWT is ever parsed — no handler reads the `Authorization` header, ever.
**Files:** `services/api/src/authorizer-handler.ts` (new), `services/api/src/jwt-verify.ts` (new), `services/api/src/request-principal.ts` (new), `infra/src/web-stack.ts`, `infra/src/data-stack.ts`, `infra/src/config.ts`.
**Steps.** 1. A `REQUEST` authorizer with `Authorization` as its identity source and a 300-second result cache — the cache is keyed on the header, so a revoked token still lives at most five minutes; that window is stated in the runbook rather than left to be discovered. 2. Verify with `aws-jwt-verify` (AWS's own library, so JWKS fetching, caching and rotation are not hand-rolled): signature, `exp`, `iss` against exactly the two known pool issuers, `aud`/`client_id` against the two known clients, and `token_use` pinned to a single value. 3. **Role comes from the issuer, never from a claim the client could influence** — a token from the clinician pool is a clinician, a token from the patient pool is a patient; `principal-clinician` versus `sub-clinician` then comes from `cognito:groups`, which only an admin action can set. 4. Look up the account/record status once and put it in the `Principal`, so a suspended patient's token stops working within the cache window rather than at the next sign-in. 5. `request-principal.ts` gives handlers `requirePrincipal(event)`, which reads the authorizer context and throws `AppError('UNAUTHENTICATED')` if it is absent or malformed — a handler can never construct a `Principal` from a header itself. 6. Fail closed on every path: any verification failure, JWKS fetch failure, or unexpected shape denies. **A 500 in this function is a denial, not an allow**, and is asserted as such. 7. Log the decision — subject, issuer, route, allow/deny — as an identifiers-only structured line, and never the token, never a claim body. 8. Log group `/ndn/authorizer-function` → `MONITORED_LOG_GROUP_NAMES`, **displacing `/ndn/media-upload-function`**, which moves to `UNMONITORED_LOG_GROUP_NAMES`: the authorizer is on the path of every authenticated request in the system and media upload is an admin-gated occasional one. The swap is recorded in `config.ts` alongside the list, per that file's own instruction.
**Interfaces:**

```ts
// services/api/src/request-principal.ts
export function requirePrincipal(event: APIGatewayProxyEventV2WithLambdaAuthorizer): Principal;
```

**Tests.** Unit: a well-formed token from each pool resolves to the right role and identity link. Negative, each its own named test: expired token; token signed with the wrong key; `alg: none`; an RS256 token re-signed as HS256 using the public key as the secret; issuer of the *other* pool on a role-restricted route; wrong `aud`; wrong `token_use`; absent header; `Bearer` with an empty token; a `cognito:groups` claim asserting `principal-clinician` on a *patient*-pool token. Negative: a JWKS fetch failure produces a denial, not an allow — this is the test that proves 500-means-deny. Negative: no test fixture's log output contains a token or a claim value. Integration (synth): every protected route on both HTTP APIs carries this authorizer; a test enumerates the routes so a new unprotected one fails the build.
**Verification:** `pnpm test` green; against the deployed API, a valid patient token gets `200` on a patient route and `403` on a clinician route, and a token from the clinician pool gets `403` on a patient-only route; `curl` with no header gets `401`.
**Flag:** none — an authorizer behind a flag is an authorizer that can be turned off. **DoD:** no protected route is reachable without a verified token from a known pool; the role a request runs as is derived from the issuer, not from anything the caller can set; every failure mode denies. **Rollback:** revert the branch — the routes it protects are all introduced by this phase and revert with it. **Do NOT:** trust any claim for role that a client could influence; read the `Authorization` header in a handler; cache a decision longer than the token's own life; return `Allow` on an internal error; log a token or a claim body; disable `enableTokenRevocation` to make the cache simpler.

### TASK 2.2.3 — Patient self-registration, and the approval lifecycle on the record

**Milestone:** M2.2 · **Requirements:** §5 (patient registration and approval) · **Decisions:** D-03, D-09 · **Risks:** R-04 · **Depends on:** 2.2.2, 2.1.3, 0.3.4 · **Blocks:** 2.5.1 · **Size:** M · **Cost:** £0.00 — within the modelled DynamoDB and SES lines.
**Context.** The platform's front door: a patient registers themselves, and is then *approved* by a clinician before they can see anything. Registration and approval are deliberately separate — a Cognito account existing is not the same as a patient record being live, and conflating them is how a stranger ends up inside a caseload. The patient record is the first real user of `person-record.ts` (0.3.4), whose header has said since Phase 0 that "wiring a real entity (patient, …) onto it is Phase 2/3."
**Files:** `services/api/src/patient-repository.ts` (new), `services/api/src/registration-handler.ts` (new), `packages/shared-types/src/patient.ts` (new), `infra/src/auth-stack.ts`, `infra/src/data-stack.ts`, `docs/runbooks/patient-registration.md` (new).
**Steps.** 1. `PAT#<id>` / `PROFILE`, built on `PersonRecord<Clinical, Personal>`: name, contact details and marketing preferences in `personal{}`, everything with a clinical retention basis in `clinical{}` — the split R-04 and the DPIA depend on, in place from the first record rather than migrated in later. 2. `account_status: 'pending' | 'approved' | 'declined' | 'suspended'` and `record_status` are separate fields, per `04-data-model-rbac.md`. **`declined` is a status, never a deleted row**, and neither is `suspended`. 3. A Post-Confirmation trigger on the patient pool creates the profile in `pending`, keyed by the pool's `sub`. The trigger is idempotent — Cognito retries — and a failure surfaces rather than leaving a confirmed account with no record. 4. Until approval, `can()` (2.1.1) already allows exactly one thing: reading their own profile. This task adds no special case; it relies on the spine, which is the point of having built it first. 5. Registration, approval, decline and suspension each write an audit row (2.1.3) with the acting principal. 6. A registration confirmation email goes out through SES's existing verified identity and configuration set — **content-free**: "your registration has been received", no clinical language, no name in the subject. 7. Rate-limit registration per source IP with `rate-limiter.ts` (0.5.x), and require Turnstile on the form, the same guards TASK 1.4.1's contact form uses. 8. Log group `/ndn/registration-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:**

```ts
// packages/shared-types/src/patient.ts
export interface PatientPersonal { fullName: string; email: string; phone?: string; marketingOptIn: boolean }
export interface PatientClinical { referralSource?: string; presentingCondition?: string }
export interface Patient extends PersonRecord<PatientClinical, PatientPersonal> {
  id: string;
  account_status: 'pending' | 'approved' | 'declined' | 'suspended';
  assigned_clinician_id?: string;
  keywords: string[];
}
```

**Tests.** Unit: the Post-Confirmation trigger is idempotent under replay — two invocations leave one record and one audit row. Unit: every status transition is recorded; no transition removes a record. Integration: a `pending` patient's token can read their own profile and gets `403` on every other route, asserted route by route. Negative: a `declined` patient's record is still `GetItem`-able and their token is denied everywhere. Negative: the confirmation email body contains no clinical term and no name in the subject. Negative: no code path writes to `clinical{}` during self-registration except the two referral fields above.
**Verification:** end-to-end against the deployed pool: sign up, confirm, and observe a `pending` `PAT#` record plus one audit row; the same token then fails every clinical route.
**Flag:** `auth.patientRegistration.enabled` — default off until 2.5.1 exists to approve anyone, so nobody can register into a system with no route out of `pending`. **DoD:** a patient can create an account; the account grants nothing until a clinician approves it; every transition is audited; no path deletes a person. **Rollback:** flip the flag off via SSM — registration closes with no deploy, and existing records are untouched. **Do NOT:** grant any access on `pending`; delete or overwrite a `declined` record; put clinical content in the confirmation email; auto-approve, for any reason, including for testing.

### TASK 2.2.4 — Authenticated web shell: PKCE exchange, `HttpOnly` cookies, protected routes

**Milestone:** M2.2 · **Requirements:** FR-X-02, NFR-03, NFR-06 · **Decisions:** D-08, ADR-0017, ADR-0003 · **Depends on:** 2.2.1, 2.2.2, 1.1.1, 1.1.2 · **Blocks:** every authenticated page in Phases 3–4 · **Size:** M · **Cost:** £0.00 — one more Lambda and one more CloudFront behaviour, both inside existing free allowances.
**Context.** D-08 and ADR-0017 put the site on S3 + CloudFront with no server runtime, which collides with the only good place to keep a refresh token. There is no server on the static origin to set an `HttpOnly` cookie, so the obvious paths — token in `localStorage`, token in `sessionStorage` — leave a long-lived credential to a patient's clinical record readable by any script that gets onto the page. That is not an acceptable trade on this data, and the strict CSP shipped at 1.2.3 mitigates but does not remove it.
**Resolution: a token-exchange endpoint on the existing HTTP API, proxied same-origin through CloudFront** — the identical shape `/health` and `/contact` already use (`web-stack.ts`'s `additionalBehaviors`). The browser never handles a refresh token: it gets an authorization code, posts it to `/auth/token` on its own origin, and the Lambda performs the PKCE exchange and returns `Set-Cookie: HttpOnly; Secure; SameSite=Lax`. The access token stays in memory in the React island for its hour; refresh is another same-origin call. **Rejected:** Amplify's default browser storage (simplest, and the reason most tutorials use it — rejected because it puts a refresh token where script can read it, on a system holding clinical records).
**Files:** `services/api/src/auth-token-handler.ts` (new), `apps/web/src/auth/` (new — `session.ts`, `SignIn.tsx`, `RequireAuth.tsx`), `apps/web/src/pages/[locale]/account/` (new), `infra/src/web-stack.ts`, `packages/i18n/src/en.ts`, `docs/runbooks/web-authentication.md` (new).
**Steps.** 1. `/auth/*` as a new CloudFront behaviour onto the existing HTTP API origin, `CACHING_DISABLED`, `ALLOW_ALL` methods, the same response-headers policy — so the whole flow is same-origin and the CSP already covers it. 2. `POST /auth/token` exchanges an authorization code with PKCE against the right pool for the client, and `POST /auth/refresh` and `POST /auth/signout` complete the set; sign-out revokes the refresh token server-side (2.2.1 enabled revocation) rather than only dropping the cookie. 3. Cookies: `HttpOnly`, `Secure`, `SameSite=Lax`, `Path=/`, host-only on the apex, `Max-Age` matching the refresh token's own life. 4. `SameSite=Lax` plus a `POST`-only exchange covers CSRF for these routes; the reasoning goes in the runbook rather than being implied. 5. The React island holds the access token in a closure — not in a global, not in `window`, not in `localStorage` — and refreshes it on `401`. 6. `RequireAuth` renders nothing until the session resolves, so no protected content is ever in the DOM before authentication; the *page* stays statically generated and empty, which keeps ADR-0017's static-output decision intact. 7. Every string through `t()` — the i18n lint rule is in the required gate and will fail the build otherwise. 8. Sign-in, sign-out, error and loading states meet the 1.1.1 primitives and 1.1.3's axe + keyboard suites; the OTP input in particular is a real keyboard-accessibility trap and gets its own test. 9. Log group `/ndn/auth-token-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:** HTTP only — `POST /auth/token`, `POST /auth/refresh`, `POST /auth/signout`, all same-origin, all cookie-bearing.
**Tests.** Unit: PKCE verifier generation and the code exchange, with a mocked Cognito client. Unit: every cookie carries all four attributes — a table-driven assertion, so dropping one fails. Negative: the response body of every `/auth/*` route contains no refresh token. Negative: no bundle in `apps/web/dist` contains a `localStorage`/`sessionStorage` write on an auth path — a build-output assertion, because this is exactly the mistake a later refactor makes quietly. Negative: an expired access token triggers exactly one refresh, not a loop. pr-env (1.1.3): axe clean and keyboard-complete on sign-in, sign-out and the OTP entry; the OTP field is reachable, labelled and submittable by keyboard alone.
**Verification:** against the deployed site, a full sign-in leaves no token in `localStorage` or `sessionStorage` (checked in the browser, not inferred); sign-out revokes the refresh token such that a captured cookie no longer refreshes; the a11y suite is green on the new routes.
**Flag:** `auth.webSignIn.enabled` — default off; with it off the site is exactly the brochure it is today. **DoD:** a patient can sign in and out on the real domain; no token is reachable from script; protected content never reaches the DOM unauthenticated; the new routes pass the a11y gate. **Rollback:** flip the flag off — sign-in disappears, the static site is unaffected. **Do NOT:** put any token in `localStorage` or `sessionStorage`; add a non-`HttpOnly` auth cookie; make the account pages server-rendered; hard-code a user-facing string; bypass `RequireAuth` for a "quick" preview page.

### TASK 2.3.1 — Notification abstraction: email-primary, per-user preferences, recorded delivery

**Milestone:** M2.3 · **Requirements:** §5 (notifications), C-02 · **Decisions:** **D-10**, ADR-0009 · **Risks:** **R-01** · **Depends on:** 2.2.3, 1.4.1 · **Blocks:** 2.3.2, 3.4.x (appointment reminders) · **Size:** M · **Cost:** £0.00 net-new — SES volume sits inside `03-cost-model.md`'s existing line ($0.05/$0.18/$0.30).
**Context.** D-10 makes email the primary channel and SMS the exception, and R-01 is the arithmetic behind it: §5 asks for ~150 SMS/month and C-02's £5 buys ~108. The register's mitigation has four parts, and the one that is easy to skip is the last: **"never silently drop a reminder."** That is a property of the abstraction, not of the SMS provider, so it is built here, before a provider exists — the same reason `sms.ts` (0.5.3) already assembles every guard and deliberately calls no provider at all.
**Files:** `services/api/src/notifications.ts` (new), `services/api/src/notification-templates.ts` (new), `services/api/src/notification-log.ts` (new), `packages/i18n/src/notifications/` (new), `services/api/src/patient-repository.ts`, `docs/runbooks/notifications.md` (new).
**Steps.** 1. `Notifier.send(recipient, template, vars)` chooses a channel by policy, not by caller: **email always**, SMS only for the templates explicitly marked `smsEligible`, which at the end of this task is exactly one — the 1-hour appointment reminder. 2. Templates live in the i18n catalogues and render through `t()`, so a notification is translatable the day D-04's trigger fires and no string escapes the lint rule. 3. Per-user channel preferences on the patient record's `personal{}` half (marketing consent is already there) — and a preference can silence *marketing*, never a clinical or safety notification. That distinction is stated in the runbook and asserted in a test. 4. **Degradation, defined here rather than discovered later:** when SMS is unavailable for any reason — capped, killed, rate-limited, not-UK, provider error — the notification falls back to email plus an in-app record and **writes a delivery record marked `degraded` with the reason**. There is no path that returns success without a record and no path that returns silence. 5. `notification-log.ts` appends a delivery record per attempt — recipient id, template, channel, outcome, reason — identifiers only, no message body, no clinical content, same discipline as the audit log. 6. Bounces and complaints from the existing SES configuration set (`email-events.ts`) mark a recipient's email unreachable so the next send degrades rather than repeating into a void. 7. Nothing schedules anything: this task builds the sender. The reminder *schedule* is 3.4.x's, and building it here would be guessing at an appointment model that does not exist. 8. Log group `/ndn/notification-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:**

```ts
// services/api/src/notifications.ts
export type Channel = 'email' | 'sms' | 'in-app';
export type DeliveryOutcome = 'sent' | 'degraded' | 'failed';

export interface DeliveryRecord {
  readonly at: string;
  readonly recipientId: string;      // never an address
  readonly template: string;
  readonly channel: Channel;
  readonly outcome: DeliveryOutcome;
  readonly reason?: string;          // 'Capped' | 'Blocked' | 'NotUk' | 'RateLimited' | 'Bounced' | …
}
```

**Tests.** Unit: an `smsEligible` template with SMS capped sends email, records `degraded`, and names the reason — asserted for each of `Capped`, `Blocked`, `NotUk` and `RateLimited` separately, because R-01 is about the ones nobody notices. Unit: a non-eligible template never reaches the SMS path even with every flag on. Unit: a marketing preference of "none" silences marketing and does not silence a clinical notification. Unit: every template renders in `en` with no missing key, and a missing key fails rather than rendering a placeholder. Negative: **no code path returns success without appending a delivery record** — the test enumerates every branch. Negative: no delivery record contains an email address, a phone number or message content.
**Verification:** `pnpm test` green including the i18n lint rule; a manual send against the deployed SES identity produces one delivery record and one real email.
**Flag:** `notifications.enabled` — default off. **DoD:** every notification the platform can send goes through one function, is translatable, is recorded, and degrades loudly; SMS is reachable by exactly one template. **Rollback:** flip the flag off. **Do NOT:** call SES or the SMS sender directly from a handler; mark a second template `smsEligible` without re-doing R-01's arithmetic in the PR; let a user preference silence a clinical or safety notification; log a message body or an address.

### TASK 2.3.2 — SMS provider: re-verify UK prices, choose, wire behind 0.5.3's guards

**Milestone:** M2.3 · **Requirements:** C-02, NFR-09 · **Decisions:** **ADR-0008 (resolved by this task)**, D-11 · **Risks:** **R-01, R-02** · **Long-lead:** **LL-02** · **Depends on:** 2.3.1, 0.5.3, 0.5.1 · **Size:** M · **Cost:** £0.00 this month — no reminder exists to send until 3.4.x. Modelled at **$2.00 (M6) / $3.63 (M12)**, hard-capped at £5 = $6.05.
**Context.** ADR-0008 is the one ADR in the set that does not decide anything: "Provider chosen at M2.2 from re-verified UK prices; Twilio verified $0.056; AWS End User Messaging price **UNVERIFIED**." `09-self-audit.md` lists that price among the six deferred `UNVERIFIED` items, with re-verification due "at the gate that precedes its first real use." This is that task. `sms.ts` already assembles every guard — flags, +44 allow-list, per-principal rate limit, monthly spend cap — and its header states plainly that "there is no code path here, in a test or otherwise, that can send a real SMS." This task is the one that changes that sentence, and it is the highest-consequence line in the phase for C-02.
**Files:** `services/api/src/sms.ts`, `services/api/src/sms-provider.ts` (new), `docs/adr/0008-sms.md`, `docs/plan/03-cost-model.md`, `docs/plan/09-self-audit.md`, `docs/runbooks/sms-hard-cap.md`.
**Steps.** 1. **Re-verify both prices live before choosing** — AWS End User Messaging's UK rate from the pricing API or console, Twilio's from its published UK price — and record both, with the date and the method, in ADR-0008. A price found rather than assumed is the whole point of the deferral. 2. Choose on total cost including the sender-ID and registration overhead, not the per-message rate alone, and write the rejected option's number next to the chosen one. 3. `SmsProvider` as a one-method interface behind `createSmsSender`'s existing shape, so ADR-0008's "reversal cost: low — behind the notification abstraction" stays true and a provider swap touches one file. 4. **The provider is called only after every existing guard has passed**, in the order `sms.ts` already enforces; this task adds a call at the end of that chain and moves nothing. 5. The provider's own spend limit is set as the backstop D-11 requires — app-level cap *and* provider-level cap, because a bug in ours should still hit theirs. 6. Sender-ID registration and any UK onboarding is LL-02, owner-owned, 2–4+ weeks: **start it at the beginning of this task, not at the end.** 7. Prove the cap against the real provider in a sandbox: drive the counter to the cap and assert the next send is refused *before* the provider is called, with a delivery record marked `Capped` (2.3.1). 8. The anomalous-velocity alarm R-02 asks for is added to the existing budget/alarm stack. 9. Verify the +44 allow-list against the real provider's E.164 formatting, not only against our normaliser.
**Interfaces:**

```ts
// services/api/src/sms-provider.ts
export interface SmsProvider {
  send(to: string, body: string): Promise<{ readonly providerMessageId: string }>;
}
```

**Tests.** Unit: the provider is not called when any guard rejects — one test per guard: flag off, kill switch, non-UK, rate-limited, capped. Unit: a provider error yields `degraded`, not a thrown request, and is recorded. Integration (provider sandbox): a real send to a UK test number succeeds and returns a message id; a send to a non-UK number is refused by our allow-list before the provider ever sees it. Integration: at the cap, the next send is refused with the counter unchanged. Negative: no test, fixture or script can reach the provider without passing the full guard chain — asserted by a test that calls the chain with each guard failing in turn.
**Verification:** the cap is demonstrated against the real provider, not simulated; `03-cost-model.md`'s SMS line and `09-self-audit.md`'s `UNVERIFIED` list are updated with the verified rate; ADR-0008 names a provider.
**Flag:** `sms.enabled` — default off, with `sms.killSwitchEngaged` available as the independent stop. **DoD:** one provider is chosen on re-verified prices and recorded; a real SMS can be sent; the £5 hard cap is proven against the real provider; no send bypasses a guard. **Rollback:** `sms.enabled` off, or `sms.killSwitchEngaged` on — either stops sends with no deploy; the provider account's own spend limit stands behind both. **Do NOT:** call the provider from anywhere but `sms.ts`; raise the cap to make a test pass; send to a non-UK number; skip the provider-side spend limit because the app-side one exists; leave LL-02 to the end of the task.

### TASK 2.4.1 — Clinician accounts: invite, TOTP enrolment, principal and sub roles, deactivate-never-delete

**Milestone:** M2.4 · **Requirements:** §5 (clinician accounts), NFR-03 · **Decisions:** D-09 · **Risks:** R-09 · **Depends on:** 2.2.1, 2.2.2, 2.1.3, 2.3.1 · **Blocks:** 2.5.1, 2.5.3 · **Size:** M · **Cost:** £0.00
**Context.** The clinician directory, and the first place `04-data-model-rbac.md`'s "Clinician accounts: C R U (deactivate only), principal alone" row becomes real. Clinicians are never self-serve: the principal clinician creates them, which is why 2.2.1 disabled self sign-up on that pool at the directory level rather than trusting a check in a handler.
**Files:** `services/api/src/clinician-repository.ts` (new), `services/api/src/clinician-admin-handler.ts` (new), `packages/shared-types/src/clinician.ts` (new), `infra/src/data-stack.ts`, `docs/runbooks/clinician-accounts.md` (new).
**Steps.** 1. `CLI#<id>` / `PROFILE` with `role: 'principal' | 'sub'` and `active: boolean`, per the data model. 2. Creation is a principal-only route guarded by `can()`; it creates the Cognito user with `AdminCreateUser` (invite by email, temporary password) **and** the `CLI#` record in one operation, with the record written first so an orphaned Cognito user is the failure mode rather than an orphaned record. 3. TOTP enrolment is enforced by the pool (`mfa: REQUIRED`, 2.2.1) — Cognito will not complete the first sign-in without it, which is exactly why the two-pool decision was worth its cost. Nothing here re-implements it. 4. **Deactivation, never deletion:** `active: false` plus `AdminDisableUser`, plus a refresh-token revocation so a signed-in session ends now rather than in an hour. `AdminDeleteUser` is denied to the deploy role (2.2.1) and appears nowhere. 5. A deactivated clinician's *record* stays fully readable — their name still resolves on every past assignment, note and audit row, which is the reason never-delete exists here and not merely a policy echo. 6. Reactivation is the same route in reverse and is audited identically. 7. Exactly one `principal` may exist; a second is rejected at the repository, not the handler. Transferring the role is a distinct, audited operation, never an implicit side effect of creating someone. 8. Invite and deactivation emails go through 2.3.1's notifier. 9. Log group `/ndn/clinician-admin-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:**

```ts
// packages/shared-types/src/clinician.ts
export interface Clinician extends BaseRecord {
  id: string;
  subjectId: string;          // Cognito sub in the clinician pool
  displayName: string;
  role: 'principal' | 'sub';
  active: boolean;
}
```

**Tests.** Unit: creating a second `principal` is rejected. Unit: deactivation sets `active: false`, disables the pool user and revokes tokens — all three asserted, since any one alone leaves a live session. Unit: a deactivated clinician's record is still readable by id. Integration: a sub-clinician calling the create route gets `403`; a patient gets `403`. Negative: no code path calls `AdminDeleteUser` — a repo-wide assertion in the same family as `lint:no-destructive`. Negative: a deactivated clinician's token is refused within the authorizer cache window and their name still renders on historical records.
**Verification:** against the deployed pool, an invited clinician cannot complete sign-in without enrolling TOTP; deactivating one ends their session; their name still resolves afterwards.
**Flag:** `clinicians.administration.enabled` — default off. **DoD:** the principal clinician can create, deactivate and reactivate clinicians; TOTP is unavoidable; no clinician can be deleted; history never loses a name. **Rollback:** flip the flag off; the directory is unchanged. **Do NOT:** call `AdminDeleteUser`; remove a `CLI#` record; allow self sign-up on the clinician pool; let a second principal exist; deactivate without revoking tokens.

**Amended, D-30 (2026-08-29) then D-32 (2026-08-30).** D-30 already removed step 8's invite email — see `docs/runbooks/clinician-accounts.md`. D-32 removes step 8's other half, the deactivation notice: "any notification will go via whatsapp." `AdminDeactivateClinicianPort.getEmail` and `ClinicianAdminFunction`'s `ses:SendEmail`/`cognito-idp:AdminGetUser` grants are deleted; deactivation/reactivation mechanics are unchanged.

### TASK 2.5.1 — Approval and first assignment, and GSI1

**Milestone:** M2.5 · **Requirements:** §5 (approval and assignment), §7 (GSI1 access patterns) · **Decisions:** D-07 · **Depends on:** 2.2.3, 2.4.1, 2.1.3 · **Blocks:** 2.5.2, 3.x (every clinical entity is scoped by assignment) · **Size:** M · **Cost:** £0.00 net-new — GSI1 write units inside the modelled DynamoDB line.
**Context.** The hinge of the whole authorisation model. A patient sits in `pending` until a clinician approves them, and `assigned_clinician_id` is what every relationship check in 2.1.1 tests against — so from this task onward "assigned" is a real fact about the data rather than a parameter in a unit test. **GSI1** (clinician → patients, and later the clinician calendar) lands here, subject to the same prove-it-first condition GSI3 carries in 2.5.3.
**Files:** `services/api/src/assignment-repository.ts` (new), `services/api/src/assignment-handler.ts` (new), `packages/shared-types/src/assignment.ts` (new), `infra/src/data-stack.ts`, `docs/adr/0002-database.md`, `docs/runbooks/patient-assignment.md` (new).
**Steps.** 1. `PAT#<id>` / `ASSIGNREQ#<ts>` with `pending | approved | declined`, per the data model — the request is its own row, so the decision has a record independent of the patient's current state. 2. Approval does three things atomically in one `TransactWriteItems`: sets the request `approved`, sets the patient's `account_status: 'approved'` and `assigned_clinician_id`, and writes the GSI1 projection. Partial application here would produce an approved patient nobody is responsible for. 3. Add **GSI1** (`gsi1pk = CLI#<id>`), sparse, with the access patterns written down and checked against the key schema first and recorded in ADR-0002 — same discipline as 2.5.3, and the calendar pattern GSI1 also has to serve in 3.4.x is included in that check now, while the index is still cheap to shape. 4. Decline sets the request `declined` and the patient `declined`. **Neither row is removed**, and a declined patient can be re-approved later — the record of the earlier decision stays. 5. Only the principal clinician assigns; a sub-clinician may approve only onto themselves, if the matrix allows it at all — this task settles that against `authz-matrix.ts` rather than inventing a rule, and if the matrix is silent, the answer is deny and the matrix gets an explicit cell. 6. Approval and decline notify the patient through 2.3.1, content-free. 7. Every transition writes an audit row with the acting principal. 8. Log group `/ndn/assignment-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:**

```ts
// packages/shared-types/src/assignment.ts
export interface AssignmentRequest extends BaseRecord<'pending' | 'approved' | 'declined'> {
  patientId: string;
  requestedAt: string;
  decidedBy?: string;         // clinician subjectId
  decidedAt?: string;
  assignedClinicianId?: string;
}
```

**Tests.** Unit: approval is atomic — a forced failure on any leg leaves the patient `pending` with no GSI1 row, asserted per leg. Unit: a declined patient can be re-approved and both decisions survive. Integration: after approval, GSI1 returns the patient under the assigned clinician and under no other. Negative: an unassigned sub-clinician is denied on that patient's every route — the cross-tenant negative Gate G2 exists to demand, asserted route by route, not once. Negative: a patient cannot approve themselves, cannot assign, and cannot read another patient's assignment request. Negative: no path deletes a request row.
**Verification:** `aws dynamodb describe-table` shows GSI1; end-to-end, a registered patient is approved, appears under exactly one clinician, and is invisible to every other clinician.
**Flag:** `assignment.enabled` — default off; turned on together with `auth.patientRegistration.enabled`, since registration without approval strands people in `pending`. **DoD:** a patient moves `pending → approved` with an assigned clinician, atomically and audibly; GSI1 serves clinician→patients with its access patterns proven; every unassigned path is denied and tested. **Rollback:** flip the flag off — no further decisions can be made; existing assignments stand. **Do NOT:** set `assigned_clinician_id` outside the transaction; delete a request or a declined patient; let a sub-clinician assign to another clinician; skip the ADR-0002 access-pattern check because GSI1 "is obvious".

**Amended, D-32 (2026-08-30).** Step 6's own patient notification is deleted: "any notification will go via whatsapp." Approval and decline are otherwise unchanged — the same atomic write, the same audit row. Full reasoning: [01-decisions.md](01-decisions.md)'s D-32, [docs/runbooks/patient-assignment.md](../runbooks/patient-assignment.md).

### TASK 2.5.2 — Reassignment, with an append-only assignment history

**Milestone:** M2.5 · **Requirements:** §5 (reassignment) · **Depends on:** 2.5.1, 2.1.3 · **Size:** S · **Cost:** £0.00
**Context.** Caseloads move: a clinician leaves, a patient's needs change, a deactivated clinician (2.4.1) leaves patients behind. Reassignment is where an authorisation model quietly breaks — the old clinician keeps access because nothing revoked it, or the history is overwritten and nobody can say who was responsible last March. Both are handled here, and neither is handled by editing a field.
**Files:** `services/api/src/assignment-repository.ts`, `services/api/src/assignment-handler.ts`, `docs/runbooks/patient-assignment.md`.
**Steps.** 1. Reassignment appends a new `ASSIGNREQ#<ts>` row and updates `assigned_clinician_id` in one transaction. **The previous row is never edited** — the history is the sequence of rows, so "who was assigned on a given date" is answerable forever. 2. The previous clinician's access ends the moment `assigned_clinician_id` changes, because `can()` reads the current value; the authorizer's 300-second cache is the only lag and the runbook states it rather than leaving it to be found. 3. GSI1 gains the new clinician's projection row. The old row is left in place and filtered by the current `assigned_clinician_id` on read — **not deleted**, because `DeleteItem` does not exist in this codebase; the read-side filter is the cost of that rule and is asserted in a test rather than assumed. 4. Bulk reassignment away from a deactivated clinician is one audited operation per patient, never a silent sweep — the audit log should read as a list of decisions. 5. Both clinicians and the patient are notified through 2.3.1. 6. A patient is never left with no clinician: reassignment requires a target, and there is no "unassign".
**Interfaces:** `POST /patients/{id}/reassign` — principal-clinician only.
**Tests.** Unit: reassignment appends and never mutates a prior row. Unit: assignment history reconstructs correctly across three consecutive assignments. Integration: after reassignment, GSI1 filtered by current assignment returns the patient under the new clinician only, despite the stale projection row still existing. Negative: the previous clinician is denied on every one of that patient's routes after the change. Negative: there is no request shape that results in a patient with no `assigned_clinician_id`.
**Verification:** end-to-end, a patient reassigned between two clinicians is visible to exactly one at every point, and the audit log names both decisions.
**Flag:** `assignment.enabled` — the same flag as 2.5.1; reassignment is not separately switchable. **DoD:** reassignment is atomic and audited, history is complete and append-only, the previous clinician's access ends, no patient is ever unassigned. **Rollback:** flip the flag off. **Do NOT:** edit or delete a prior assignment row; delete a stale GSI1 projection; allow an "unassign"; reassign in bulk without one audit row per patient.

**Amended, D-32 (2026-08-30).** Step 5's own notification to both clinicians and the patient is deleted: "any notification will go via whatsapp." `AssignmentFunction`'s `cognito-idp:AdminGetUser`/`ses:SendEmail` grants are removed with it; reassignment's own atomic write and audit trail are unchanged.

### TASK 2.5.3 — Principal clinician's caseload view, and GSI3

**Milestone:** M2.5 · **Requirements:** **FR-DP-02** · **Decisions:** D-07 · **Depends on:** 2.4.1, 2.1.2, 2.5.1 · **Size:** M · **Cost:** £0.00 net-new — GSI3 adds write units on `PAT#`/`CLI#` writes only; inside `03-cost-model.md`'s DynamoDB line, and reconciled at G2 against real volume.
**Context.** FR-DP-02's cross-caseload admin view is the query `04-data-model-rbac.md` reserves **GSI3** for, and `09-self-audit.md`'s red-team names "admin cross-caseload views" as one of the two queries most likely to defeat the single-table design. 1.3.1 created GSI2 only and said the rest "land with the Phase 2/3 tasks that first need them." This is the task that first needs GSI3, and it must **prove the query against the key schema before writing the code** — ADR-0002's standing condition and the red-team's own mitigation.
**Files:** `infra/src/data-stack.ts`, `services/api/src/caseload-repository.ts` (new), `services/api/src/caseload-handler.ts` (new), `apps/web/src/pages/[locale]/account/caseload.astro` (new), `docs/adr/0002-database.md`.
**Steps.** 1. Write the access pattern down first — every read the principal's view performs, with its key condition and expected item count — and check it against the key schema on paper before any CDK change, recording the result in ADR-0002. If a pattern needs a `Scan`, the design is wrong and the ADR names the fallback rather than the code shipping a `Scan`. 2. Add **GSI3** as a sparse index projecting only what the list renders (`KEYS_ONLY` or a narrow `INCLUDE`) — a full projection doubles storage on the largest partition for a view nobody paginates deeply. 3. Sparse on purpose: only rows that belong in an admin view carry `gsi3pk`, so the index never has to be filtered down from everything. 4. Every read goes through 2.1.2's projection: the principal's matrix row is `R` on `private{}`, so it is one of the two roles that legitimately sees it — which makes this the first place the boundary is exercised in the *allow* direction, and the negative tests matter as much as the positive ones. 5. Paginate with `LastEvaluatedKey`; never accumulate a caseload in memory. 6. The page is a React island behind `RequireAuth` (2.2.4), keyboard-navigable, and axe-clean under 1.1.3. 7. Log group `/ndn/caseload-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:** `GET /caseload?cursor=` — principal-clinician only, paginated.
**Tests.** Unit: every documented access pattern resolves to a `Query`, never a `Scan` — asserted by inspecting the command each repository method issues. Unit: pagination round-trips a cursor and never drops or repeats an item across pages. Integration: GSI3 returns only rows carrying `gsi3pk`. Negative: a sub-clinician and a patient each get `403`, and a sub-clinician cannot reach another clinician's caseload by any parameter. Negative: the response for a sub-clinician-scoped variant carries no `private{}` field. pr-env: the caseload page is axe-clean and keyboard-complete.
**Verification:** `aws dynamodb describe-table` shows GSI3 with the intended projection; the deployed endpoint returns a paginated caseload for the principal and `403` for everyone else; ADR-0002 records the proven access patterns.
**Flag:** `caseload.view.enabled` — default off. **DoD:** the principal clinician sees every patient across every caseload; the query is proven against the key schema and documented in ADR-0002; no `Scan` reaches the table; nobody else can call it. **Rollback:** flip the flag off. GSI3 stays — dropping an index is a table operation, not a revert. **Do NOT:** add a `Scan` "just for admin"; project the whole item into GSI3; create GSI4 speculatively while in the file; return an unpaginated caseload.

### TASK 2.5.4 — Retire the `ADMIN_API_TOKEN` bearer gate

**Milestone:** M2.5 · **Requirements:** NFR-03, NFR-06 · **Risks:** R-07 · **Depends on:** 2.2.2, 2.4.1 · **Size:** M · **Cost:** £0.00 — removes one SSM parameter and four grants.
**Context.** `services/api/src/admin-auth.ts` has said since 1.3.2 exactly what it is: "one narrow, explicitly-temporary bearer-token gate… no user identity, no session, no scopes — just 'did the caller present the one shared secret'… **Superseded by Phase 2's Cognito RBAC**." Four production endpoints stand behind it today — content authoring, workshop authoring, testimonial moderation and media upload. It is a single shared secret with no attribution: the audit log built in 2.1.3 records `actor` for every write, and for these four routes that actor is a token rather than a person. Phase 2 is the phase that promised to remove it, and a temporary gate that survives its own replacement is how a shared secret becomes permanent.
**Files:** the four `verifyAdminToken` call sites — `services/api/src/content-authoring.ts`, `media-upload.ts`, `testimonial-moderation.ts`, `workshop-authoring.ts` — and the four handlers that resolve the secret for them, `content-authoring-handler.ts`, `media-upload-handler.ts`, `testimonial-moderation-handler.ts`, `workshop-authoring-handler.ts`; `services/api/src/admin-auth.ts` (deleted); `infra/src/data-stack.ts`, `infra/src/web-stack.ts`, `infra/src/config.ts`; `docs/runbooks/content-authoring.md`, `docs/runbooks/testimonials.md`, `docs/runbooks/workshops.md`.
**Steps.** 1. Put all four routes behind 2.2.2's authorizer and `can()`, with clinician-role authorisation replacing "presented the secret". 2. Their audit rows now carry a real `subjectId` and `actorRole` instead of an anonymous actor — the reason this task belongs in the phase that built the audit log, not later. 3. Delete `admin-auth.ts` and its tests. Deleting *code* is not what C-03 prohibits; leaving a live shared-secret path in place because "delete" appears in the sentence would be a misreading. 4. Remove `ADMIN_TOKEN_PARAMETER_NAME` from all four functions’ environments and the four `ReadAdminApiToken` IAM statements — three in `data-stack.ts` (content authoring, testimonial moderation, workshop authoring), one in `web-stack.ts` (media upload, which lives next to `MediaBucket`). A fifth mention of the name exists in `contact-form-handler.ts` and is a comment only — correct its reference rather than deleting the SSM-caching reasoning around it. 5. The SSM parameter `/ndn/admin-api-token` is **deleted by the owner by hand after the deploy is verified**, not by CDK and not by this task's code — the same convention every other secret in `config.ts` follows for creation, applied to retirement. Until then it is inert: nothing reads it. 6. Update the three runbooks that document `curl -H "Authorization: Bearer $ADMIN_API_TOKEN"` so the documented procedure is the real one; a runbook describing a retired auth mechanism is a defect. 7. Sequencing matters: this lands **after** at least one clinician account exists (2.4.1) and has been proven to sign in, or the four endpoints become unreachable by anyone.
**Interfaces:** unchanged — same routes, same request and response shapes, different authentication.
**Tests.** Unit: each of the four handlers rejects a request with no principal, and accepts one with a clinician principal. Negative: a valid *bearer token* — the old mechanism — is now rejected on all four routes, asserted individually; this is the test that proves retirement rather than addition. Negative: a patient principal is rejected on all four. Negative: `grep` finds no reference to `ADMIN_API_TOKEN` or `admin-auth` in `services/` or `infra/` — a build-level assertion. Integration (synth): no function's environment carries `ADMIN_TOKEN_PARAMETER_NAME` and no role holds a `ReadAdminApiToken` statement. Regression: the four endpoints' existing behaviour tests still pass unchanged apart from how the caller authenticates.
**Verification:** against the deployed API, a clinician token succeeds on all four routes and the old bearer token gets `403` on all four; `aws iam simulate-principal-policy` shows no role can read `/ndn/admin-api-token`; the three runbooks describe the Cognito flow.
**Flag:** none — a route cannot have two authentication mechanisms behind a flag without the weaker one being reachable. This is a cutover, and it is small enough to revert. **DoD:** no shared-secret authentication path exists anywhere in the platform; all four admin endpoints authenticate a person; their audit rows name that person. **Rollback:** revert the branch — the bearer gate returns, and the SSM parameter must therefore not be deleted until the deploy has been verified in production (step 5's ordering is the rollback plan). **Do NOT:** delete `/ndn/admin-api-token` in the same change that removes its readers; leave `admin-auth.ts` in the tree "in case"; land this before a clinician can sign in; leave a runbook documenting the retired mechanism.

**Gate G2:** patients and clinicians authenticate; every role boundary in `04-data-model-rbac.md` is implemented and negatively tested; every change to a person or a record is durably audited; no shared secret authenticates a human. Gate-specific addition, per `06-gate-checklists.md`: **a negative test for every cross-tenant path** — patient→other patient, sub-clinician→unassigned patient, patient→`private{}`, and the retired bearer token against all four legacy admin routes.

## Phase 3 — Clinical core

*(Elaborated to full §12 detail at Gate G2, per D-27 — the same policy that elaborated Phase 2 at Gate G1. Phases 4–7 remain stubs below, elaborated at their own gates.)*

**A citation limitation this elaboration inherits from Phase 2's.** TASK 2.5.4's own header (line 379 of this file, before Phase 2) recorded that the full FR/NFR requirements matrix was never committed to this repo — only `FR-DP-02`, `FR-DP-05`, `FR-PP-10`, `FR-VID-04` and the `FR-X-*`/`FR-WEB-*` IDs Phase 1 established are pinned to a requirement anywhere in the tree, and reconstructing the matrix is a still-open action item from the Gate G1 report that `09-self-audit.md` says should close "before G2 audits coverage." It has not closed. Phase 3's tasks below cite only the IDs already pinned, plus `§5` (the brief's own patient/clinician/clinical-record section) where a task's subject matches it directly, exactly as Phase 2's did — no new `FR-DP-0x`/`FR-PP-0x` number is invented here for diagnosis, care plans, assessments, appointments or messaging, because nothing in this repo has ever pinned one. **This gate does not close the gap either.** It is named again here, at the point Gate G2 was supposed to close it, so the open item stays visible rather than quietly aging out.

**What Phase 3 inherits, built and unused.** Three pieces of Phase 2 infrastructure exist specifically for entities that did not exist yet when they were built, and every task below is that promise being kept rather than new invention: `services/api/src/repository.ts`'s `Repository<T>` (generic CRUD, audit-wired, `Unprojected<T>`-branded) for simple entities; `services/api/src/versioned-repository.ts`'s `VersionedRepository<T>` (append-only, one immutable key per version, audit-wired) for diagnosis, care plan and assessment forms, named directly in its own header as "the read path 3.2.x wires through `projectFor`"; and `services/api/src/authz-matrix.ts`'s `ASSESSMENT_ENTITY_TYPE`/`ASSESSMENT_ROWS`, coded in TASK 2.1.1 against a `fieldSet: 'visible' | 'private'` that no assessment record existed to populate until M3.3. `docs/adr/0002-database.md` also already proves GSI1's clinician-calendar access pattern (`gsi1pk = CLI#<clinicianId>`, `gsi1sk` ranged over `APPT#<iso-utc>`) "not built here — key shape checked now, while the index is still cheap to shape," naming TASK 3.4.x as the task that builds it. This phase is where each of those four promises is collected.

**One matrix gap settled here, the same way TASK 2.5.1 settled "Patient assignment."** `04-data-model-rbac.md`'s key-shape table describes the Message entity as "Patient↔clinician" — bidirectional — but its RBAC matrix row as transcribed grants the patient `C R (own thread)` and the assigned sub-clinician only `R (own patients)`: no `C`. A channel a patient can write into and a clinician can only read is not what "Patient↔clinician" describes, and no decision, ADR or runbook anywhere in the tree gives a one-way channel a rationale — the far more likely explanation is a transcription gap of exactly the shape 2.5.1 found in the same table ("Patient assignment" had no row at all). Settled the same way: read literally against the entity's own description, not against a silent asymmetry. M3.6 below corrects the `Sub-clinician (assigned)` cell to `C R (own patients)` and records the correction in `04-data-model-rbac.md` before writing any code against it — the doc-first discipline `authz-matrix.ts`'s own header requires of every change to that table.

### TASK 3.1.1 — The patient's own profile, read and updated for real

**Milestone:** M3.1 · **Requirements:** §5 (patient record) · **Depends on:** 2.2.3, 2.1.2 · **Blocks:** 3.1.2, 3.2.x, 3.4.x, 3.5.x, 3.6.x (every later Phase 3 task reads or links against a real `PAT#` profile) · **Size:** M · **Cost:** £0.00 — one more 128 MB arm64 function inside the always-free allowance; no new AWS resource.
**Context.** TASK 2.2.3 created the `PAT#<id>` / `PROFILE` record and the approval lifecycle on it; nothing since has ever read or written that record through an HTTP route. `authz-matrix.ts`'s `'Patient profile'` row (`R U (self)` for the owning patient, `C R U` for an assigned or unassigned-until-approval sub-clinician, `R U` for the principal) has stood fully implemented and exhaustively tested since TASK 2.1.1 with no handler ever calling it. This task is that handler — the milestone note in this plan's own stub table put it exactly: "M3.1 starts from the `PAT#`/`PROFILE` record 2.2.3 creates, not from nothing."
**Files:** `services/api/src/patient.ts` (new), `services/api/src/patient-handler.ts` (new), `services/api/src/patient-repository.ts`, `infra/src/data-stack.ts`, `docs/runbooks/patient-record.md` (new).
**Steps.** 1. `GET /patients/{id}`, `PATCH /patients/{id}` — one handler, `can(principal, 'read'|'update', { entityType: 'patient-profile', ownerPatientId, assignedClinicianId })` per route, the same shape `caseload.ts` (2.5.3) and `clinician-admin.ts` (2.4.1) already establish. `id` for a patient principal must equal their own `patientId`, checked before `can()` runs — a patient guessing at another id gets the matrix's `'Patient profile' × 'Patient (other)'` denial, not a lookup that leaks whether the id exists. 2. **The `PATCH` body's shape depends on who is calling, and this is a schema decision, not a bigger matrix cell.** The matrix's `R U` grants the patient `update` on the row; it does not say which fields. `person-record.ts`'s clinical/personal split already exists for exactly this: a patient-supplied `PATCH` accepts `personal{}` only (`fullName`, `phone`, `marketingOptIn` — never `email`, which stays bound to the Cognito identity that authenticated the request); a clinician-supplied `PATCH` accepts both `personal{}` and `clinical{}`. Two Zod schemas, not two routes — the same `PATCH /patients/{id}` dispatches on `principal.role`. 3. `PatientRepository` gains `findById`/`update` if it does not already expose them cleanly for this shape; no new store — `DynamoPatientStore` already exists from 2.2.3. 4. Every write reaches `projectFor` — patients carry no `private{}` half today, so this is a no-op in practice and a closed door in the future, the identical reasoning `caseload-repository.ts`'s own doc comment already states for the same reason. 5. A minimal `apps/web/src/pages/[locale]/account/patient.astro` page (statically empty, `RequireAuth`-gated, same discipline as `account/caseload.astro`) lets a signed-in patient see and edit their own personal details — the first real page inside the account shell 2.2.4 built with nothing behind it yet. 6. Log group `/ndn/patient-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:** `GET /patients/{id}`, `PATCH /patients/{id}` — real Lambda authorizer, no override.
**Tests.** Unit: a patient reading/updating their own id succeeds; the identical call against any other id is `403` before a lookup happens. Unit: a patient's `PATCH` body containing a `clinical{}` key is rejected with `400`, not silently dropped — a caller that tries to write a field it cannot see should learn that, not be quietly ignored. Unit: a patient's `PATCH` cannot change `email`, even inside `personal{}`. Integration: an assigned sub-clinician can read and update both halves; an unassigned sub-clinician gets `403`; the principal can read and update both halves regardless of assignment. Negative: a `pending`/`declined`/`suspended` patient can still read their own profile (status gates to *own-profile-read-only*, per `can()`'s own rule) and nothing else changes because of this task. pr-env: the new account page is axe-clean and keyboard-complete — the same honestly-scoped gap as every prior authenticated page in this codebase (no live-session pr-env mechanism exists yet), documented in the runbook rather than claimed.
**Verification:** against a deployed stack, a patient's own token round-trips a personal-field edit and is refused a clinical-field one; a clinician's token round-trips both; `aws dynamodb get-item` shows no `private` attribute on any patient row.
**Flag:** `patients.profile.enabled` — default off. **DoD:** a patient can read and edit their own personal details; a clinician can read and edit an assigned patient's full record; nobody else can reach any patient's record; no route accepts a patient-authored write to `clinical{}` or `email`. **Rollback:** flip the flag off. **Do NOT:** let a patient's `PATCH` touch `clinical{}` or `email`; add a second lookup path that skips `can()`; guess at another patient's id before checking it against the caller's own.

### TASK 3.1.2 — A sub-clinician's own caseload, at last using GSI1's deferred grant

**Milestone:** M3.1 · **Requirements:** §5 · **Decisions:** D-07 · **Depends on:** 3.1.1, 2.5.1 · **Size:** S · **Cost:** £0.00 — GSI1 already exists (2.5.1); this task adds one `Query` IAM grant and one route, no new index.
**Context.** TASK 2.5.1 built GSI1 (`gsi1pk = CLI#<id>`) and its own `listPatientIdsForClinician` query, then deliberately withheld the IAM grant that would let any function actually call it: "nothing this function's own routes call reaches `listPatientIdsForClinician`; that grant lands with whichever future task first calls it." No task has called it since. A sub-clinician today has no way to see their own caseload at all — TASK 2.5.3 built the *principal's* cross-caseload view (GSI3, everyone), which a sub-clinician is explicitly denied. This task is the other half: a sub-clinician's own list of their own assigned patients.
**Files:** `services/api/src/patient.ts`, `infra/src/data-stack.ts`.
**Steps.** 1. `GET /caseload/mine` (deliberately not `GET /caseload`, which 2.5.3 already owns for the principal's cross-caseload view, and deliberately not a query parameter on it — a sub-clinician's caseload and the principal's are different queries against different indexes, and collapsing them onto one path was exactly the mistake TASK 2.5.4's own runbook found and reversed for `GET /testimonials`). `can(principal, 'read', { entityType: 'patient-profile' })`, denied to a patient by the matrix's own `'Patient (own)'`/`'Patient (other)'` columns, allowed to both clinician roles and the principal. 2. For a sub-clinician, the query is `listPatientIdsForClinician(principal.clinicianId)` — their own assigned patients, GSI1, `Query` never `Scan`, paginated the same way 2.5.3's `CaseloadRepository` paginates. For the principal, this route is redundant with `GET /caseload` and simply delegates to the same GSI1 query scoped to their own `clinicianId` — a principal is also a clinician who can have their own assigned patients. 3. The new IAM grant is `dynamodb:Query` on `[table ARN, table ARN + '/index/GSI1']`, added to whichever function serves this route (folded into the function 3.1.1 creates, not a new one) — the exact grant 2.5.1's runbook named and deferred. 4. Every returned patient goes through `projectFor`, same as 3.1.1.
**Interfaces:** `GET /caseload/mine?cursor=` — either clinician role, paginated.
**Tests.** Unit: a sub-clinician sees exactly their own assigned patients, in GSI1's order, never another clinician's. Negative: a patient principal gets `403`. Negative: pagination round-trips a cursor with no drops or repeats, same property 2.5.3's own suite proves for GSI3. Integration (synth): the new `Query` grant names exactly `[table, table/index/GSI1]` — no `Scan` action anywhere in the statement.
**Verification:** `aws iam simulate-principal-policy` shows `allowed` for `dynamodb:Query` on the GSI1 ARN and `implicitDeny` for `dynamodb:Scan`; a sub-clinician's deployed token returns only their own caseload.
**Flag:** `patients.profile.enabled` — reused from 3.1.1; this route is a small enough extension of the same surface that a second flag would only be a second thing to remember to turn on together. **DoD:** a sub-clinician can list their own caseload; nobody sees another clinician's; no `Scan` reaches the table. **Rollback:** flip the flag off. **Do NOT:** let this route accept a `clinicianId` parameter that could point at someone else's caseload; reuse `GET /caseload`'s own path for a differently-scoped query.

### TASK 3.2.1 — Diagnosis and care plan: versioned, clinician-authored, with private notes

**Milestone:** M3.2 · **Requirements:** §5, R-09 · **Decisions:** D-07 · **Depends on:** 3.1.1, 2.1.2, 0.3.3 · **Blocks:** 3.2.2, 3.3.1 (assessment forms follow the identical shape) · **Size:** M · **Cost:** £0.00 — `PAT#<id>` / `DIAG#<v>` and `PLAN#<v>` rows are a few hundred bytes each, inside the existing DynamoDB line.
**Context.** R-09 — a clinician's private notes reaching a patient — is the register's one **Critical**-rated risk, and `02-risk-register.md`'s own amendment at Gate G1 built the chokepoint (`projection.ts`, TASK 2.1.2) one phase ahead of schedule specifically so it would exist before any real entity did. This is that entity: the first record in the whole platform whose clinician-authored content includes something a patient must never see, alongside content a patient must. `04-data-model-rbac.md`'s key shapes already name both — `PAT#<id>` / `DIAG#<v>` and `PLAN#<v>`, "**Versioned, append-only**" — and the RBAC matrix already grants the patient bare `R` on the row and either clinician role `C R U`.
**Files:** `packages/shared-types/src/clinical-record.ts` (new), `services/api/src/clinical-record-repository.ts` (new), `services/api/src/clinical-record.ts` (new), `services/api/src/clinical-record-handler.ts` (new), `services/api/src/dynamo-store.ts`, `infra/src/data-stack.ts`, `docs/runbooks/clinical-record.md` (new).
**Steps.** 1. Diagnosis and care plan share one shape and one task, because `04-data-model-rbac.md` gives them identical matrix cells and an identical versioned key pattern differing only in the `DIAG#`/`PLAN#` prefix — one `VersionedRepository<ClinicalRecord>` instantiated twice, against two `KeyValueStore` prefixes, not two parallel implementations. 2. `ClinicalRecord { visible: { summary: string }; private?: { notes: string } }` — `visible{}` is what `04-data-model-rbac.md`'s bare patient `R` reads; `private{}` is R-09's own content, present only on clinician-authored versions and absent, not empty, when a clinician chooses to record nothing private for a given version. 3. `POST /patients/{id}/diagnosis`, `POST /patients/{id}/care-plan` — each call creates the *next* version (`VersionedRepository.createVersion`, whose own `attribute_not_exists` guard already makes "overwrite version N" a thrown error, not a possible request). No `PATCH`: a correction is a new version, not an edit — the append-only property is a route that does not exist, the same discipline `content-authoring.ts`'s "no `DELETE`" already established for a different reason. 4. `can(principal, 'create'|'update', { entityType: 'diagnosis'|'care-plan', assignedClinicianId })` gates the write; only an assigned sub-clinician or the principal reaches it. 5. Every write reaches `projectFor` before it is echoed back to its own author — the "forgot to project" case TASK 2.1.2's own header names as the reason a freshly created record is branded too, not only a read one. 6. Log group `/ndn/clinical-record-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:**

```ts
// packages/shared-types/src/clinical-record.ts
export interface ClinicalRecordVisible { readonly summary: string }
export interface ClinicalRecordPrivate { readonly notes: string }
export interface ClinicalRecord extends VersionedRecord {
  readonly patientId: string;
  readonly visible: ClinicalRecordVisible;
  readonly private?: ClinicalRecordPrivate;
}
```

**Tests.** Unit: creating version 2 while version 1 exists succeeds and leaves version 1 unchanged and independently readable; creating version 1 twice throws `VERSION_ALREADY_EXISTS`, `VersionedRepository`'s own existing guarantee, exercised here for the first time against a real entity. Unit: a version created with no `private{}` argument stores no `private` key at all (`Object.keys` assertion), not an empty object — the distinction `projection.test.ts`'s own suite already draws for "absent" vs "empty." Negative: an unassigned sub-clinician's `create`/`update` is `403`. Negative: a patient's `POST` to either route is `403` before any write — `can()` denies `create` on the row entirely for the `'Patient (own)'` column, which only grants `R`. Integration: `dynamo-store.test.ts`'s `DynamoClinicalRecordStore` case proves the write is a real `PutCommand`, never a `Scan`-reachable path.
**Verification:** `aws dynamodb get-item` on a version written with no private notes shows no `private` attribute in the raw item; two versions of the same diagnosis both remain independently retrievable.
**Flag:** `clinicalRecords.enabled` — default off. **DoD:** a clinician can author successive versions of a patient's diagnosis and care plan, each with an optional private half; no version can be overwritten; a patient cannot create or update either. **Rollback:** flip the flag off. **Do NOT:** add an `UpdateItem`/`PATCH` path that mutates an existing version; let a `private{}` value reach the create-response's echo without going through `projectFor`; merge diagnosis and care plan into a single record type — they are two entities that happen to share a shape, not one entity with a type field.

### TASK 3.2.2 — The patient's own read: diagnosis and care plan, visible half only

**Milestone:** M3.2 · **Requirements:** §5, FR-DP-05, R-09 · **Depends on:** 3.2.1 · **Blocks:** 3.3.2 (the assessment read follows the identical proof shape) · **Size:** S · **Cost:** £0.00
**Context.** 3.2.1 built the write side and the record shape; nothing yet lets a patient — or a clinician catching up on a colleague's notes — read it back. This is the first task in the whole platform where `projectFor` is exercised in the **allow** direction for a real, populated `private{}` field rather than the no-op it has been against `Patient`'s empty one, and it is why R-09's own register entry names `3.2.x` as "the task that wires the first real entity through it": everything since 2.1.2 has proven the mechanism works against a boundary nothing has ever tested with real content behind it.
**Files:** `services/api/src/clinical-record.ts`, `services/api/src/clinical-record-handler.ts`, `apps/web/src/pages/[locale]/account/patient.astro`, `docs/runbooks/clinical-record.md`.
**Steps.** 1. `GET /patients/{id}/diagnosis`, `GET /patients/{id}/care-plan` — return every version, newest first (`VersionedRepository` has no "list all versions" method yet; add `listVersions(id): Promise<Unprojected<T>[]>` to it directly, reading versions `1..latest` — the simplest correct implementation for a low-version-count entity, and one `VersionedRepository` method benefits every future versioned entity that follows, not a bespoke query in this file). 2. Every version passes through `projectFor(principal, record, { entityType, assignedClinicianId })` individually before the array is returned — a patient's response has every `private{}` key stripped from every version, not only the latest; a clinician's or the principal's has none stripped. 3. Extend 3.1.1's account page with a read-only diagnosis/care-plan timeline for the signed-in patient — visible-only content, rendered from exactly what the API already returned stripped, never a client-side filter of a fuller payload (the allow-then-hide anti-pattern `private-field-boundary.md` already warns against). 4. The negative-test convention `private-field-boundary.md` states ("negative test per endpoint, forever") gets its first two real entries here, named individually in the runbook rather than left implicit.
**Interfaces:** `GET /patients/{id}/diagnosis`, `GET /patients/{id}/care-plan` — real authorizer, `can(principal, 'read', …)`.
**Tests.** Unit: a patient's response array contains zero occurrences of the string `"private"` at any depth, asserted with `containsPrivateField` (`projection.ts`, reused directly, not reimplemented) against the full JSON body, for a fixture where every version carries private notes. Unit: a clinician's and the principal's responses carry every version's private half intact. Negative: a patient reading another patient's diagnosis (guessed id) is `403`, never a 200 with an empty array — the "does this id exist" question a wrong-tenant caller must not be able to answer from the response shape. Negative: an unassigned sub-clinician is `403`. pr-env: the extended account page is axe-clean and keyboard-complete for the read-only timeline (same honestly-scoped gap as 3.1.1 — no live-session pr-env mechanism exists to verify this beyond construction).
**Verification:** a patient's deployed token returns a diagnosis history with no `private` key anywhere in the response body (grepped, not eyeballed); a clinician's token returns the same history with private notes intact.
**Flag:** `clinicalRecords.enabled` — reused from 3.2.1; a read half with no write half to read is not a useful flag state. **DoD:** a patient can read their own diagnosis/care-plan history with every private note stripped, at every version, not only the latest; a clinician and the principal read the full history. **Rollback:** flip the flag off. **Do NOT:** filter `private{}` client-side after a fuller fetch; add a "trusted" query parameter that skips `projectFor`; return an unprojected record from any code path, including an error message that might echo one.

### TASK 3.3.1 — Assessment forms: the two-row `visible{}`/`private{}` split, for real

**Milestone:** M3.3 · **Requirements:** §5, R-09 · **Depends on:** 3.2.1 · **Size:** M · **Cost:** £0.00 — `PAT#<id>` / `ASSESS#<id>#v<n>` rows inside the existing DynamoDB line.
**Context.** `authz-matrix.ts` has carried `ASSESSMENT_ENTITY_TYPE` and two dedicated matrix rows (`'Assessment — visible{}'`, `'Assessment — private{}'`) since TASK 2.1.1, and `authz.test.ts`'s exhaustive suite has asserted every cell of both since the same task — including the row `R-09`'s own test names directly: `'a patient reaches no private assessment field, in any relationship'`. No assessment record has existed for any of that to run against a real handler until now. Unlike diagnosis/care-plan's single matrix row with an internal visible/private split (3.2.1), an assessment is **two matrix rows for one entity** — `can()` needs an explicit `fieldSet: 'visible' | 'private'` on every call, per `resolveRow`'s own "an assessment lookup that doesn't say which half it wants is a denial, not a guess."
**Files:** `packages/shared-types/src/assessment.ts` (new), `services/api/src/assessment-repository.ts` (new), `services/api/src/assessment.ts` (new), `services/api/src/assessment-handler.ts` (new), `services/api/src/dynamo-store.ts`, `infra/src/data-stack.ts`, `docs/runbooks/assessment-forms.md` (new).
**Steps.** 1. `Assessment { visible: { formType: string; responses: Record<string, unknown> }; private?: { clinicianImpression: string } }` — same `VersionedRepository<Assessment>` pattern as 3.2.1, keyed `PAT#<id>` / `ASSESS#<id>#v<n>` per the data model's own shape (an assessment `id` distinct from its version, unlike diagnosis/care-plan which version the whole record — an assessment is a named form, e.g. "initial mobility assessment," that a clinician re-administers over time, so `id` names *which* form and `v<n>` names *which sitting*). 2. `POST /patients/{id}/assessments/{assessmentId}` creates the next version of that named form; no `PATCH`, identical append-only reasoning to 3.2.1. 3. `can()` is called twice per read where both halves might be needed — once per `fieldSet` — never once with a fabricated "give me everything" resource; the two-row design exists so a caller must ask each question separately, which is what makes each answerable and testable independently rather than merged into one implicit decision. 4. `create`/`update` reach the matrix's `'Assessment — visible{}'` row (an assigned sub-clinician or the principal can write the visible half); a `private{}` value on the same write is permitted by construction of the request shape, not a separate authorisation call — writing is one action on one record, reading is where the split matters. 5. Every write reaches `projectFor` before its own echo, identical to 3.2.1.
**Interfaces:** `POST /patients/{id}/assessments/{assessmentId}` — real authorizer, clinician roles only.
**Tests.** Unit: creating version 2 of assessment `mobility-initial` leaves version 1 independently retrievable — `VersionedRepository`'s own guarantee, exercised against a named-form key shape for the first time. Negative: a patient's `POST` is `403`. Negative: an unassigned sub-clinician's `POST` is `403`. Integration: `dynamo-store.test.ts` proves the write is a `PutCommand`. Type-level: a call site that reaches `can()` for an `'assessment'` resource with no `fieldSet` does not compile against this file's own usage (the existing `authz.ts` runtime denial is defence in depth; this task's own code never constructs the omittable shape to begin with).
**Verification:** two versions of the same named assessment both remain independently readable after deploy; `aws dynamodb get-item` on a version with no clinician impression recorded shows no `private` attribute.
**Flag:** `assessments.enabled` — default off. **DoD:** a clinician can author successive versions of a named assessment form, each with an optional clinician-only impression; no version can be overwritten; a patient cannot create one. **Rollback:** flip the flag off. **Do NOT:** collapse the two-`fieldSet` design into one `can()` call with an inferred field set; add an update path that mutates an existing version.

### TASK 3.3.2 — Reading an assessment: visible to the patient, both halves to a clinician

**Milestone:** M3.3 · **Requirements:** §5, FR-DP-05, R-09 · **Depends on:** 3.3.1, 3.2.2 · **Size:** S · **Cost:** £0.00
**Context.** The read side of 3.3.1, and the task R-09's own register entry and `09-self-audit.md`'s red-team both point at most directly — the two-row matrix split exists specifically so this read can be proven, not merely typed. 3.2.2 already proved the mechanism once against a single-row entity; this proves it against the two-row shape the matrix actually reserves for the case a leak would matter most.
**Files:** `services/api/src/assessment.ts`, `services/api/src/assessment-handler.ts`, `apps/web/src/pages/[locale]/account/patient.astro`, `docs/runbooks/assessment-forms.md`.
**Steps.** 1. `GET /patients/{id}/assessments/{assessmentId}` — for a patient principal, resolves `fieldSet: 'visible'` alone and the response never carries a `private` key because the value was never fetched into the response path, not merely stripped from it after; for either clinician role, resolves both `fieldSet`s and merges. This mirrors `projection.ts`'s own two-brand design at the handler level: the visible-only path and the full path are structurally different code, not one path with a redaction step. 2. `VersionedRepository.listVersions` (3.2.2) serves this the same way it serves diagnosis/care-plan — one method, its second real caller. 3. Extend the patient account page with the same form's response history, visible fields only. 4. The runbook records this as the negative-test convention's third and most direct entry: the exact test `R-09`'s register line and `authz.test.ts`'s own existing test name both call out.
**Interfaces:** `GET /patients/{id}/assessments/{assessmentId}` — real authorizer, `can()` per `fieldSet`.
**Tests.** Unit (the R-09 test, named as such): a patient's response to a version carrying a clinician impression contains no `private` key and no `clinicianImpression` string anywhere in the body — asserted against the raw serialised response, not the pre-serialisation object, because a leak through `JSON.stringify` on the wrong value is exactly the class of bug a chokepoint upstream cannot catch if this test only checks the object. Unit: a clinician's response for the same version contains both halves. Negative: an unassigned sub-clinician is `403`. Negative: a patient reading another patient's assessment (guessed id) is `403`, not a 200 with a partial body.
**Verification:** a patient's deployed token, called against a version known to carry a clinician impression, returns a body that contains neither the word `private` nor the impression text, confirmed by inspecting the raw HTTP response.
**Flag:** `assessments.enabled` — reused from 3.3.1. **DoD:** a patient reads only the visible half of any assessment version, ever; a clinician reads both; the platform's highest-consequence negative test (R-09, named in the risk register since Gate G1) passes against a real, deployed endpoint for the first time. **Rollback:** flip the flag off. **Do NOT:** fetch the private half into a response the code then filters; let an error message on this path echo any part of a fetched-but-undelivered private value.

### TASK 3.4.1 — Appointments, and GSI1's second half: the clinician calendar

**Milestone:** M3.4 · **Requirements:** §5, FR-VID-04 (the entity video eventually attaches to) · **Decisions:** D-07 · **Depends on:** 3.1.2 · **Blocks:** 3.4.2, 3.4.3 · **Size:** M · **Cost:** £0.00 net-new — GSI1 already exists; this adds write units on `PAT#`/`APPT#` writes only, inside the existing DynamoDB line.
**Context.** `docs/adr/0002-database.md` proved this task's own access pattern before either GSI1 or this entity existed: "`gsi1pk = CLI#<clinicianId>` AND `gsi1sk BETWEEN 'APPT#<start>' AND 'APPT#<end>'` … TASK 3.4.x, not built here — key shape checked now, while the index is still cheap to shape." GSI1 already carries the clinician→patients projection (2.5.1) on the identical partition key with a `PAT#` sort-key prefix; this task adds the `APPT#` prefix the ADR reserved, on the same partition, which is why the ADR calls the two patterns "never collide … even though they share a partition."
**Files:** `packages/shared-types/src/appointment.ts` (new), `services/api/src/appointment-repository.ts` (new), `services/api/src/appointment.ts` (new), `services/api/src/appointment-handler.ts` (new), `services/api/src/dynamo-store.ts`, `infra/src/data-stack.ts`, `docs/runbooks/appointments.md` (new).
**Steps.** 1. `Appointment { patientId, clinicianId, scheduledAt: string /* iso-utc */, durationMinutes: number, status: 'scheduled' | 'completed' | 'cancelled' | 'no-show' }` — no video/call field yet; Phase 4 adds one additively when it lands, and this entity is deliberately shaped so that addition needs no migration. Main-table key `PAT#<id>` / `APPT#<iso-utc>`, per the data model. 2. `POST /patients/{id}/appointments` — `can(principal, 'create', { entityType: 'appointment', assignedClinicianId })`, so only an assigned sub-clinician or the principal schedules one; the matrix denies the patient `create` outright (bare `R`), so there is no self-service booking flow in this task — a patient requesting a time is out of scope here and not assumed. 3. On write, derive `gsi1pk = CLI#<clinicianId>`, `gsi1sk = APPT#<scheduledAt>` on the same attributes GSI1 already reads, the identical "derived inside the store, never a separate input" discipline `DynamoAssignmentStore.writeDecision` established for the `PAT#` pattern — the field and the index cannot disagree by construction. 4. `GET /clinicians/me/calendar?from=&to=` — the clinician-calendar read the ADR proved: `Query` on GSI1 with `gsi1sk BETWEEN`, never a `Scan`, scoped to the caller's own `clinicianId` (a sub-clinician cannot pass another's). `GET /patients/{id}/appointments` — the patient's own upcoming/past list, main-table `Query` on `PAT#<id>` with the `APPT#` prefix. 5. Every read goes through `projectFor`; appointments carry no `private{}` today, same no-op-now / closed-door-later reasoning as every prior entity. 6. A read-only "next appointment" panel on the patient account page. 7. Log group `/ndn/appointment-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:** `POST /patients/{id}/appointments`, `GET /clinicians/me/calendar?from=&to=`, `GET /patients/{id}/appointments` — real authorizer throughout.
**Tests.** Unit: a scheduled appointment appears in the assigned clinician's calendar query, ordered chronologically by `gsi1sk`, and in the patient's own list. Negative: a patient's `POST` is `403`. Negative: an unassigned sub-clinician cannot schedule for a patient they are not assigned to, and cannot read another clinician's calendar. Integration: the calendar query is asserted as `Query`, never `Scan`, against a mocked SDK client — the same assertion shape 2.5.1/2.5.3's own GSI1/GSI3 store tests already use. Integration (synth): the new IAM statement on this function's role names `dynamodb:Query` on `[table, table/index/GSI1]` and nothing wider.
**Verification:** `aws dynamodb query` against GSI1 with a clinician's key and a date range returns exactly their own scheduled appointments in chronological order; no `Scan` permission exists on the function's role.
**Flag:** `appointments.enabled` — default off. **DoD:** an assigned clinician can schedule an appointment; a clinician's calendar and a patient's own list both resolve to a real `Query`; nobody unassigned reaches either. **Rollback:** flip the flag off. **Do NOT:** let a patient create an appointment; add a video/call field before Phase 4 needs one; let the calendar query accept a `clinicianId` parameter a caller could point at someone else.

### TASK 3.4.2 — Reschedule and cancel, never delete

**Milestone:** M3.4 · **Requirements:** §5 · **Depends on:** 3.4.1 · **Size:** S · **Cost:** £0.00
**Context.** Every entity in this codebase transitions state rather than disappearing; an appointment is no exception, and the four-state `status` field 3.4.1 already defined exists specifically so "cancelled" and "rescheduled" are facts about a row that stays readable, not a row that stops existing.
**Files:** `services/api/src/appointment.ts`, `services/api/src/appointment-handler.ts`.
**Steps.** 1. `POST /patients/{id}/appointments/{apptId}/cancel` transitions `status` to `'cancelled'`; the row, its `gsi1pk`/`gsi1sk` and its history stay exactly as they were — nothing is removed from GSI1, so a cancelled appointment simply stops mattering to a clinician scanning their live calendar without ever leaving a stale-vs-current ambiguity for a reader to filter (the calendar read (3.4.1) filters `status !== 'cancelled'` in the query's own follow-up, the same "index gives candidates, the read confirms them" discipline `CaseloadRepository` already uses for its own stale-row case). 2. Rescheduling is **not** an edit to `scheduledAt` on the existing row — it is `cancel` the old one and `POST` a new one, so the append-only property `04-data-model-rbac.md` asks of every entity in this table holds here without a special case, and a patient's own appointment history shows the real sequence of what was booked and when it changed, not a silently-overwritten time. 3. `can(principal, 'update', …)` gates `cancel` the same way it gates the original `create` — assigned clinician or principal only; a patient cannot cancel their own appointment through this route (out of scope here, same reasoning 3.4.1 gives for booking).
**Interfaces:** `POST /patients/{id}/appointments/{apptId}/cancel`.
**Tests.** Unit: cancelling transitions status without touching `gsi1pk`/`gsi1sk` or any other field. Unit: a cancelled appointment is excluded from the clinician calendar read but still returned by the patient's full history read — two different "which appointments" questions, answered correctly by each. Negative: a patient's cancel attempt is `403`. Negative: no code path calls anything delete-shaped — the repo-wide `no-destructive-primitives` lint rule already prevents the primitive; this test proves the higher-level property that this file's own logic never tries.
**Verification:** a cancelled appointment remains `GetItem`-able and shows `status: 'cancelled'`; the clinician's calendar query no longer surfaces it.
**Flag:** `appointments.enabled` — reused from 3.4.1. **DoD:** an assigned clinician can cancel an appointment; the row survives; a rescheduled appointment is two rows telling the true history, not one row with its time silently changed. **Rollback:** flip the flag off. **Do NOT:** add an `UpdateItem` path that changes `scheduledAt` on an existing row; let a patient cancel their own appointment through this route.

### TASK 3.4.3 — The 1-hour reminder, GSI4, and the first real SMS send

**Milestone:** M3.4 · **Requirements:** C-02, NFR-09, R-01, R-02 · **Decisions:** D-10, D-11 · **Risks:** **R-01, R-02** · **Depends on:** 3.4.1, 2.3.1, 2.3.2 · **Size:** M · **Cost:** £0.00 this month at current volume — modelled at $2.00 (M6)/$3.63 (M12) in `03-cost-model.md`, hard-capped at £5 = $6.05; this task is the one that makes that spend real rather than theoretical.
**Context.** `02-risk-register.md`'s R-01 names the arithmetic directly: "§5 asks for ~150 SMS/month; C-02's £5 buys ~108" at scale, mitigated by "email-primary; SMS reserved for the 1-hour reminder … never silently drop a reminder — built as a property of the notification abstraction at 2.3.1." TASK 2.3.2 built and proved a real SMS send against a real provider but noted its own cost line as "£0.00 this month — no reminder exists to send until 3.4.x." This is that task, and it is the first time this platform sends an SMS anyone will actually receive. `04-data-model-rbac.md` reserves **GSI4** for exactly this — "appointment-window lookups for reminders" — named but never built, the same "prove before building" discipline ADR-0002 already applied to GSI1/GSI3.
**Files:** `docs/adr/0002-database.md`, `infra/src/data-stack.ts`, `services/api/src/appointment-repository.ts`, `services/api/src/reminder-sweep.ts` (new), `services/api/src/reminder-sweep-handler.ts` (new), `docs/runbooks/appointment-reminders.md` (new).
**Steps.** 1. **Prove GSI4 against the key schema before writing any code**, per ADR-0002's own standing rule for every index in this table. The access pattern this sweep needs: "every scheduled, not-yet-reminded appointment starting in roughly the next hour, across every clinician and patient" — a query with no natural per-clinician or per-patient partition, unlike GSI1/GSI3's fixed-actor patterns. Proved shape: `gsi4pk = 'APPT#REMINDER'` (one fixed partition, the same "GSI3's own `CASELOAD#all`" precedent 2.5.3 already established for a partition with no natural per-entity key), `gsi4sk = <iso-utc>#<patientId>` (chronological, unique), `KEYS_ONLY`, sparse — set only while `status === 'scheduled'` and `scheduledAt` is in the future; **never removed by the sweep itself, so idempotent replay never re-derives a false "not reminded" state from a missing attribute.** 2. Reminder-sent tracking is a plain attribute, `reminder_sent_at`, set once the SMS/email guard chain (2.3.1/2.3.2) returns; the sweep's `Query` narrows by `gsi4sk` to the next hour's window and a `FilterExpression` (`attribute_not_exists(reminder_sent_at)`) excludes already-reminded rows within that already-narrow result set — a filter after a `Query`'s key condition, never a `Scan`, the same distinction R-09's own audit-partition guardrail relies on elsewhere in this codebase. 3. An EventBridge scheduled rule (`rate(15 minutes)`, comfortably inside the 1-hour window with margin for a missed tick) invokes `reminder-sweep-handler.ts`. 4. Every send goes through 2.3.1's `Notifier`/2.3.2's real `SmsSender` — `appointmentReminder1Hour` is already `smsEligible` in `packages/i18n/src/notifications/index.ts` since TASK 2.3.1, unused until now. **R-01's own property, restated for this task specifically:** every guard failure (flag off, rate-limited, capped, provider error) still writes a `degraded` delivery record naming the reason and still marks `reminder_sent_at` — a reminder that could not be sent is a recorded fact, not a silent retry storm on the next sweep tick. 5. Email is the fallback the same delivery record already expresses (2.3.1's own "degrades to email exactly the same way a provider outage does") — this task adds no separate email-only path; the Notifier's existing behaviour already covers it. 6. Log group `/ndn/reminder-sweep-function` → `UNMONITORED_LOG_GROUP_NAMES` (a scheduled function, not a per-request one; volume is bounded by appointment count, not traffic).
**Interfaces:** No HTTP route — an EventBridge-invoked Lambda, `services/api/src/reminder-sweep-handler.ts`, matching `webshop-checkout`'s pattern of an event source that is not `HttpApi`.
**Tests.** Unit: an appointment scheduled for 55 minutes from now is included in the sweep's query window; one scheduled for 3 hours from now is not. Unit: a swept appointment gets a `reminder_sent_at` timestamp and is excluded from the next sweep's `FilterExpression` result, proving idempotent replay — running the sweep twice against the same window sends exactly one SMS, not two. Unit: every one of R-02's existing SMS guards (flag off, kill switch, non-UK, rate-limited, capped) still applies to a reminder exactly as 2.3.2 proved for a synthetic send, and each failure still writes a `degraded` record and still stamps `reminder_sent_at` — the property that failing to send is never confused with never having tried. Integration: the sweep's own `Query` is asserted against a mocked SDK client as `Query` on GSI4, never `Scan`. Integration (deployed, provider sandbox): a real appointment inside the window produces a real SMS to a UK test number, exactly once. Negative: a cancelled appointment (3.4.2) inside the window is never reminded — the sweep's own query or filter excludes non-`'scheduled'` status.
**Verification:** `aws dynamodb describe-table` shows GSI4 with the proved key schema and `KEYS_ONLY` projection; the EventBridge rule's target and schedule are visible in the console; a manually-scheduled test appointment inside the window produces exactly one real SMS and one delivery record.
**Flag:** `appointments.reminders.enabled` — default off, independent of `appointments.enabled` (D-11's own "SMS only behind authentication" plus this task's own added carefulness: scheduling and reminding are two capabilities, and turning one on should never silently turn on real spend). **DoD:** every scheduled appointment inside its 1-hour window receives exactly one reminder attempt, recorded whether it succeeds, degrades or is capped; no code path can send two reminders for one appointment; GSI4 is proved and documented before being built. **Rollback:** flip `appointments.reminders.enabled` off — the EventBridge rule keeps firing but the guard chain's flag check stops every send with no deploy; GSI4 stays, same as every other index in this codebase. **Do NOT:** create GSI4 without proving the access pattern in the ADR first; let the sweep re-derive "already reminded" from anything but `reminder_sent_at`; let a sweep failure retry-storm by never stamping the attempt; raise the SMS cap to make a reminder go through.

**Deleted, D-32 (2026-08-30).** The owner's own words: "remove this reminder thing, the clinician handling whatsapp will send a reminder manually." Every artefact this task built — GSI4, `reminder-sweep.ts`, `reminder-sweep-handler.ts`, `Appointment.reminder_sent_at`, `ReminderSweepFunction` and its EventBridge rule, `appointments.reminders.enabled` — is removed from the codebase, not merely flagged off. This sweep was the only caller that ever made `appointmentReminder1Hour`'s `smsEligible: true` reachable, so `sms.ts`/`sms-provider.ts`/the SMS rate limiter and spend cap (TASK 0.5.3) are now unreachable by the same removal, without themselves being touched — the same "dark, not deleted" posture D-31 already holds for Stripe. LL-02 (UK SMS onboarding) is closed as moot alongside it. Full reasoning: [01-decisions.md](01-decisions.md)'s D-32, [docs/runbooks/appointment-reminders.md](../runbooks/appointment-reminders.md).

### TASK 3.5.1 — Content assignment: a clinician assigns existing content to a patient

**Milestone:** M3.5 · **Requirements:** §5, FR-PP-10 (the keyword index this reuses) · **Depends on:** 3.1.1, 1.3.1 · **Size:** S · **Cost:** £0.00 — no new entity storage beyond a thin assignment row; `ContentRepository`, GSI2 and every content Lambda already exist from Phase 1.
**Context.** `04-data-model-rbac.md`'s key shape for this entity is deliberately minimal: `PAT#<id>` / `CONTENT#<id>`, no payload beyond the link itself — the content *item* (title, body, per-language translations, keywords) already lives in full at `CONTENT#<id>` / `META`, built and public-readable since TASK 1.3.1. This task is the assignment relationship alone: which already-published content a clinician has pointed a specific patient at, most of the milestone's own name ("content + media") already built in Phase 1 and reused rather than re-specified here.
**Files:** `packages/shared-types/src/content-assignment.ts` (new), `services/api/src/content-assignment-repository.ts` (new), `services/api/src/content-assignment.ts` (new), `services/api/src/content-assignment-handler.ts` (new), `infra/src/data-stack.ts`, `docs/runbooks/content-assignment.md` (new).
**Steps.** 1. `ContentAssignment { patientId, contentId, assignedAt: string }` — a thin `Repository<ContentAssignment>` (0.3.3's generic base, not `VersionedRepository` — an assignment is not versioned, it either exists or it does not, and "remove an assignment" is out of scope for this task, matching the matrix's own silence on a `D`elete-shaped action for this row). 2. `POST /patients/{id}/content` — body names a `contentId`; the handler confirms the content item exists and is `published` (`ContentRepository.findById`, reused, not reimplemented) before writing the assignment — an assignment pointing at a draft or a nonexistent item is refused, not silently created. `can(principal, 'create', { entityType: 'content-assignment', assignedClinicianId })` — assigned sub-clinician or principal only, per the matrix's `C R U` for both, bare `R` for the patient. 3. `GET /patients/{id}/content` — the patient's own assigned-content list, each entry hydrated from `ContentRepository.findById` (title/excerpt per the caller's locale) rather than returning bare ids the frontend would need a second round trip to resolve. 4. Log group `/ndn/content-assignment-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:** `POST /patients/{id}/content`, `GET /patients/{id}/content`.
**Tests.** Unit: assigning a published content item succeeds; assigning a draft or a nonexistent id is refused with a named error, not a silent no-op. Unit: a patient's read returns hydrated title/excerpt, not bare ids. Negative: a patient's `POST` is `403`. Negative: an unassigned sub-clinician's `POST` is `403`.
**Verification:** a clinician assigns a real published post; the patient's deployed token immediately lists it with a real title.
**Flag:** `contentAssignment.enabled` — default off. **DoD:** a clinician can assign published content to an assigned patient; the patient can read their own assigned list, hydrated; nobody else can assign or read another patient's list. **Rollback:** flip the flag off. **Do NOT:** allow assigning unpublished content; return bare content ids from the patient-facing read; let this task re-specify anything Phase 1's `ContentRepository` already owns.

### TASK 3.5.2 — The patient's "my content" page

**Milestone:** M3.5 · **Requirements:** §5 · **Depends on:** 3.5.1 · **Size:** S · **Cost:** £0.00
**Context.** The read side of 3.5.1 exposed via the account shell, the same "backend task ships its own page" shape 2.5.3's caseload view and 3.1.1's profile page already established, rather than a separate frontend-only task with nothing new to build against.
**Files:** `apps/web/src/account/AssignedContent.tsx` (new), `apps/web/src/pages/[locale]/account/content.astro` (new).
**Steps.** 1. A React island, same `RequireAuth`-gated, statically-empty-page discipline as every other authenticated page in this codebase. 2. Renders the patient's assigned content as a list linking into the existing public blog pages (1.3.2) — no new content-rendering surface, reusing the article page a signed-out visitor already sees. 3. Loading/empty/error states as semantic HTML with `role="status"`/`role="alert"`, matching `CaseloadView.tsx`'s own established shape rather than inventing a new one.
**Interfaces:** none new — consumes 3.5.1's `GET /patients/{id}/content`.
**Tests.** Component-level tests are not introduced here either, for the same reason 2.5.3's runbook gives: no React Testing Library/jsdom pattern exists anywhere in `apps/web` yet, and inventing one for a single page is out of this task's scope. Coverage is the backend handler tests (3.5.1) plus construction-time accessibility (semantic HTML, ARIA roles), documented honestly rather than claimed as more than it is. pr-env: axe-clean and keyboard-complete by construction — same honestly-scoped gap as every prior authenticated page.
**Verification:** manual sign-in as a test patient with assigned content shows the list; empty state renders for a patient with none.
**Flag:** `contentAssignment.enabled` — reused from 3.5.1. **DoD:** a signed-in patient sees their assigned content, linking to the real article. **Rollback:** flip the flag off. **Do NOT:** build a second content-rendering surface distinct from the public blog page.

### TASK 3.6.1 — Messages: patient↔clinician, rate-limited, the matrix corrected

**Milestone:** M3.6 · **Requirements:** §5 · **Decisions:** D-07 · **Depends on:** 3.1.1, 0.5.x (`rate-limiter.ts`) · **Size:** M · **Cost:** £0.00 — `PAT#<id>` / `MSG#<ts>` rows are a few hundred bytes each, inside the existing DynamoDB line.
**Context.** This task opens by correcting `04-data-model-rbac.md`'s Messages row, per this elaboration's own preamble: the assigned sub-clinician's cell moves from `R (own patients)` to `C R (own patients)`, so the entity actually matches its own key-shape description, "Patient↔clinician." The correction is made in the doc first, then transcribed into `authz-matrix.ts`, per that file's own standing rule — the identical order TASK 2.5.1 followed for "Patient assignment."
**Files:** `docs/plan/04-data-model-rbac.md`, `services/api/src/authz-matrix.ts`, `services/api/src/authz.test.ts`, `packages/shared-types/src/message.ts` (new), `services/api/src/message-repository.ts` (new), `services/api/src/message.ts` (new), `services/api/src/message-handler.ts` (new), `infra/src/data-stack.ts`, `docs/runbooks/messaging.md` (new).
**Steps.** 1. `Message { patientId, senderId, senderRole: 'patient' | 'sub-clinician' | 'principal-clinician', body: string }`, `PAT#<id>` / `MSG#<ts>`, append-only (`Repository<Message>`, 0.3.3's generic base — a message is never edited or removed once sent, the same discipline every other entity in this table follows, and the one a messaging feature specifically must not compromise on for a clinical record). 2. `POST /patients/{id}/messages` — `can(principal, 'create', { entityType: 'message', ownerPatientId, assignedClinicianId })`, now genuinely bidirectional per the corrected row: the owning patient or an assigned sub-clinician or the principal. 3. Rate-limited per principal with `rate-limiter.ts`'s existing `FixedWindowRateLimiter` (reused directly, the same generic limiter SMS and the contact form already share) — a message thread is a lower-stakes, higher-frequency channel than SMS, so the window/limit constants are named and justified in the runbook rather than copied from SMS's own tight figures unexamined. 4. `GET /patients/{id}/messages?cursor=` — the thread, chronological, paginated; `can(principal, 'read', …)` per the same corrected row. 5. A new message triggers a `Notifier` send (2.3.1) to the *other* party — content-free, "you have a new message," never the message body, the same privacy posture every other notification template in this codebase states for a mailbox that may not be the recipient's alone to read. 6. Every message reaches `projectFor`; messages carry no `private{}` half — same no-op-now / closed-door-later reasoning as every entity before it. 7. Log group `/ndn/message-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:** `POST /patients/{id}/messages`, `GET /patients/{id}/messages?cursor=`.
**Tests.** `authz.test.ts`'s exhaustive suite is regenerated against the corrected `DOC_TABLE` row — the existing generator, not a bespoke test, so the correction is proven the same exhaustive way every other cell already is. Unit: a patient can send and read their own thread; an assigned sub-clinician can send and read the same thread; both see the same messages, in order. Negative: a patient reading another patient's thread (guessed id) is `403`. Negative: an unassigned sub-clinician is `403` on both routes. Negative: a principal or sub-clinician's send is rejected past the rate limit, exactly as SMS's own limiter proves for its own guard, reusing the identical test shape. Unit: the notification fires to the other party and carries no message content.
**Verification:** a patient and their assigned clinician's deployed tokens can round-trip a message each way; the recorded rate limit refuses a burst past its own threshold.
**Flag:** `messaging.enabled` — default off. **DoD:** a patient and their assigned clinician can message each other in one thread; nobody else reaches it; no message can be edited or removed once sent; the corrected matrix row is proven by the same exhaustive suite as every other row. **Rollback:** flip the flag off. **Do NOT:** let a message be edited or removed; let the notification carry any message content; leave the matrix correction untranscribed in `authz-matrix.ts` while citing it in code.

**Amended, D-32 (2026-08-30).** Step 5's own "new message" notice is deleted: "any notification will go via whatsapp." `MessageFunction`'s `cognito-idp:AdminGetUser`/`ses:SendEmail` grants are removed with it; sending and reading a message are unchanged.

### TASK 3.6.2 — The message thread, on both sides of the account shell

**Milestone:** M3.6 · **Requirements:** §5 · **Depends on:** 3.6.1 · **Size:** S · **Cost:** £0.00
**Context.** The read/compose surface for 3.6.1, the last page this phase adds to the account shell 2.2.4 built with nothing behind it and Phase 3 has spent six milestones filling in.
**Files:** `apps/web/src/account/MessageThread.tsx` (new), `apps/web/src/pages/[locale]/account/messages.astro` (new).
**Steps.** 1. A React island: the thread as a list, oldest-first, a compose box, `RequireAuth`-gated, statically-empty-page discipline unchanged. 2. No real-time transport (no WebSocket, no polling loop) — a new message appears on next page load or an explicit refresh, the same "static page, island decides what to fetch" posture every other Phase 2/3 page takes; real-time is a Phase 4+-adjacent decision this task does not make on its own. 3. Loading/empty/error states as semantic HTML, matching `CaseloadView.tsx`/`AssignedContent.tsx`'s established shape.
**Interfaces:** none new — consumes 3.6.1's two routes.
**Tests.** Same honestly-scoped gap as every prior page this phase added: no component-test infrastructure invented, coverage is the backend handler tests (3.6.1) plus construction-time accessibility. pr-env: axe-clean and keyboard-complete by construction, including the compose box's own keyboard reachability — a text-entry control is a more direct a11y risk than a read-only list, and gets its own explicit check rather than inheriting the page's general pass.
**Verification:** manual sign-in as a test patient and their assigned clinician shows the same thread from both sides, in the same order.
**Flag:** `messaging.enabled` — reused from 3.6.1. **DoD:** both sides of an assigned relationship can read and compose in one thread from the account shell. **Rollback:** flip the flag off. **Do NOT:** add a real-time transport as a side effect of building the page.

**Gate G3:** no private field reachable by a patient in any code path — re-audited from scratch across every entity this phase added, not only the ones the phase's own tests already cover. Gate-specific addition, per `06-gate-checklists.md`. This phase's own contribution to that audit: R-09's chokepoint (2.1.2) now has five real callers instead of zero (diagnosis, care plan, assessment-visible, assessment-private, and every entity's echo-on-write path), and the two-row assessment split (3.3.1/3.3.2) is the first proof that a `private{}` field genuinely never reaches a patient response rather than merely being denied a matrix cell in the abstract.

## Phase 4 — Video

*(Elaborated to full §12 detail at Gate G3, per D-27 — the same policy that elaborated Phase 2 at Gate G1 and Phase 3 at Gate G2. Phases 5–7 remain stubs below, elaborated at their own gates.)*

**The same citation limitation, named again.** Phase 3's elaboration inherited Phase 2's note that the full FR/NFR matrix was never committed to this repo and that reconstructing it remains an open action item (`07-traceability.md`, `gate-g1-report.md`). It is still open at this gate. `FR-VID-04` is the one video-related ID actually pinned to anything in the tree — `02-risk-register.md`'s R-03 cites it directly, against exactly the TURN-cost concern M4.4 below closes. No other `FR-VID-0x` number is invented for signalling, call authorisation, the peer connection or the join button, because nothing in this repo has ever pinned one; those tasks cite `D-12` (the video-transport decision) and `R-03` where their subject matches directly, the same discipline Phase 3 held to for its own uncited entities.

**Two numbers this elaboration owes an explanation for.** `02-risk-register.md`'s R-03 row was written before any Phase 4 task existed and already names `4.4.1, 4.4.2` as the tasks that mitigate it — a citation this elaboration cannot renumber without breaking a cross-reference in a file Phase 4 does not otherwise touch, so M4.4 below is built to land on exactly those two numbers. Second: the stub table this section replaces estimated "~12" tasks; elaboration settled on **nine**. The gap is not padding removed — it is what the dependency-ordering check `09-self-audit.md` runs at every gate ("each task's `Depends on` names only lower-numbered tasks") forced once actually worked through: a call cannot be relayed before it is joined, and joining is what server-side call authorisation *is*, so the relay handler moves into M4.2 as its own task rather than sitting in M4.1 ahead of the authorisation step it depends on. Nine tasks across five milestones, each still ≤M per `00-conventions.md`'s own size cap, is the shape that survived that check.

**What Phase 4 inherits, built and unused.** `docs/adr/0007-signalling.md` decided "API Gateway WebSocket + DynamoDB connection table" before any of it existed, rejecting a self-hosted socket server on standing cost. `docs/adr/0006-video-transport.md` and `D-12` decided "WebRTC P2P first, Cloudflare TURN fallback (1 TB/mo free), concurrency-capped" — verified worst case ≈80–90 GB/mo, self-hosted coturn (~£6/mo) rejected on cost. `03-cost-model.md` has carried a `$0` Cloudflare TURN line since Gate G0 on the strength of that same worst-case figure. TASK 3.4.1 shaped `Appointment` deliberately with "no video/call field yet … this addition needs no migration," and its own calendar/list reads are what M4.2's authorisation check queries against. None of the four had a caller until now.

**Price re-verified before this gate elaborates the phase that first spends against it.** `09-self-audit.md`'s `UNVERIFIED` list named "Cloudflare TURN free-tier period," deferred to "the gate that precedes its first real use (TURN provider selection before Phase 4)" — this gate. Re-verified live 2026-08-26 against `developers.cloudflare.com/realtime/sfu/pricing`: **1,000 GB/month free, $0.05/GB overage, unchanged** from the figure `03-cost-model.md` has modelled against since G0. Struck from the `UNVERIFIED` list in `09-self-audit.md`, the second of the six to move from deferred to resolved (after AWS SMS at TASK 2.3.2).

### TASK 4.1.1 — The WebSocket signalling channel: connect, disconnect, the connection table

**Milestone:** M4.1 · **Requirements:** D-12 · **Depends on:** 2.2.1, 2.2.2 · **Blocks:** every later Phase 4 task · **Size:** M · **Cost:** ≈$0.01–0.02/month at M6/M12 — see the new WebSocket line in `03-cost-model.md` below; no free tier assumed, per this cost model's own no-12-month-offer rule.
**Context.** ADR-0007 named this task before it existed: "API Gateway WebSocket + DynamoDB connection table," rejecting a self-hosted socket server on standing cost. It is the first WebSocket surface this codebase has built, and the first time a caller has to be authenticated with no `Authorization` header available to read: a browser's `WebSocket` constructor cannot set arbitrary headers on the handshake, so the Cognito ID token this task's authorizer needs travels as a query-string parameter on the connect URL instead — a real constraint, not a shortcut, and named here rather than discovered mid-implementation.
**Files:** `infra/src/data-stack.ts`, `infra/src/ws-authorizer.ts` (new), `services/api/src/ws-connect-handler.ts` (new), `services/api/src/ws-disconnect-handler.ts` (new), `services/api/src/connection-repository.ts` (new), `packages/shared-types/src/connection.ts` (new), `docs/adr/0007-signalling.md` (amend — record the query-string-token decision), `docs/plan/03-cost-model.md`, `docs/runbooks/video-signalling.md` (new).
**Steps.** 1. A second API Gateway resource in `data-stack.ts`: `WebSocketApi` (`aws-cdk-lib/aws-apigatewayv2`), routes $connect/$disconnect/$default (the last stubbed here only enough to deploy cleanly — message relay is 4.2.2's task, not this one). 2. The $connect route's Lambda REQUEST authorizer reads `event.queryStringParameters.token`, verifies it with the existing `TokenVerifier` from `jwt-verify.ts` unchanged — the identical two-pool check `authorizer.ts` already performs for HTTP, wrapped in a WebSocket-shaped authorizer rather than reimplemented. Access logging on this stage is explicitly disabled: the token rides in the URL, and the "never log PII" discipline `00-conventions.md` states generally extends to not letting API Gateway's own access log capture a connect URL that carries one. 3. On success, `ws-connect-handler.ts` writes `CONN#<connectionId>` / `PROFILE` — `principalId`, `role`, `connectedAt`, and a `ttl` attribute (DynamoDB native TTL, `connectedAt` + 12h). This is the *only* cleanup mechanism this task builds: no code path here or in `ws-disconnect-handler.ts` issues a `DeleteItem` against a connection row, ever. $disconnect (which AWS does not guarantee delivery of — a lost network never fires it) sets `status` to `disconnected` and stamps `disconnectedAt` on the same row rather than removing it; DynamoDB's own background TTL sweep is what actually reclaims it, the identical "the platform never issues the delete, the storage layer's own housekeeping does" reasoning `log-retention-volume-control.md` already applies to CloudWatch's log expiry, used here for the first time on a table row. 4. `connection-repository.ts` — one method, `findById`, no audit wiring: a connection row is operational metadata with no clinical content, not an entity the `AuditAction` union (`AuditWriter`, TASK 2.1.3) needs to know about — the same distinction that task's own header draws between what gets audited and what does not. 5. Log groups `/ndn/ws-connect-function`, `/ndn/ws-disconnect-function` → `UNMONITORED_LOG_GROUP_NAMES`. 6. `03-cost-model.md` gains one row: API Gateway WebSocket, modelled at ~250/~500 monthly calls × ~30 signalling messages and ~60 connection-minutes each — **$0.01 (M6) / $0.02 (M12)**, live-priced 2026-08-26 at $1.00/million messages + $0.25/million connection-minutes (`eu-west-2`, standard rate, no regional premium).
**Interfaces:** `wss://{api-id}.execute-api.eu-west-2.amazonaws.com/{stage}?token={Cognito ID token}` — no REST route.
**Tests.** Unit: a valid token's $connect writes exactly one connection row with a future `ttl` and no `private` key (`containsPrivateField`, reused — connections carry no `private{}` half, same no-op-now/closed-door-later reasoning as every prior entity). Negative: an invalid or expired token is refused before any row is written. Unit: $disconnect marks the row `disconnected` without removing it, proven by a subsequent `GetItem` still succeeding. Negative: no code path in this task's files calls `DeleteItemCommand` — the repo-wide `no-destructive-primitives` lint rule enforces it; this task adds nothing new to police and its own comment states why TTL is the sanctioned exception. Integration (synth): the table's TTL attribute is enabled and named; the WebSocket stage has access logging explicitly off, asserted rather than left to default.
**Verification:** a WebSocket client connects with a real Cognito token and completes a normal handshake; `aws dynamodb get-item` shows the connection row with a `ttl` a few hours out; disconnecting and re-querying shows `status` as `disconnected`, not a missing item.
**Flag:** `video.signalling.enabled` — default off. **DoD:** an authenticated patient or clinician can open and cleanly close a WebSocket connection; every connection is findable by id; nothing is ever explicitly deleted. **Rollback:** flip the flag off — the authorizer denies every connect attempt before any row is written, the same "flag gates the decision, not the deploy" shape every prior flag in this codebase uses. **Do NOT:** expect a bearer token in a header on the WebSocket handshake; issue a `DeleteItem` against a connection row for any reason; rely on $disconnect firing instead of TTL.

### TASK 4.2.1 — Server-side call authorisation: the join message, the appointment window, one audited decision

**Milestone:** M4.2 · **Requirements:** D-12, R-03 (the window this closes sits upstream of R-03's relay-cost concern) · **Depends on:** 4.1.1, 3.4.1, 3.4.2 · **Blocks:** 4.2.2, 4.3.1, 4.5.1 · **Size:** M · **Cost:** £0.00 — Lambda logic and audit writes inside the existing DynamoDB line, no new resource beyond one CloudWatch alarm reserved in 4.4.2's own budget.
**Context.** 4.1.1 proves who is on a socket; nothing yet has asked "is this appointment — the call's own reason to exist — the one this principal may join, right now." The `Appointments` matrix row (`R` for the owning patient, `C R U` for an assigned clinician) already governs *reading* the record; joining its call is a stricter claim this task adds as a new `can()` action on the same row, not a new resource.
**Files:** `services/api/src/authz.ts`, `services/api/src/authz-matrix.ts`, `services/api/src/authz.test.ts`, `services/api/src/ws-join-handler.ts` (new), `services/api/src/connection-repository.ts`, `docs/plan/04-data-model-rbac.md`, `docs/runbooks/video-signalling.md`.
**Steps.** 1. Add `'join-call'` to `authz.ts`'s action union; the `Appointments` row's existing cells (owning patient, assigned clinician) grant it — no new matrix row, since joining is a stricter version of reading the same resource. 2. A `{ type: 'join', appointmentId }` WebSocket message (the one 4.1.1 deliberately left unhandled) loads the appointment, runs `can(principal, 'join-call', { entityType: 'appointment', ownerPatientId, assignedClinicianId })`, then two checks the matrix cannot express: `status === 'scheduled'` against the live record (a cancelled appointment, 3.4.2, is never joinable, cache or no cache), and `now` inside a named window around `scheduledAt` — opens 10 minutes before, closes 30 minutes after, constants stated in the runbook rather than left as magic numbers, sized so a late-running clinician can still join and a stale link cannot be reused days later. 3. On success, the handler writes a second, thin row — `CALL#<appointmentId>` / `CONN#<connectionId>` (`principalId`, `role`, the same `ttl` 4.1.1's row carries) — a purpose-built partition on the main table, at most two live items per call, that 4.2.2's relay queries directly; no new GSI, the same "single table, PK/SK overloaded" discipline `D-07` states generally, applied here to a partition keyed by call rather than by patient or clinician for the first time. 4. Every join attempt — allowed or denied — writes an audit event (`AuditWriter`, 2.1.3, reused directly) naming `appointmentId`, the principal, and the decision: the first entity in this codebase whose *read-shaped* action is audited, because `AuditAction` has no `'read'` member and a call join is closer to "did this consultation happen" than to an ordinary read — a deliberate, narrow exception to `private-field-boundary.md`'s own "reads are not recorded" note, stated here rather than left as a silent contradiction of it. 5. A denied join returns a typed reason (`too-early`, `too-late`, `cancelled`, `not-your-appointment`) on the same socket, never a bare close, so 4.5.1's join-button state machine can show the caller why.
**Interfaces:** WebSocket message `{ type: 'join', appointmentId }` → `{ type: 'joined' }` or `{ type: 'join-denied', reason }`.
**Tests.** `authz.test.ts`'s exhaustive generator gains `join-call` as a fourth action alongside `create`/`read`/`update`, proven against every matrix cell the same exhaustive way every other action already is. Unit: the owning patient and the assigned clinician join a scheduled appointment inside the window. Negative: an unassigned sub-clinician, or a patient naming another patient's appointment, gets `not-your-appointment` — and an audit event still records the attempt, the one place in this codebase a denial is durably recorded rather than only logged. Negative: a cancelled appointment is `cancelled` regardless of window; 11 minutes early is `too-early`; 31 minutes late is `too-late`. Unit: a successful join's `CALL#` row carries the right `connectionId`, proven by 4.2.2's own relay fixture depending on it rather than duplicating the setup.
**Verification:** joining a real scheduled test appointment inside its window succeeds and `GET /audit?date=` shows the join; joining 40 minutes early is refused with `too-early` and also appears in the audit log as a denial.
**Flag:** `video.callAuthz.enabled` — default off, independent of `video.signalling.enabled` (the same "two capabilities, two flags" reasoning TASK 3.4.3 gave scheduling versus reminding — a socket that connects but can never join a call is a safe intermediate state to ship). **DoD:** only an appointment's own two parties can join its call, only inside its window, only while it is `scheduled`; every attempt is audited. **Rollback:** flip the flag off. **Do NOT:** let a join succeed against a cancelled or superseded appointment; skip the audit write on a denial; widen the join window without naming the new constants in the runbook.

### TASK 4.2.2 — Signalling relay: offer, answer, ICE candidates, routed to the other authorised party alone

**Milestone:** M4.2 · **Requirements:** D-12 · **Depends on:** 4.2.1 · **Blocks:** 4.3.1 · **Size:** M · **Cost:** £0.00, inside 4.1.1's WebSocket line.
**Context.** A joined call still has two parties who cannot reach each other directly — the small handshake WebRTC needs before two browsers can talk P2P. This is D-12's "P2P first" clause made real: nothing this task carries is video or audio media, only SDP/ICE messages, and it is the second half of what ADR-0007's "connection table" was always for.
**Files:** `services/api/src/ws-relay-handler.ts` (new), `services/api/src/connection-repository.ts`.
**Steps.** 1. $default route (`ws-relay-handler.ts`) receives `{ appointmentId, type: 'offer' | 'answer' | 'ice-candidate' | 'leave', payload }`. 2. The handler queries `CALL#<appointmentId>` (4.2.1's own pointer partition, at most two items), confirms the sender's own `principalId` is one of them — proving the sender actually joined, without re-running 4.2.1's own authorisation decision a second time, since two independent authorisation paths for the same decision is a way for them to drift, not a safety margin — then forwards the envelope to the *other* item's `connectionId` via API Gateway Management API's `PostToConnection`. Never a broadcast; never a lookup by anything the sender supplies about who to reach — the `CALL#` partition, written only by an authorised join, is the sole source of truth for "who is the other party." 3. If the other party has no live row, the sender gets `{ type: 'peer-unavailable' }` back on their own socket rather than a silent drop — the same "a failure is a recorded fact, not a silent retry storm" discipline TASK 3.4.3's reminder sweep already established for a different kind of send. 4. `PostToConnection` returning `GoneException` (a stale `connectionId` whose row `ttl` has not yet expired) soft-marks that row `disconnected`, the identical update 4.1.1's $disconnect handler makes — never a delete. 5. The payload itself is never logged — `type` and `appointmentId` are; an SDP payload can carry network-topology information about a caller's device, the first plausibly-sensitive shape this discipline has met since `00-conventions.md` first stated "identifiers only."
**Interfaces:** WebSocket message, $default route — no new HTTP surface.
**Tests.** Unit: an offer from the patient's connection reaches only the matched clinician's connection for that call. Negative: a message naming an `appointmentId` the sender never joined is refused before any lookup runs. Unit: `peer-unavailable` returns when the other party has not (yet) joined. Unit: a `GoneException` soft-marks the stale row and does not throw past the handler. Integration: the log line for a relayed message asserts `type` and `appointmentId` present, `payload` absent — `containsPrivateField`-shaped, applied to a log line instead of a response body for the first time.
**Verification:** two real sessions signed in as the matched patient/clinician for a real scheduled test appointment exchange a real offer/answer pair and see each other's ICE candidates arrive.
**Flag:** `video.signalling.enabled` — reused from 4.1.1; this route does nothing a joined call does not already require. **DoD:** an offer/answer/ICE message from one party of a joined call reaches only the other party's live connection; a payload never appears in a log line. **Rollback:** flip the flag off. **Do NOT:** broadcast to more than one connection; let a client name an arbitrary `connectionId` to relay to; log the SDP/ICE payload.

### TASK 4.3.1 — The peer connection: offer/answer/ICE over the relay, STUN-only first attempt

**Milestone:** M4.3 · **Requirements:** D-12 · **Depends on:** 4.2.2 · **Blocks:** 4.3.2, 4.3.3 · **Size:** M · **Cost:** £0.00 — Cloudflare's STUN service is free and unlimited.
**Context.** Every prior Phase 4 task moves signalling messages; nothing yet does anything with them. This is where `RTCPeerConnection` first appears in the codebase, on both sides of the account shell, and where D-12's "P2P first" clause becomes an actual call: STUN only, no TURN credentials requested — those are 4.4.1's to issue, reached only from 4.3.3's fallback path.
**Files:** `apps/web/src/account/VideoCall.tsx` (new — one component, parameterised by role, shared by both account-shell sides, since the peer-connection logic is identical and only the join-button copy differs, which 4.5.1 owns), `apps/web/src/account/webrtc-signalling-client.ts` (new), `apps/web/src/pages/[locale]/account/call/[appointmentId].astro` (new, both sides), `docs/runbooks/video-calls.md` (new).
**Steps.** 1. On mount: open the WebSocket with the token query param 4.1.1's authorizer expects, send `{ type: 'join', appointmentId }`, and construct `RTCPeerConnection` only after `{ type: 'joined' }` arrives — never against a call this principal was denied. 2. Offer/answer roles are fixed, not raced: the patient always offers, the clinician always answers — WebRTC glare handling for "whoever connects second" is out of scope for a first version. 3. ICE candidates trickle over the same relay as they arrive (`onicecandidate` → `type: 'ice-candidate'`), standard trickle-ICE. 4. `iceServers` is STUN-only here: `[{ urls: 'stun:stun.cloudflare.com:3478' }]` — no TURN entry until 4.4.1. 5. `getUserMedia` (video+audio) attaches to the connection and renders a local preview; the remote track renders on `ontrack`. 6. Connection-state transitions (`connecting`/`connected`/`disconnected`/`failed`) are exposed as one typed status that 4.3.3 (fallback) and 4.5.1 (join button) both consume — this task defines the type, 4.3.3 is its first real reader.
**Interfaces:** none new over the wire — consumes 4.2.2's relay envelope shape.
**Tests.** The same honestly-scoped gap every prior frontend-only task in this codebase names: no RTL/jsdom pattern exists for stateful media components, and WebRTC has no meaningful jsdom shim. Coverage is `webrtc-signalling-client.ts`'s message-parser unit tests (rejects any envelope not matching 4.2.2's shape rather than throwing past the caller) plus manual verification, documented as exactly that rather than claimed as more.
**Verification:** two real browser sessions, signed in as the matched patient/clinician for a real scheduled test appointment, complete a STUN-only call and see/hear each other on a network where P2P is reachable — TURN's own case is 4.3.3/4.4.1's to verify.
**Flag:** `video.call.enabled` — default off, depends on `video.callAuthz.enabled`. **DoD:** two matched parties complete a STUN-only WebRTC call end-to-end. **Rollback:** flip the flag off. **Do NOT:** create a peer connection before a `joined` response; let both sides race to offer; request TURN credentials from this task's own code (4.4.1 owns issuance; 4.3.3 owns when to ask).

### TASK 4.3.2 — Device check: camera and microphone permission, preview, before either party joins

**Milestone:** M4.3 · **Requirements:** FR-X-02 (WCAG 2.2 AA, the primitive this reuses) · **Depends on:** 4.3.1 · **Size:** S · **Cost:** £0.00
**Context.** `getUserMedia` in 4.3.1 is called directly against the connection; this task moves it earlier, behind a dedicated permission/preview step, so a denied-permission caller gets a legible, accessible state rather than a call that silently never connects — the same "a failure is a recorded fact, not a silent hang" discipline this phase has followed since 4.2.2's `peer-unavailable`.
**Files:** `apps/web/src/account/DeviceCheck.tsx` (new), `apps/web/src/account/VideoCall.tsx`.
**Steps.** 1. Before 4.5.1's join button is reachable, `DeviceCheck.tsx` requests camera+microphone permission and renders a local preview once granted — semantic HTML, WCAG 2.2 AA target sizes and focus states per TASK 1.1.1's own primitives. 2. A denied or unavailable device is a named, visible state (`role="alert"`, plain-language copy), not a console error the caller never sees. 3. A device selector (`navigator.mediaDevices.enumerateDevices`) for a caller with more than one camera/microphone — keyboard-reachable, the same "a text-entry-adjacent control gets its own explicit check" precedent TASK 3.6.2's compose box already set. 4. The stream this step obtains is the same stream 4.3.1's peer connection attaches — one `getUserMedia` call, not two, so granting permission once never prompts twice.
**Interfaces:** none new.
**Tests.** Same honestly-scoped frontend gap as 4.3.1 — construction-time accessibility is the coverage. Unit: the denied-permission branch renders the alert copy and does not advance to a join-enabled state.
**Verification:** a real browser with camera/mic permission denied shows the accessible error, not a hang; granting permission shows a working local preview; a machine with two cameras shows a working selector.
**Flag:** `video.call.enabled` — reused from 4.3.1. **DoD:** a caller sees and can act on their own device state before ever attempting to join; a denied permission is a legible, accessible state. **Rollback:** flip the flag off. **Do NOT:** call `getUserMedia` a second time for one session; let a permission denial fail silently past a caller who never opens devtools.

### TASK 4.3.3 — Fallback: ICE failure detection, one retry, a clear terminal state — no TURN yet

**Milestone:** M4.3 · **Requirements:** D-12 · **Depends on:** 4.3.1 · **Blocks:** 4.4.1, 4.5.1 · **Size:** S · **Cost:** £0.00
**Context.** D-12 states its own order deliberately — "P2P first, Cloudflare TURN fallback" — and this task builds the *first* half of "fallback" on its own: what a call does when STUN-only ICE fails, before TURN exists to try. TURN is the enhancement 4.4.1 wires into this state machine afterward, not a dependency this task can carry forward, since a task may only depend on a lower-numbered one (`09-self-audit.md`'s own ordering check) and 4.4.1 is not yet built when this one is.
**Files:** `apps/web/src/account/VideoCall.tsx`, `apps/web/src/account/call-state-machine.ts` (new).
**Steps.** 1. `call-state-machine.ts` — a small typed state machine consuming 4.3.1's connection-state transitions: `connecting → connected` is the happy path; `failed`, or `disconnected` held for more than a short grace period, transitions to `reconnecting`. 2. `reconnecting` triggers exactly one automatic renegotiation (a fresh offer/answer cycle over the same joined call — the relay and the `CALL#` row are untouched, only the peer connection is rebuilt). 3. If the retry also fails, the state machine reaches a terminal `call-failed` state with plain-language copy ("this call could not connect — check your network and try again") — never a silent spinner, never an infinite retry loop. 4. The state type is exported for 4.4.1 to extend (TURN becomes a second retry tier ahead of the terminal state) and for 4.5.1 to render.
**Interfaces:** none new.
**Tests.** Unit: `failed` transitions to exactly one `reconnecting` attempt, never a loop. Unit: a second failure reaches `call-failed`, not another retry. Unit: `connected` after a successful retry clears the terminal path.
**Verification:** forcing an ICE failure (e.g., blocking UDP in a test network) shows one retry attempt, then the accessible terminal error — never a hang.
**Flag:** `video.call.enabled` — reused. **DoD:** an ICE failure gets exactly one automatic retry, then a legible terminal state; nothing hangs silently. **Rollback:** flip the flag off. **Do NOT:** retry more than once without TURN in the loop (4.4.1's own job); let `call-failed` render as a blank screen or an unstyled error.

### TASK 4.4.1 — TURN credential issuance, wired into the fallback as a second retry tier

**Milestone:** M4.4 · **Requirements:** D-12, R-03, FR-VID-04 · **Depends on:** 4.2.1, 4.3.3 · **Blocks:** 4.4.2, 4.5.1 · **Size:** M · **Cost:** £0.00 — within Cloudflare's re-verified 1,000 GB/month free tier (`03-cost-model.md`, ≈85 GB modelled worst case).
**Context.** R-03 names the risk directly: "TURN relay cost blow-out (FR-VID-04)," mitigated by "Cloudflare free tier covers ~11× worst case; concurrent-relay cap; egress telemetry + alarm; kill switch degrades to audio-only" — and cites this task and 4.4.2 by number for that mitigation, written before either existed. D-12's "P2P first, Cloudflare TURN fallback" is the order this task completes: 4.3.3 built the generic retry/terminal-state machine with no TURN in it; this task is what TURN adds to that machine, not a parallel path.
**Files:** `services/api/src/turn-credentials.ts` (new), `services/api/src/turn-credentials-handler.ts` (new), `apps/web/src/account/VideoCall.tsx`, `apps/web/src/account/call-state-machine.ts`, `docs/runbooks/video-calls.md`.
**Steps.** 1. `POST /calls/{appointmentId}/turn-credentials` — gated by the exact same `join-call` check 4.2.1 already built (a principal who could not join this call cannot mint relay credentials for it either; the check is called, not re-implemented), plus confirmation the caller's `CALL#` row from 4.2.1 still exists and is unexpired. 2. On success, calls Cloudflare's TURN credential-generation API (short-lived, scoped, per-call — never a static secret shipped to every client) and returns the `iceServers` entry the browser adds to its config; the credential itself is never logged, the same "identifiers only" discipline 4.2.2 already applies to a signalling payload. 3. `call-state-machine.ts`'s `reconnecting` state (4.3.3) now requests TURN credentials before its retry attempt and adds them to `iceServers` for that attempt only — the terminal `call-failed` state 4.3.3 already built is unchanged; TURN gives the retry a better chance of succeeding, it does not remove the case where nothing succeeds. 4. Log group `/ndn/turn-credentials-function` → `UNMONITORED_LOG_GROUP_NAMES`.
**Interfaces:** `POST /calls/{appointmentId}/turn-credentials` — real authorizer, `join-call`-equivalent check.
**Tests.** Unit: a principal who never joined this call is refused credentials. Unit: credentials are requested only on the `reconnecting` transition, never on first connect (STUN-only stays the first attempt, per 4.3.1's own DoD). Negative: an expired `CALL#` row (TTL passed) is refused even for a principal who joined earlier. Integration: the credential value never appears in a log line, asserted the same way 4.2.2's payload-absence test already is.
**Verification:** forcing STUN failure on a real joined call causes exactly one retry that succeeds via TURN, visible in Cloudflare's own dashboard as a relayed session; a non-party's credential request is refused.
**Flag:** `video.turn.enabled` — default off, independent of `video.call.enabled` (a call that works P2P and simply fails without a relay option is a safe, cheaper intermediate state to ship and prove before spending anything against the free tier). **DoD:** only a call's own already-joined party can obtain TURN credentials for it, scoped to that call; a STUN failure's retry succeeds over relay when the network requires one. **Rollback:** flip the flag off — the retry attempt then has no TURN entry and falls straight to 4.3.3's terminal state, never a hang. **Do NOT:** issue a long-lived or unscoped credential; skip the `join-call`-equivalent check; request TURN on the first connection attempt.

### TASK 4.4.2 — Concurrent-relay cap, egress telemetry and alarm — the cap itself is the kill switch

**Milestone:** M4.4 · **Requirements:** R-03, FR-VID-04 · **Depends on:** 4.4.1 · **Blocks:** 4.5.1 · **Size:** S · **Cost:** ~£0.10/month — one more CloudWatch alarm inside D-18's own ~8-alarm budget (5 in use in production today; this is the 6th).
**Context.** R-03's mitigation list is three items — "concurrent-relay cap; egress telemetry + alarm; kill switch degrades to audio-only" — and this task builds them as one mechanism rather than three: the cap, enforced at credential-issuance time, *is* the kill switch, because 4.3.3's fallback state machine already treats "no TURN available" as a legitimate path to its own terminal or STUN-only-continues state, not a crash. Nothing new is invented for "degrades to audio-only" beyond what 4.3.3/4.4.1 already built; this task is what makes the cap real and the spend visible.
**Files:** `services/api/src/turn-credentials.ts`, `infra/src/budget-stack.ts`, `docs/runbooks/video-calls.md`.
**Steps.** 1. A concurrent-relay counter — the count of unexpired `CALL#` rows whose principal currently holds active TURN credentials, read via the same `CALL#<appointmentId>` query 4.2.2 already uses, filtered to a `turnActive` flag 4.4.1's issuance step sets. 2. `turn-credentials.ts` refuses a new credential once the counter reaches a named cap (a small constant, stated and justified in the runbook against the modelled worst case, not tuned to a guess) — a caller refused this way falls straight into 4.3.3's existing terminal/STUN-only path, exactly the "kill switch degrades to audio-only" R-03 asks for, built from pieces that already exist rather than a fourth new code path. 3. Each TURN-assisted call's client reports its own estimated relay duration at `type: 'leave'` (4.2.2's existing message, extended with an optional `turnDurationSeconds` field) — the server multiplies by a named, documented assumed bitrate (this task's own honestly-stated estimate, since Cloudflare's real usage figure is not available inside AWS) and emits a CloudWatch custom metric, `EstimatedTurnRelayGB`. 4. One new alarm (`infra/src/budget-stack.ts`, the 6th of D-18's ~8) fires on that metric crossing a fraction of the re-verified 1,000 GB free tier, notifying the same SNS topic the existing budget alarms already use — an early-warning companion to the hard cap, not a second enforcement mechanism.
**Interfaces:** none new over the wire — `type: 'leave'` gains one optional field.
**Tests.** Unit: a credential request at the cap is refused; one below it succeeds. Unit: the estimated-GB calculation is a pure function of duration and the named bitrate constant, unit-tested in isolation from the metric emission. Integration (synth): the new alarm's threshold and SNS target are asserted, and the total alarm count is asserted against D-18's own budget rather than left to grow unchecked, the same accounting `budget-stack.test.ts` already does for the alarms that exist today.
**Verification:** `aws cloudwatch describe-alarms` shows the new alarm; deliberately exceeding the cap in a test environment shows the next request refused and the caller landing in 4.3.3's existing STUN-only/terminal path, not an error the caller cannot make sense of.
**Flag:** `video.turn.enabled` — reused from 4.4.1; the cap applies whenever TURN issuance is possible at all. **DoD:** concurrent TURN-relayed calls cannot exceed the named cap; estimated relay GB is visible in CloudWatch; a refusal at the cap degrades gracefully to the same path a real network failure already takes. **Rollback:** flip `video.turn.enabled` off — every call becomes STUN-only-or-terminal, the cheapest and simplest state, with no separate switch to remember. **Do NOT:** let the cap check race under concurrent requests without a consistent read of the counter; invent a fourth code path for "degrade to audio-only" when 4.3.3's terminal/STUN-only states already are one.

### TASK 4.5.1 — The join-button state machine, on both sides of the account shell

**Milestone:** M4.5 · **Requirements:** D-12 · **Depends on:** 4.2.1, 4.3.3, 4.4.2 · **Size:** M · **Cost:** £0.00
**Context.** Every prior Phase 4 task built a mechanism; nothing yet has given a patient or clinician a button. This task is the account-shell surface — the same "backend task ships its own page" shape TASK 2.5.3's caseload view and TASK 3.1.1's profile page already established — and it is where 4.2.1's typed `join-denied` reasons and 4.3.1/4.3.3's connection-state type both finally have a reader.
**Files:** `apps/web/src/account/JoinCallButton.tsx` (new), `apps/web/src/account/VideoCall.tsx`, `apps/web/src/pages/[locale]/account/call/[appointmentId].astro`, `docs/runbooks/video-calls.md`.
**Steps.** 1. A small state machine over 4.2.1's join outcome and 4.3.1/4.3.3's connection state: `too-early` (shows a countdown to the join window), `ready` (device check, 4.3.2, then the join button itself), `in-call`, `call-failed`, `ended` — each a distinct, legible, keyboard-reachable state, `RequireAuth`-gated, the statically-empty-page discipline unchanged from every prior authenticated page. 2. `too-late` and `cancelled` (4.2.1's other denial reasons) render as plain-language explanations, not a bare error code — a patient or clinician reading "this appointment was cancelled" rather than a 403 they have to interpret. 3. Leaving a call (`type: 'leave'`, 4.2.2/4.4.2) is a real button, not only a tab close — it transitions to `ended` on both the leaver's own UI and, via the relay, tells the other party's client the call ended rather than leaving them staring at a frozen video. 4. Loading/empty/error states as semantic HTML with `role="status"`/`role="alert"`, matching every prior authenticated page's established shape.
**Interfaces:** none new — consumes every earlier Phase 4 task's own interface.
**Tests.** Same honestly-scoped frontend gap as every UI-only task this phase has built — construction-time accessibility is the coverage; the state transitions themselves are unit-tested against `call-state-machine.ts` and 4.2.1's typed denial reasons directly, not re-derived from a live call. pr-env: axe-clean and keyboard-complete by construction for every named state, including the `too-early` countdown and the `call-failed` terminal screen — a caller stuck in an inaccessible error state is the single worst outcome a video-call feature can produce, and gets its own explicit check rather than inheriting the page's general pass.
**Verification:** manual sign-in as a matched test patient and clinician, at each of the five named states, on both sides of the account shell.
**Flag:** `video.call.enabled` — reused; this page is the first thing that makes the flag visible to an actual user. **DoD:** a patient and their assigned clinician can find, join, conduct and end a real video consultation from the account shell, with every failure mode along the way rendered as a legible state rather than a hang or a bare error. **Rollback:** flip any of Phase 4's flags off — the page renders `too-early`/`call-failed`-shaped states gracefully rather than crashing, since it was built to handle "the backend said no" from its very first task. **Do NOT:** let a state transition depend on a live poll where an existing WebSocket message already carries the fact; leave `ended` indistinguishable from `call-failed` — one is expected, the other is not, and a caller should never have to guess which happened.

**Gate G4:** a real cross-network call (not same-LAN) completes end-to-end at least once, forced through TURN to prove the relay path actually works, not only the cap that limits it; measured relay cost is compared against `03-cost-model.md`'s modelled ≈85 GB/worst-case figure using 4.4.2's own `EstimatedTurnRelayGB` metric. Gate-specific addition, per `06-gate-checklists.md`. This phase's own contribution to that audit: R-03's three-part mitigation — cap, telemetry, degrade-to-audio-only — lands as one mechanism (4.4.2) rather than three independent ones, and the join-authorisation boundary 4.2.1 built is the first entity in this codebase to audit a *read*-shaped decision rather than only a write.

## Phase 5 — Hardening & launch

*(Elaborated to full §12 detail at Gate G4, per D-27 — the same policy that elaborated Phase 2 at Gate G1, Phase 3 at Gate G2, and Phase 4 at Gate G3. Phases 6–7 remain stubs below, elaborated at their own gates.)*

**A different shape of citation than Phases 2–4.** Every earlier phase's tasks trace mostly to FR-prefixed requirement IDs — new capability, cited against what a patient or clinician can now do. Phase 5 adds no new FR. Its tasks prove **properties of what already exists** — capacity, security posture, accessibility, recoverability — so most cite an NFR, a decision, or a risk instead: NFR-05 and D-20 (M5.1), R-04/LL-05 (M5.2), FR-X-02 (M5.3, the one FR carried over from Phase 1's own accessibility primitive), D-21/D-22 (M5.4), C-01 (M5.5). The standing citation gap (`07-traceability.md`) is otherwise unchanged by this phase and is not repeated task-by-task below — see the Gate G4 report.

**One number this elaboration owes an explanation for.** `02-risk-register.md`'s R-10 already cites a task by number, written before this phase existed: "Cold starts breach p95 < 500ms (NFR-05) … measured at M5.1. If missed, provisioned-concurrency cost shown to you rather than absorbed" — Task **5.1.2**. Elaboration lands squarely on that number: M5.1 is exactly two tasks, a harness-and-baseline task and the cold-start finding-and-fix task R-10 already named, the same forward-citation constraint R-03 placed on M4.4 at the last gate. `01-decisions.md`'s D-21 makes the equivalent claim at milestone grain rather than task grain — "restore drill actually executed at **M5.4**" — satisfied here by landing M5.4 as a single task, 5.4.1, rather than splitting a one-event drill into artificial parts just to fill a stub estimate.

**What Phase 5 inherits, built and unused.** DynamoDB PITR has been enabled and billed for (`03-cost-model.md`'s PITR line) since the table itself was created in Phase 0/1 — never restored from. `docs/compliance/dpia-skeleton.md` has stood since TASK 0.3.4, gained one section at TASK 2.1.3, and untouched since — still explicitly "a skeleton, not a completed DPIA," LL-05's to finish. The account-shell accessibility gap this phase's own M5.3 closes has been named honestly at every UI task since TASK 2.2.4 (2.2.4, 3.1.1, 3.5.2, 3.6.2, and every Phase 4 UI task) — six citations before this one closes it. Every flag from Phase 1 through Phase 4 is off in production (confirmed live at Gate G4) — this phase's own M5.5 is where that changes, deliberately last and deliberately gated on everything above it.

**Two one-off costs, distinguished from every recurring line in `03-cost-model.md`.** TASK 5.1.1's load-test run and TASK 5.4.1's restore drill both provision real, temporary AWS resources, priced and torn down within the same task — neither is a new recurring line, and each task's own Cost header says so explicitly rather than leaving a reader to wonder why `03-cost-model.md`'s total is untouched by tasks that plainly spend something.

### TASK 5.1.1 — Load-test harness and a 10× baseline run

**Milestone:** M5.1 · **Requirements:** NFR-05 · **Decisions:** D-20 · **Depends on:** 0.6.3, 4.5.1 · **Blocks:** 5.1.2, 5.5.1 · **Size:** M · **Cost:** one-off, bounded — a short-lived disposable stack's own request/throughput cost for the run's duration, modelled before the run and re-priced against the actual bill afterward; no recurring line.
**Context.** D-20 sets the SLO this whole product is held to (99.9%/month, 43 minutes' budget) but nothing built so far has ever tested capacity — every DoD line through Phase 4 proves correctness on demand, never behaviour under load. "10×" needs a real baseline to multiply, and none of `03-cost-model.md`'s own figures is one: they describe modelled *monthly* volume (signups, appointments, WebSocket messages), not peak concurrency. This task's first job is deriving a peak-concurrency figure from that monthly volume before firing a single request — the same "prove the shape before building" discipline ADR-0002 applied to indexes, applied here to load for the first time.
**Files:** `tests/load/` (new), `docs/runbooks/load-testing.md` (new), `infra/src/data-stack.ts`, `infra/bin/app.ts`, `infra/src/config.ts`, `docs/plan/03-cost-model.md` (one-off run-cost line, added once a real run exists).
**Steps.** 1. Add a load-testing tool as a new devDependency — the first of its kind in this repo — named and justified in the runbook rather than assumed. 2. Derive 10×-peak figures from `03-cost-model.md`'s own M12 monthly volumes for public/content HTTP traffic and the WebSocket signalling connect path — stating the active-hours and peak-to-average assumptions used to convert "per month" into "peak concurrent," so the 10× figure is traceable to a number already in this repo, not invented fresh. 3. Provision a disposable copy of production — **`DataStack` + `WebStack` only**, extending TASK 0.6.3's `ephemeral`/`prLabel` pattern to `DataStack` — and tear it down after. **Corrected during implementation, not as originally written here:** no `AuthStack` copy. `infra/bin/app.ts`'s own comment on `AuthStack` already states why one is never created for an ephemeral stack (`RETAIN` plus deletion protection — "a directory nothing could clean up") — the same reason applies to a load-test copy. It also is not needed: `DataStack`/`WebStack`'s Lambdas resolve Cognito identifiers from `config.ts`'s fixed constants, not a cross-stack reference, so the disposable copy authenticates against the same real production pools a real test principal already exists in. 4. Run the harness at the derived figures; capture p50/p95/p99 latency, error rate, and DynamoDB/Lambda throttle counts per route. The signalling scenario is connect-only in this first pass — a real `join` needs a real scheduled test appointment inside its own window, a fixture shared with TASK 5.3.1 that does not exist yet; a connect→join→relay scenario is this task's own later extension, not deferred to 5.1.2. 5. Add one *one-off* line to `03-cost-model.md` for this run's own cost, explicitly distinguished from every recurring line already there.
**Interfaces:** none new — this task measures existing interfaces.
**Tests.** The harness's own completed run, producing the four captured metrics per route with no script error, is this task's deliverable — the same "the guard's own test is the deliverable" shape TASK 0.3.1 used for the destructive-primitive lint rule. The 10x derivation itself is unit-tested directly (`derive-targets.test.ts`), independent of whether a live run has happened.
**Verification:** a completed run against the disposable stack produces a per-route p50/p95/p99/error-rate/throttle report; the stack's teardown is confirmed the same way an ephemeral PR stack's is (`aws cloudformation describe-stacks` → `NOT_FOUND`/`DELETE_COMPLETE`).
**Flag:** none — this task tests behaviour, it gates nothing new. **DoD:** a repeatable, documented 10×-derived load run has been executed at least once against a disposable copy, producing per-route latency/error/throttle data. **Landed in two parts, named honestly rather than claimed together:** the harness, the CDK ephemeral-mode extension, and the 10x derivation are built and `cdk synth`/unit-test verified; the live run itself — the part of this DoD that is an event, not an artefact — is deferred to a separate, owner-approved go-ahead (`docs/runbooks/load-testing.md`'s own "Status" section). **Rollback:** n/a — nothing here runs in production. **Do NOT:** run this load test against production; invent the 10× figure without deriving it from `03-cost-model.md`'s own volumes first; treat the mechanism landing as equivalent to the run having happened.

### TASK 5.1.2 — Cold-start p95 finding and fix

**Milestone:** M5.1 · **Requirements:** NFR-05 · **Risks:** **R-10** (names this task by number) · **Decisions:** D-20 · **Depends on:** 5.1.1 · **Blocks:** 5.5.1 · **Size:** S–M · **Cost:** £0 if no fix requires provisioned concurrency; named explicitly and owner-approved before enabling if one does — never silently switched on.
**Context.** `02-risk-register.md`'s R-10 names this task before it existed: "Cold starts breach p95 < 500ms (NFR-05) … measured at M5.1. If missed, provisioned-concurrency cost shown to you rather than absorbed." This is where that measurement happens and where the cost-disclosure promise is kept, not deferred further.
**Files:** `docs/runbooks/load-testing.md`, `docs/plan/03-cost-model.md` (only if a fix is enabled).
**Steps.** 1. From 5.1.1's captured p95 figures, isolate the cold-start-prone routes — arm64 Lambdas with no VPC on the request path, R-10's own mitigation shape, cross-referenced against invocation frequency, since a rarely-hit route's occasional cold start matters less than a busy one's. 2. For any route whose p95 exceeds 500ms with a material cold-start share, name the fix candidates in the runbook: provisioned concurrency (a real, ongoing, named cost — computed from AWS's published per-GB-second provisioned rate against the function's memory and the warm-instance count a fix needs), a leaner bundle (re-checking TASK 0.1.2's own bundling discipline for regressions Phases 1–4 may have introduced), or accepting the miss with a documented reason. 3. **Never enable provisioned concurrency without first pricing it into `03-cost-model.md` and getting the owner's explicit go-ahead** — R-10's "shown to you rather than absorbed" clause, honoured literally. 4. Re-run the relevant slice of 5.1.1's harness after any fix to confirm the p95 figure actually moved.
**Interfaces:** none new.
**Tests.** The harness re-run itself is this task's regression test — no new test infrastructure.
**Verification:** a per-route p95 table exists, naming pass/fail against NFR-05's 500ms bar; any fix's effect is shown by a before/after re-run.
**Flag:** none. **DoD:** every route's p95 is measured and compared against NFR-05; any provisioned-concurrency spend is priced in `03-cost-model.md` and owner-approved before being enabled. **Rollback:** remove provisioned concurrency (if enabled) via the same CDK change that added it. **Do NOT:** enable an ongoing-cost fix without pricing and sign-off first; treat an unmeasured route as passing by omission.

### TASK 5.2.1 — Full-codebase security review

**Milestone:** M5.2 · **Depends on:** 4.5.1 · **Blocks:** 5.2.2 · **Size:** M · **Cost:** £0 — review effort only; any finding that needs a code change becomes its own sized follow-up task, not work absorbed into this one.
**Context.** Every gate since G0 has run a *scoped* security check — that gate's own diff, that gate's own dependency audit, an authorisation-boundary re-audit "from scratch" but against the current entity set only. None has been a holistic pass across the whole, now-complete web product — four phases and nine merged video-calling tasks of accumulated surface, reviewed together for the first time, before Phase 6 adds a second consumer of the same API.
**Files:** `docs/plan/gate-g4-security-review.md` (new, or folded into the next gate report if the review lands close enough to G5 — decided at execution time).
**Steps.** 1. Review, across the whole account rather than one gate's diff: every IAM role/policy in `ndn-prod`; every externally-reachable endpoint (HttpApi, WebSocketApi, CloudFront) against its own stated rule in `04-data-model-rbac.md`; every secret's storage/rotation posture (SSM SecureString, Cognito, the TURN credential's own short-lived-token design); the dependency tree (`pnpm audit` plus a manual look at anything the audit tool can't reason about — the known Stripe dependency behaviour, `gate-g3-report.md` §6a, is this review's first confirmed non-issue to re-check for anything new). 2. Every finding gets a severity and a named owner — a follow-up task number if code must change, an owner-action if it needs a real-world decision, the same two-lane triage `gate-g3-report.md` §6a already used. 3. Record findings in this task's own file. 4. No finding is fixed silently inside this task — a code-shaped finding becomes its own reviewed task, so this task's own diff stays reviewable.
**Interfaces:** none.
**Tests.** n/a — a review, not a code change; any fix it spawns carries its own Tests line as its own task.
**Verification:** the review document exists; every finding has a severity and an owner; `pnpm audit --audit-level=high` reports no known vulnerabilities at the time of the pass.
**Flag:** none. **DoD:** every IAM role, endpoint, and secret has been reviewed at least once as a whole, not only diff-by-diff; every finding is recorded with a severity and an owner. **Rollback:** n/a. **Do NOT:** fix a finding inline instead of spawning a reviewed follow-up task; treat a clean review as equivalent to a completed DPIA — 5.2.2 is a separate, narrower claim.

### TASK 5.2.2 — DPIA skeleton updated with what Phases 2–4 actually built

**Milestone:** M5.2 · **Requirements:** R-04, LL-05 · **Depends on:** 5.2.1 · **Size:** S · **Cost:** £0.
**Context.** `docs/compliance/dpia-skeleton.md` states its own scope precisely: "a skeleton, not a completed DPIA … out of scope for the executor to fill in" — full completion is **LL-05**, the owner's and their DPO's action, blocking launch, and this task does not change that. What the skeleton is missing is a summary of what actually shipped since its last update at TASK 2.1.3: the private-field split now has five real callers instead of zero (`gate-g3-report.md`), and the clinical-record, assessment, messaging and video-calling entities all now exist for a DPO to be told about rather than have to rediscover by reading the codebase themselves.
**Files:** `docs/compliance/dpia-skeleton.md`.
**Steps.** 1. Add sections in the skeleton's own existing style ("implemented \<date\>") for: the assessment split (3.3.1/3.3.2 — the two-row private/visible proof), messaging (3.6.1 — content-free notifications, append-only, never edited or removed), video calling (4.x — no media ever stored, P2P-first, the SDP/ICE payload never logged, TURN as a Cloudflare-processed short-lived credential rather than a stored one). 2. Do not attempt to resolve R-04's open tension, perform a lawful-basis analysis, or mark LL-05 closed — the skeleton's own closing lines ("Do NOT treat this skeleton as a completed DPIA") govern this task exactly as they governed 0.3.4 and 2.1.3 before it.
**Interfaces:** none. **Tests.** n/a — a documentation update.
**Verification:** a DPO reading the skeleton today can see every clinical-data-adjacent entity this codebase has ever built, not only the two it had at TASK 2.1.3.
**Flag:** none. **DoD:** the skeleton names every clinical-data-handling entity built through Phase 4, still explicitly marked incomplete. **Rollback:** revert the doc change. **Do NOT:** mark LL-05 closed; make any lawful-basis or erasure-policy decision on the DPO's behalf.

### TASK 5.3.1 — The live-session accessibility check the account shell has never had

**Milestone:** M5.3 · **Requirements:** FR-X-02 · **Depends on:** 4.5.1 · **Blocks:** 5.3.2 · **Size:** M · **Cost:** negligible — reuses existing Cognito/API infra; no new AWS resource beyond a small number of clearly-labelled test-principal records.
**Context.** Named for the sixth time at Gate G4's own §7 — every authenticated account-shell page since TASK 2.2.4 has stated the same honest limitation: construction-time accessibility is real coverage, a live-rendered-DOM axe scan is not, because `tests/pr-env/a11y-full.test.ts` reads only `routes.ts`, and every authenticated page is deliberately absent from it (`call.astro`'s own header: "an authenticated-only page has no accessible content to find that way"). This task closes the gap, but not by extending the existing per-PR mechanism: TASK 0.6.3's ephemeral stack deploys `WebStack` **alone** — no `DataStack`, no `AuthStack` (`infra/bin/app.ts`: "DataStack's resources aren't behind the PR-environment integration/a11y suite today") — so an authenticated page in a PR environment has no real API or Cognito pool to sign into. A live-session check for these pages needs a real, signed-in session, which means running against **production** (or a full-stack disposable copy, per 5.1.1), not the lightweight per-PR stack every public page's a11y gate already uses.
**Files:** `apps/web/src/account-routes.ts` (new), `tests/pr-env/a11y-authenticated.test.ts` (new, run on a separate schedule — see Steps), `.github/workflows/ci.yml` (new scheduled job), `docs/runbooks/video-calls.md` and other affected runbooks (cross-reference this closing the gap they named).
**Steps.** 1. A small number of permanent, clearly-labelled test identities — one patient, one principal clinician, one sub-clinician — created once in production Cognito, named unambiguously (e.g. `test.patient+ndn@…`) and excluded from any real notification/marketing path: the same test identities every manual Verification line since Phase 2 has already informally relied on ("a real test patient," "a matched test patient/clinician"), made durable and named rather than ad hoc. 2. A rolling test appointment kept inside `call.astro`'s own join window (a small scheduled job, or a fixture the check creates and cancels per run), since that page is the one authenticated surface gated by time as well as auth. 3. A new route registry, `account-routes.ts`, mirroring `routes.ts`'s own shape for exactly the pages `routes.ts` deliberately excludes — profile, caseload, content, messages, call among them — so a new authenticated page that forgets to register here silently drops out of this gate, the same guarantee `routes.ts`'s own DoD already gives public pages. 4. A new Playwright spec, run on a **schedule** (nightly) rather than per-PR — tied to production's own flag state and a live time-boxed appointment window, so gating it to arbitrary PR-merge timing would be flaky in a way the existing per-PR gate never has to tolerate — that signs in via the real Cognito hosted-UI flow with each test identity's credentials (a GitHub secret, never in code) and axe-scans every route `account-routes.ts` registers. 5. A failing scan surfaces named violations, the same shape `a11y-full.test.ts` already uses for public pages.
**Interfaces:** none new over the wire — a test consumer of every existing authenticated route.
**Tests.** The spec's own scheduled runs are the deliverable, the same "the guard's own test is the deliverable" shape as TASK 0.3.1's lint rule and 5.1.1's harness.
**Verification:** a scheduled run signs in as each test identity in turn and axe-scans every page `account-routes.ts` registers, asserting zero violations the same way `a11y-full.test.ts` does for public pages.
**Flag:** none — the check reads whichever flags are already on wherever it runs; it gates nothing itself. **DoD:** every authenticated account-shell page is axe-scanned in a real, signed-in, rendered session at least once per day, not only at construction time. **Rollback:** disable the scheduled workflow; nothing it built is load-bearing for user-facing behaviour. **Do NOT:** run this check against a per-PR ephemeral stack (it has no backend to sign into); let a test identity receive a real SMS/email by forgetting to exclude it from a notification path; add an authenticated page without registering it in `account-routes.ts`.

**Landed in two parts, named honestly rather than claimed together, the same shape TASK 5.1.1 used:** `account-routes.ts`, the Playwright spec, its sign-in setup project, and the scheduled/`workflow_dispatch` CI job are built and unit/lint/typecheck-verified. **Corrected during implementation, not as originally written above:** only the clinician test identity signs in, not the three named in Steps 1 — the clinician pool is password + TOTP (D-09/ADR-0004), computable from a stored secret with no external service, while the patient pool's passwordless email OTP needs a real inbox no test in this repo can read, and SES production access remains denied regardless ([ses-production-access.md](../runbooks/ses-production-access.md)). Every route is still scanned, including patient-owned ones — the clinician identity reaches a real, legible forbidden state there, not the patient's own content, named per-route by the spec's own annotations. Creating the real test identities, adding the GitHub secrets, and building the rolling test-appointment fixture are owner actions still outstanding ([live-session-accessibility.md](../runbooks/live-session-accessibility.md)) — the DoD's own "at least once per day" is therefore not yet met by this PR alone, the identical gap TASK 5.1.1's own DoD line names for its live run.

### TASK 5.3.2 — Full sweep and remediation

**Milestone:** M5.3 · **Depends on:** 5.3.1 · **Size:** S–M (variable, stated honestly — depends on what 5.3.1's first run finds) · **Cost:** £0.
**Context.** 5.3.1 built the mechanism; this task is its first real run and whatever it finds, fixed — the inverse of this plan's usual "spec first, build second" order, because here the check comes first and the fix follows the check's own findings.
**Files:** whichever account-shell components 5.3.1's run names.
**Steps.** 1. Run 5.3.1's suite against every registered authenticated route. 2. Fix every violation found, holding the same WCAG 2.2 AA target this codebase has held since TASK 1.1.1. 3. Re-run to confirm zero violations. 4. A keyboard-completeness pass alongside axe, matching `keyboard.test.ts`'s existing shape for public pages, extended to the authenticated set for the first time.
**Interfaces:** none. **Tests.** The re-run itself, at zero violations, is the test.
**Verification:** 5.3.1's suite passes clean across every authenticated route.
**Flag:** whichever flags already gate each page — unchanged by this task. **DoD:** every authenticated page passes the live-session check with zero axe violations and is fully keyboard-operable. **Rollback:** revert whichever fix regresses something else. **Do NOT:** suppress a violation instead of fixing it; narrow 5.3.1's own registered route list to make this task's pass easier.

### TASK 5.4.1 — Restore drill: a real DynamoDB PITR restore, executed

**Milestone:** M5.4 · **Decisions:** D-21, D-22 · **Depends on:** 4.5.1 · **Size:** M · **Cost:** transient, one-off — a restored table's own storage/throughput for the drill's short lifetime, modelled in `03-cost-model.md` as a one-off line the same way 5.1.1's load-test run is, then torn down; no recurring line.
**Context.** D-21 named this exact moment before Phase 5 existed — "restore drill actually executed at M5.4" — drawing a distinction this task exists to close: "PITR on a sub-1 GB clinical dataset costs pennies" is a fact about *storage*, not about whether a restore has ever been attempted. PITR has been billed for and enabled since Phase 0/1 (`03-cost-model.md`'s own PITR line); nothing in this codebase has ever restored from it. This is this plan's first task whose entire DoD is an **event**, not a deployed artefact — every prior Verification line proves a mechanism exists and behaves correctly on demand; this one proves the mechanism actually works, once, for real, because a backup nobody has restored from is a belief, not a fact.
**Files:** `docs/runbooks/restore-drill.md` (new).
**Steps.** 1. Pick a real point in time inside the table's current PITR window (confirmed against the table's own settings, not assumed at 35 days by default). 2. `aws dynamodb restore-table-to-point-in-time` into a **new, isolated table name** in the same account — never overwriting the live table, the same "never touch what's live" discipline every destructive-primitive guard in this codebase enforces for a delete, applied here to a restore instead. 3. Verify the restored table's item count and a sample of known real rows (a known test patient/appointment/audit entry from before the chosen point in time) match expectations — not merely that `RESTORE_IN_PROGRESS` reached `ACTIVE`. 4. Measure wall-clock time from "decision to restore" to "verified usable" — the real number behind D-21's RTO ≤4-working-hours promise, turned from a target into a measured fact. 5. Also restore D-22's periodic export (the second, independent layer — "PITR **plus** periodic export to a separate object-locked prefix") into a scratch location and confirm it is readable too, since D-22 named it as protection against an account-compromise scenario PITR alone would not survive, and an export nobody has read is the same kind of unproven belief PITR itself was before this task. 6. Delete the restored table and scratch export copy once verified — real, temporary resources, fully cleaned up, the same discipline 5.1.1's disposable load-test stack uses. 7. Write `docs/runbooks/restore-drill.md`, recording the exact commands, the measured RTO, and what was checked, so the next restore — real or drilled — has a proven procedure rather than one improvised under pressure.
**Interfaces:** none — an operational procedure, not a code change.
**Tests.** n/a in the usual sense — the drill's own successful, verified, timed execution is the test, the same "the guard's own test is the deliverable" shape as every other verification-shaped task in this plan.
**Verification:** a restored table (from PITR) and a restored export copy both exist temporarily, both verified against known data, both torn down; `docs/runbooks/restore-drill.md` records a real measured RTO against the ≤4-working-hour target.
**Flag:** none. **DoD:** a real PITR restore and a real export restore have both been executed at least once, verified against known data, measured for RTO, and documented; the drill's own temporary resources are gone. **Rollback:** n/a — this task is itself the recovery mechanism being proven; the live table is never touched. **Do NOT:** restore into or over the live table; leave the drill's restored table or export copy running after verification; treat "PITR is enabled" as equivalent to "a restore has been proven to work."

### TASK 5.5.1 — Cost reconciliation across the whole C-01 envelope

**Milestone:** M5.5 · **Requirements:** C-01 · **Depends on:** 5.1.2 · **Size:** S · **Cost:** £0.
**Context.** Every gate has reconciled spend against the model as a checklist line, but always against a system still partly unbuilt and every flag off. This is the first reconciliation against the complete Phase 0–4 web product, including 5.1.1/5.1.2's own load-test-derived figures — real per-route cost at 10× volume, not only the monthly-average line `03-cost-model.md` has carried since G0.
**Files:** `docs/plan/03-cost-model.md`.
**Steps.** 1. Re-price every line in `03-cost-model.md` against real, live `ndn-prod` billing data (Cost Explorer, per-service) rather than modelled figures — the same live-repricing discipline every `UNVERIFIED`-price resolution in `09-self-audit.md` already uses. 2. Reconcile the load test's own measured per-request cost (5.1.1) against the model's assumed request volume, closing the loop between "modelled at M12 volume" and "what 10× that volume actually costs." 3. Update the model's total line if reconciliation finds material drift; explain any drift rather than silently absorbing it, matching every prior gate's own spend-vs-model section.
**Interfaces:** none. **Tests.** n/a — a documentation/data reconciliation.
**Verification:** `03-cost-model.md`'s total is shown alongside real Cost Explorer figures for the equivalent period, with any drift explained.
**Flag:** none. **DoD:** every cost-model line is checked against real billing data at least once before launch; drift is named, not absorbed. **Rollback:** n/a. **Do NOT:** silently adjust the model to match reality without explaining why they diverged.

### TASK 5.5.2 — Runbook consolidation and index

**Milestone:** M5.5 · **Depends on:** 5.1.2, 5.2.2, 5.3.2, 5.4.1 · **Size:** M · **Cost:** £0.
**Context.** This codebase holds 48 runbooks, one per task or closely-related task cluster — exactly the pattern this plan has followed since Phase 0 — but nothing indexes them, and a future maintainer (or Phase 6's own team) has no map from "I need to understand X" to which of 48 files covers it. This is a consolidation pass, not a rewrite: every existing runbook's content stays exactly where it is (the "one runbook per task, extended not duplicated" pattern `docs/runbooks/video-calls.md` itself demonstrates across four tasks remains the right shape for task-level detail) — what is missing is the layer above it.
**Files:** `docs/runbooks/README.md` (new), spot updates to existing runbooks' own "Owner actions" sections where later tasks closed them.
**Steps.** 1. `docs/runbooks/README.md` — an index grouping every runbook by phase/subsystem (auth, data, video, notifications, cost/observability, CI/deploy), each with a one-line description. 2. A pass over every runbook's own open-item / "Owner actions" sections to confirm which are now closed by a later task and mark them so — several almost certainly closed silently as later phases landed without circling back (e.g. `video-calls.md`'s own "no reachable link to the call page" gap, still open per TASK 4.5.1's Files note, closed by 5.5.3 below in dependency order — this task records that closure once it happens, not before). 3. No runbook is deleted — the append-only, never-remove-history discipline this codebase applies to data, extended to documentation for the first time.
**Interfaces:** none. **Tests.** n/a — a documentation task.
**Verification:** `docs/runbooks/README.md` exists and every file in the directory is reachable from it; a sample of "Owner actions" sections are confirmed accurate against current state.
**Flag:** none. **DoD:** every runbook is indexed and its own open items are current, not stale. **Rollback:** revert the doc changes. **Do NOT:** delete or merge runbook files — extend the index layer, not the existing convention.

### TASK 5.5.3 — Close the remaining named gaps and flip the go-live flags

**Milestone:** M5.5 · **Depends on:** 5.5.2 · **Size:** M · **Cost:** £0 beyond whatever real usage the newly-live flags generate — already modelled in `03-cost-model.md` since each flag's own originating task.
**Context.** This is the task `05-execution-plan.md`'s own stub table named directly — "**G5 = WEB IS DONE**." Every flag from Phase 1 through Phase 4 is still off in production. This task is where that changes — deliberately last, deliberately dependent on every other Phase 5 task (load capacity proven, security reviewed, accessibility swept, restore drill executed, runbooks current), because turning on real patient-facing capability before those are true would make the rest of this phase theatre.
**Files:** `docs/runbooks/go-live.md` (new), `docs/runbooks/video-calls.md` (close the "find" gap), whichever appointment-list/calendar components (2.5.3, 3.4.1) need a real link into `call.astro`.
**Steps.** 1. Close the specific, already-named gaps still open at this point rather than newly discovered ones — starting with `video-calls.md`'s own "no reachable link to the call page" (TASK 4.5.1's own Files note: the appointment list and calendar views need a real link into `call.astro`'s query-string entry point, the "find" step 4.5.1 explicitly left undone). 2. **The flag sequence is the owner's own go/no-go, not a default executed unprompted** — each flag flips only with the account owner's explicit, named approval at execution time, in an order this runbook states and justifies (e.g. authentication and profile before scheduling before messaging before video, since each depends on the last being real for anyone to reach it) — matching this plan's standing distinction between a task's mechanism (built and provably correct) and a real launch decision (always named as the owner's action, never assumed). 3. Confirm **LL-05** (DPIA) and **LL-06** (solicitor/DPO sign-off on R-04) are closed by their own owners before flipping any flag that creates real clinical data — this task does not close either long-lead item itself and does not proceed past this check silently. 4. Post-flip, the existing canary/smoke/rollback mechanism (0.6.2) is what actually governs each flip's safety, unchanged — this task provides the sequence and the go/no-go gate, not a new deployment mechanism.
**Interfaces:** none new — this task turns on interfaces every earlier task already built and tested.
**Tests.** Each flag's own originating task already carries its full test suite; this task's own test is the sequence and the LL-05/LL-06 gate check, verified by their presence in the runbook and an explicit sign-off record, not an automated assertion.
**Verification:** `docs/runbooks/go-live.md` names the flip sequence and the owner sign-off for each step, and confirms LL-05/LL-06 closed before any clinical-data-creating flag goes on; `aws ssm describe-parameters` shows the flags coming on in the documented order, not all at once.
**Flag:** every flag from Phase 1 through Phase 4, per the sequence above — this is the one task in this plan that turns them on rather than building or testing them off. **DoD:** every named gap is closed, the go-live sequence is documented and owner-approved step by step, LL-05/LL-06 are confirmed closed before any clinical-data flag flips, and the web product is live for real patients and clinicians — the moment this table called "G5 = WEB IS DONE." **Rollback:** flip any individual flag back off — the same "flag gates the decision, not the deploy" property every flag in this codebase has held since it was introduced. **Do NOT:** flip a clinical-data-creating flag before LL-05/LL-06 are confirmed closed; flip flags in an order other than the documented sequence without re-justifying it; treat this task's completion as something executed without the owner's explicit, step-by-step approval.

**Step 1 closed, 2026-08-28** — both halves of `video-calls.md`'s "no reachable link" gap (patient's next-appointment panel, a new clinician calendar page), see that runbook's own section. **Step 2 closed, 2026-08-29** — the sequence itself is written, [docs/runbooks/go-live.md](../runbooks/go-live.md): split into a public-site track with no LL dependency and a patient-facing track gated on LL-05/LL-06, against the actual live flag state (four on, for D-29's synthetic-patient proof only; every other flag still off).

**Steps 3–4 and the DoD's own "live for real patients," closed 2026-08-30 — but not the way this task's own step 3 assumed.** Step 3 reads "confirm LL-05/LL-06 are closed... before flipping any flag that creates real clinical data." **They are not closed.** The owner instead named an explicit override — **D-33** (`01-decisions.md`): "I will get it done legally later on but I want to go live now for real patients. go with it." Track A's five flags and Track B's ten were both flipped that same day, per `go-live.md`'s own updated record, with LL-05/LL-06 reframed for India (DPDPA 2023, not UK GDPR — see D-33) and left genuinely open, not closed. This task's own "Do NOT: flip a clinical-data-creating flag before LL-05/LL-06 are confirmed closed" is the one Do NOT line in this entire plan that a direct, explicit owner instruction has overridden — recorded here rather than silently treated as satisfied. The web product is live for real patients; the legal review that was meant to precede that is not, and remains the owner's own to complete.

**Gate G5:** restore drill evidence; load test at 10×. Gate-specific addition, per `06-gate-checklists.md`. This phase's own contribution to that criterion: D-21's "restore drill actually executed" and R-10's forward-cited cold-start measurement both land as real, dated events rather than descriptions of a mechanism — the first gate in this plan whose defining criterion is only satisfiable by something happening, not something existing.

## Phase 6 — Mobile

*(Elaborated to full §12 detail at Gate G5, per D-27 — the same policy that elaborated Phase 2 at Gate G1, Phase 3 at Gate G2, Phase 4 at Gate G3, and Phase 5 at Gate G4.)*

**A citation gap Phase 5 didn't have.** Phase 5 could still reach for an NFR, a decision or a risk ID against every task, because the brief anchors those elsewhere even though it cites no new FR. Phase 6 has less than that: the brief's own stub row is three cells — "M6.1–M6.7", "~14", "Only after G5; additive versioned API changes only" — no milestone names, no FR/NFR, nothing for this elaboration to expand. Everything below cites what the brief and this plan's own committed docs actually give it: **D-26**/**ADR-0013** (the platform decision itself), **R-01** (push notifications' own forward-citation, first named at TASK 2.3.1 and repeated at every self-audit since), **LL-09**/**LL-10** (the two store-side long-lead items), and **C-01**/**C-10** (cost, since store fees sit outside it by the brief's own words). The standing FR/NFR traceability gap (`07-traceability.md`) is unchanged by this phase and not repeated task-by-task below.

**What Phase 6 inherits, built and unused.** `apps/mobile` has existed since TASK 0.1.2 as an empty workspace package — one placeholder function, one placeholder test, wired into the monorepo's own lint/typecheck/test scripts and CI matrix from day one, never touched since. `packages/api-client` and `packages/shared-types` are the same: real workspace packages, real `tsconfig`/`vitest` wiring, and in `shared-types`'s case a genuinely complete set of the platform's own domain types (`Patient`, `Appointment`, `ClinicalRecord`, `Message`, …) already mirrored 1:1 against `services/api`'s own shapes — but `api-client`'s own `src/index.ts` is still the literal placeholder TASK 0.1.2 wrote (`export function placeholder(): string`). **D-26's own promise — "sharing api-client, types, i18n" — is half true today:** `shared-types` and `packages/i18n` are real and already shared by `apps/web`; `api-client` is not yet a real thing anything shares, because nothing has needed a second HTTP consumer of this API until now. `apps/web` itself does not consume `packages/api-client` — every account-shell page since TASK 2.2.4 has written its own small, colocated fetch wrapper (`patient-admin-request.ts` is the most recent instance, and its own header explains why: no render-test harness exists in that directory, so extracting shared HTTP logic has had no test-coverage pressure to reuse it under). **Phase 6 is what makes `api-client` real** — it does not retroactively migrate `apps/web` onto it, which is out of scope here and not this phase's own Files to touch.

**"Additive versioned API changes only" (the stub table's own words), and what it rules out.** Every Phase 6 task that needs a server-side change adds a new route, a new optional field, or a new versioned response shape — never edits an existing handler's request/response contract in place. `apps/web` and the Lambda authorizer that fronts both clients are the reason: a breaking change to `GET /patients/me/appointments`'s own shape would break production the moment it deployed, for a client this phase's own tasks don't control the release cadence of. Where a genuinely new capability needs a genuinely new endpoint (device-token registration, 6.3.2), it is named as new; where an existing endpoint already serves what mobile needs (profile, diagnosis/care plan, assessments, messaging), mobile calls it unchanged.

### TASK 6.1.1 — Expo app scaffold, navigation shell, and the real `api-client`

**Milestone:** M6.1 · **Decisions:** D-26, ADR-0013 · **Depends on:** 0.1.2 · **Blocks:** 6.1.2, 6.2.1 · **Size:** M · **Cost:** £0.00 — Expo's own tooling and EAS's free build tier cover this task; no AWS resource, no store account needed yet (that's M6.6).
**Context.** `apps/mobile` today is TASK 0.1.2's own placeholder — one function, one test, real workspace wiring. This task is the first one to put a real Expo app inside it and give `packages/api-client` its first real consumer, closing the honest half of D-26's promise named above. Reusing `shared-types` means every request/response shape mobile ever sends or receives is the exact same TypeScript type `services/api`'s own handlers already validate against — a shape drifting between client and server is a compile error, not a runtime surprise, the identical guarantee `apps/web` already has.
**Files:** `apps/mobile/app.json`, `apps/mobile/App.tsx` (new), `apps/mobile/src/navigation/` (new), `packages/api-client/src/index.ts` (real implementation, replacing the placeholder), `packages/api-client/src/http-client.ts` (new), `packages/api-client/src/token-store.ts` (new).
**Steps.** 1. `npx create-expo-app` inside `apps/mobile`, TypeScript template, Expo Router for file-based navigation — the same "convention over a hand-rolled router" reasoning `apps/web`'s own Astro file-based routing already uses, so a mobile screen and a web page are discoverable the same way. 2. Build `HttpClient` in `packages/api-client`: a thin `fetch` wrapper typed against `@ndn/shared-types`, one function per resource (`getPatientProfile`, `listAppointments`, …), a single place that attaches the bearer token and the API's base URL — no per-screen fetch wrapper, unlike `apps/web`'s own established pattern, because this package's whole reason to exist is being that one place. 3. `TokenStore` interface with an Expo `SecureStore`-backed implementation — never `AsyncStorage`, which is unencrypted device storage; a session token is exactly the kind of thing `00-conventions.md`'s own "no secret in a place that isn't SecureString/SecureStore" convention already covers for every other credential in this codebase. 4. A navigation shell with a signed-out stack (sign-in only, 6.1.2) and a signed-in tab stack (profile, appointments, content, messages — one tab per Phase 6 milestone below, empty screens for now).
**Interfaces:** `packages/api-client`'s own typed functions become the interface every later Phase 6 task calls; no backend change.
**Tests.** `http-client.test.ts` — every function attaches the stored token as `Authorization: Bearer <token>`, and a 401 response clears the stored token rather than retrying silently. `token-store.test.ts` — an injectable `SecureStore`-shaped seam (the same "inject the platform dependency" pattern `clock.ts`/`FlagSource` already establish), proving `TokenStore` never falls back to `AsyncStorage` even if `SecureStore` is unavailable — it surfaces an error instead. No render tests — `apps/mobile` has no test harness for React Native components today, the identical "test the pure logic, not the render" position every account-shell task in `apps/web` has stated since TASK 2.2.4.
**Verification:** `pnpm --filter @ndn/mobile exec expo start` boots the app in Expo Go against a local/dev build; `pnpm --filter @ndn/api-client test` green; the signed-out stack renders with no backend call made.
**Flag:** none — nothing here is user-facing yet (no sign-in works until 6.1.2). **DoD:** the Expo app builds and runs, `api-client` has a real, tested implementation with no placeholder left, and every later Phase 6 task calls it rather than writing its own fetch. **Rollback:** n/a — no production surface exists yet. **Do NOT:** give `HttpClient` a second, ad hoc fetch path for "just this one screen" — every resource call goes through it, the whole point of building it now rather than letting `apps/web`'s per-page pattern repeat here too.

### TASK 6.1.2 — Mobile sign-in against the same Cognito pools

**Milestone:** M6.1 · **Requirements:** §5 · **Decisions:** D-09, D-29 · **Depends on:** 6.1.1, 2.2.1 · **Blocks:** every signed-in Phase 6 task below · **Size:** M · **Cost:** £0.00 — no new Cognito pool, no new infrastructure; the two pools TASK 2.2.1/D-29 already built.
**Context.** Mobile authenticates against the *same two user pools* the web account shell already uses — a patient pool (D-29: staff-issued password, SRP, no self-service) and a clinician pool (D-09: TOTP untouched). Nothing here creates a third identity system or a third set of credentials; a patient or clinician who already has a password/TOTP secret from the web flow uses the identical one on mobile. The live SRP round-trip already proven in `patient-account-provisioning.md`'s own status update (`amazon-cognito-identity-js`, no dependency added to that scratch script) is the same library this task adds for real, as a genuine dependency this time.
**Files:** `apps/mobile/src/auth/` (new — `cognito-client.ts`, `sign-in-screen.tsx`), `packages/api-client/src/token-store.ts` (refresh-token handling), `infra/src/auth-stack.ts` (a second app client per pool, mobile-specific), `services/api/src/flags.ts` (new flag).
**Steps.** 1. `amazon-cognito-identity-js` (the narrower, dependency-lighter library over Amplify's full `Auth` category — matching this repo's own "no dependency without a concrete need" discipline) performs SRP sign-in against the patient pool, TOTP-challenge sign-in against the clinician pool — the exact two flows `web-authentication.md`/`cognito-user-pools.md` already document, a second client, not a second flow. 2. **A second app client per pool, mobile-scoped, is added rather than reusing the web app client** — `infra/src/auth-stack.ts`'s existing clients carry `HttpOnly` cookie-oriented settings (TASK 2.2.4) that make no sense for a native app storing its own tokens in `SecureStore`; a distinct client with its own `ExplicitAuthFlows` (`ALLOW_USER_SRP_AUTH`/TOTP as appropriate) but the *same* pool means the *same* user directory — a patient or clinician exists once, authenticates through whichever client matches whichever surface they're on. Both clients are **provisioned unflagged, dark** — D-23's own "build dark behind flags, not around them" reasoning, matching how every earlier phase's infra ships before its own flag ever turns on. 3. `TokenStore` persists the refresh token in `SecureStore`; access-token refresh happens transparently on a 401, mirroring `lambda-authorizer.md`'s own token-verification contract — the authorizer does not know or care which app client issued a token, only that it's a valid, unexpired token for a `Principal` it can resolve. 4. Sign-out clears `SecureStore` and nothing server-side — the same stateless-token model the web shell already uses.
**Interfaces:** none new for the authorizer's own contract — a mobile-issued token is verified by the exact same `lambda-authorizer.md` Lambda every web request already goes through; the additive-only guarantee holds here in its purest form (a second app client is infrastructure, not an API change).
**Tests.** `cognito-client.test.ts` — SRP and TOTP flows against a mocked Cognito SDK, both success and the named failure shapes (wrong password, wrong TOTP code, `NEW_PASSWORD_REQUIRED` never reached — D-29's own permanent-password guarantee holding for mobile too). `auth-stack.test.ts` — the two new app clients synthesize with the mobile-appropriate `ExplicitAuthFlows` and none of the `HttpOnly`-cookie-specific settings that don't apply here. Negative: an expired refresh token signs the app out to the signed-out stack, never a silent crash. Negative: the authorizer rejects a mobile-client-issued token while `mobile.signIn.enabled` is off, exactly as it would any other unrecognised-but-otherwise-valid token.
**Verification:** a real sign-in against `ndn-prod`'s own patient pool, using D-29's existing synthetic test patient, from a real Expo Go session — the same "prove it live, not just synthesized" bar `patient-account-provisioning.md` already set for the web side.
**Flag:** a new `mobile.signIn.enabled` (default off) gates the authorizer's acceptance of a token issued by either mobile app client — the clients themselves are provisioned dark per Step 2 above, so this flag is the only thing standing between "infrastructure exists" and "a session actually works," the same distinction `patients.administration.enabled`'s own comment draws. Not `auth.webSignIn.enabled` — that flag's name and every existing caller are web-specific, and conflating the two would flip mobile sign-in on the day someone flips the web flag for an unrelated reason. **DoD:** a patient and a clinician can each sign in from a real device/simulator against the real pools, get a working session, and sign out; no new identity store, no new credential type. **Rollback:** flip `mobile.signIn.enabled` off — the app clients stay provisioned but issue no working session path the app's own UI can complete. **Do NOT:** create a third user pool "for mobile" — every identity in this system lives in exactly two pools, by design, since TASK 2.2.1.

### TASK 6.2.1 — Patient profile, diagnosis/care plan, and assessments — read-only mobile views

**Milestone:** M6.2 · **Requirements:** §5 · **Depends on:** 6.1.2, 3.1.1, 3.2.2, 3.3.2 · **Blocks:** 6.2.2 · **Size:** M · **Cost:** £0.00 — reuses `GET /patients/me`, `GET /patients/{id}/diagnosis`, `GET /patients/{id}/assessments` unchanged; no new Lambda, no new route.
**Context.** The first genuinely additive-only Phase 6 task in the strict sense: every endpoint this screen calls already exists, already enforces R-09's chokepoint (`projection.ts`) at the repository layer regardless of which client calls it, and already has its own negative tests. Mobile adds no server code here at all — the whole task is `api-client` functions plus screens.
**Files:** `packages/api-client/src/patient.ts`, `packages/api-client/src/clinical-record.ts`, `packages/api-client/src/assessment.ts` (new client functions, typed against `@ndn/shared-types`), `apps/mobile/src/screens/profile/`, `apps/mobile/src/screens/clinical-record/`, `apps/mobile/src/screens/assessments/` (new).
**Steps.** 1. Add the three read functions to `api-client`, each a direct typed wrapper over an existing route — no request shape invented. 2. Build the three screens as plain, non-editable views (profile *update* is 6.2.2's own scope, not this task's). 3. A sub-clinician's own caseload read (TASK 3.1.2) is explicitly **out of this task** — a clinician-facing caseload view on a phone screen is a real, separate design problem (list density, no room for `CaseloadView.tsx`'s own table layout) this task's own Files don't attempt; named here so it isn't silently assumed covered.
**Interfaces:** none new.
**Tests.** `patient.test.ts`/`clinical-record.test.ts`/`assessment.test.ts` (in `api-client`) — each function calls the right path/method and returns the typed shape, the same "test the client, not the render" position 6.1.1 already took. A negative test confirms the assessment client function's own return type has no `private{}`-shaped field in its TypeScript type at all — R-09's boundary enforced again at the type level, the same guarantee `shared-types`'s own `Assessment` type already gives every consumer, mobile included, for free.
**Verification:** a real signed-in session (patient and clinician) against `ndn-prod` renders each screen with real data — no field a patient's own role shouldn't see, confirmed by the same account (not a spot check) R-09's own standing discipline expects at every new surface.
**Flag:** `patients.profile.enabled`/`clinicalRecords.enabled`/`assessments.enabled` — the same three flags the web account shell already reads, not new ones; a flag flip is shared infrastructure across both clients by design (D-23's own "one source of truth" reasoning). **DoD:** a patient reads their own profile, diagnosis/care plan and assessment-visible half on mobile; a clinician reads both assessment halves; nothing a patient's role forbids is ever fetched. **Rollback:** flip the shared flag off — mobile and web both lose the surface together, never independently, since they read the identical flag. **Do NOT:** fetch an assessment's private half into the mobile client "to have it ready" and filter client-side — the server-side projection is the only boundary this codebase trusts, the same rule `assessment-forms.md` states for the web client.

### TASK 6.2.2 — Patient profile update, and "my content" on mobile

**Milestone:** M6.2 · **Requirements:** §5 · **Depends on:** 6.2.1, 3.1.1, 3.5.2 · **Size:** M · **Cost:** £0.00 — reuses `PUT /patients/me`, `GET /patients/{id}/content` unchanged.
**Context.** Closes M6.2 with the two remaining surfaces `apps/web` already has: the patient's own profile edit (`PUT /patients/me`, TASK 3.1.1) and the assigned-content list (TASK 3.5.2) — media rendering (PDFs, video links) on a phone is the one genuinely new client-side concern this task takes on; everything else is the same additive-only shape 6.2.1 already established.
**Files:** `packages/api-client/src/patient.ts` (add the update function), `packages/api-client/src/content-assignment.ts` (new), `apps/mobile/src/screens/profile/edit.tsx`, `apps/mobile/src/screens/content/` (new).
**Steps.** 1. Profile edit: a form over the same fields `PatientRecord`'s own type already names, `PUT /patients/me` unchanged. 2. Content list: fetch assigned content, render per `content.type` — text/markdown inline, PDF via the device's own viewer (`expo-web-browser` or a native share-sheet, not an embedded PDF renderer this task would have to build and maintain), external video links opened out to the device's own browser/player rather than embedded. 3. No media *upload* capability on mobile — `content-authoring.md`'s own authoring surface stays clinician/web-only; this task is the patient's own read side plus profile write, nothing that creates content.
**Interfaces:** none new.
**Tests.** `patient.test.ts` (update path), `content-assignment.test.ts` — client-side, same shape as 6.2.1. A negative test: the profile-update function never sends a field `PatientRecord`'s own writable-fields type doesn't include (compile-time, via the shared type, not a runtime check this task has to write itself).
**Verification:** a real profile edit round-trips against `ndn-prod`; a real assigned-content item opens correctly by type.
**Flag:** `patients.profile.enabled`, `contentAssignment.enabled` — shared with web. **DoD:** a patient edits their own profile and reads assigned content, of every type this platform supports, from a phone. **Rollback:** flip the shared flag off. **Do NOT:** build an in-app PDF/video renderer — the device's own capability is the right tool here, and maintaining a second rendering engine is scope this task doesn't need.

### TASK 6.3.1 — Appointments on mobile

**Milestone:** M6.3 · **Requirements:** §5 · **Depends on:** 6.1.2, 3.4.1, 3.4.2 · **Blocks:** 6.3.2, 6.5.2 · **Size:** M · **Cost:** £0.00 — reuses `GET /patients/me/appointments`, `GET /clinicians/me/calendar`, the reschedule/cancel routes, unchanged.
**Context.** The mobile equivalent of `NextAppointmentPanel.tsx` (patient) and `ClinicianCalendar.tsx` (clinician, TASK 5.5.3) — same data, a phone-appropriate list instead of a 30-day grid. This task's own list is also where 6.5.2's join link lives, the same `callHref`-shaped `<patientId>#<scheduledAt>` construction `video-calls.md` already established, ported rather than reinvented.
**Files:** `packages/api-client/src/appointment.ts` (new), `apps/mobile/src/screens/appointments/` (new).
**Steps.** 1. Patient: next-appointment + upcoming list, reusing the exact response shape `NextAppointmentPanel.test.ts`'s own `findNext` already proves against. 2. Clinician: the same 30-day-forward window `ClinicianCalendar.tsx` computes (`calendarWindow`), ported as a pure function into `api-client` rather than duplicated ad hoc, so both clients derive the same window from the same one implementation for the first time — `apps/web` re-imports it from there too, the first real instance of D-26's "shared" promise reaching backward into `apps/web`, not just forward into mobile. 3. Reschedule/cancel (TASK 3.4.2) wired for both roles, unchanged routes.
**Interfaces:** none new.
**Tests.** `appointment.test.ts` — `calendarWindow`/`findNext`-equivalent pure functions, tested once in `api-client`, re-asserted (not re-implemented) from both `apps/web` and `apps/mobile`'s own test suites — a divergence here would be a real bug, not a style choice.
**Verification:** a real signed-in patient and clinician session each show the correct upcoming/calendar data against `ndn-prod`.
**Flag:** `appointments.enabled`, shared with web. **DoD:** a patient sees their next/upcoming appointments and can reschedule/cancel; a clinician sees their 30-day calendar; both from `calendarWindow`'s one shared implementation. **Rollback:** flip the shared flag off. **Do NOT:** reimplement `calendarWindow` a second time in `apps/mobile` — port it into `api-client` once, imported by both clients.

### TASK 6.3.2 — Push notifications: a new channel on the existing notification abstraction

**Milestone:** M6.3 · **Requirements:** §5 · **Risks:** R-01 · **Decisions:** D-10, D-11 · **Depends on:** 2.3.1, 6.1.2 · **Size:** M · **Cost:** £0.00 recurring — Expo's push service, APNs and FCM are all free to use at this account's volume; the *one-off* Apple Developer enrolment (LL-09) this channel's own production certificate needs is priced and gated in M6.6, not here.
**Context.** **This is R-01's own forward-citation, landing:** every risk-register entry and every self-audit red-team pass since TASK 2.3.1 has named "push notifications in Phase 6 relieve [the SMS cap] permanently" as the plan's own answer to reminder volume outrunning the SMS budget at scale. This task is where that becomes real — not a replacement for email-primary (D-10 stands unchanged) or for the SMS reminder (D-11's two-flag independence stands too), but a *third*, free, high-reliability channel the notification abstraction (`notifications.ts`, TASK 2.3.1) gains for the first time since it shipped with exactly two.
**Files:** `services/api/src/notifications.ts` (a third `NotificationChannel` implementation, `PushNotificationSender`), `services/api/src/flags.ts` (new flag), `infra/src/data-stack.ts` (a new route, a new access pattern), `packages/api-client/src/device.ts` (new), `apps/mobile/src/push/` (new), `docs/plan/04-data-model-rbac.md` (a new row).
**Steps.** 1. **A new, additive route: `POST /devices`** — a signed-in principal registers their own Expo push token, keyed `PAT#<id>`/`DEVICE#<token>` or `CLI#<id>`/`DEVICE#<token>` (append-only, never deleted — a stale token simply fails delivery and is marked so, the same "never delete, mark instead" discipline every other entity in this schema already follows). 2. `PushNotificationSender` implements the same interface `notifications.ts`'s email/SMS senders already do — `send(principal, message)` — resolving every registered device token for that principal and calling Expo's push API (a single HTTPS POST, no SDK dependency needed for the send path itself; `expo-server-sdk` only if batching/receipt-checking earns its own dependency once real volume exists). 3. Wire the 1-hour appointment reminder (TASK 3.4.3) to attempt push *first*, falling back to email/SMS exactly as `notifications.ts`'s own degradation ladder already defines for any channel failure — never a second, parallel reminder path. 4. On the mobile app: request notification permission at a deliberate moment (after sign-in, not at first launch — a permission prompt with no context is the single most common reason a user denies one), register the returned Expo push token via `POST /devices` on grant.
**Interfaces:** `POST /devices` (new, additive) · the existing reminder sweep (3.4.3) gains a channel; its own trigger and schedule unchanged.
**Tests.** Unit: `PushNotificationSender` attempts push, and on a delivery failure (invalid/expired token) falls through to the next channel in the ladder, recording a `degraded` delivery exactly as `notifications.ts`'s own test suite already asserts for the email→SMS case — this is a third rung on the same ladder, not a new mechanism to re-test from scratch. Unit: a principal with two registered devices gets exactly one reminder, delivered to both, not two independent sends. Negative: `POST /devices` without a valid principal → `401`; a device token is never returned in any `GET` response. Integration (synth): the devices access pattern grants `PutItem`/`Query` scoped to the caller's own `PAT#`/`CLI#` partition, nothing broader.
**Verification:** a real registered device on `ndn-prod` receives a real push notification for a real synthetic-patient test appointment's 1-hour reminder, confirmed delivered (Expo's own delivery-receipt API) — the same "prove it live" bar this plan holds every notification-adjacent task to.
**Flag:** `notifications.push.enabled`, default off, independent of `appointments.reminders.enabled` — the same two-flag independence D-11 already established for SMS versus scheduling, extended to a third channel rather than special-cased. **DoD:** a registered device receives a real push notification for a real reminder; a delivery failure degrades to email/SMS exactly as every other channel failure already does; R-01's own risk-register mitigation is struck as closed rather than merely "prioritised," the wording every red-team pass has carried since TASK 2.3.1. **Rollback:** flip `notifications.push.enabled` off — the ladder's next rung (SMS, then email) is what every reminder falls back to, unchanged. **Do NOT:** make push the *only* channel for anything — the whole design is defence in depth across three channels, and a platform-specific outage (Expo's own push service, or Apple's/Google's upstream) must never silently drop a reminder, the identical invariant R-01 has stated since TASK 2.3.1.

### TASK 6.4.1 — Messaging on mobile

**Milestone:** M6.4 · **Requirements:** §5 · **Depends on:** 6.1.2, 3.6.1, 3.6.2 · **Blocks:** 6.4.2 · **Size:** M · **Cost:** £0.00 — reuses the existing rate-limited messaging routes unchanged.
**Context.** The mobile equivalent of `MessageThread.tsx` — same rate-limited send/read routes TASK 3.6.1 built, the same one-flag-for-both-halves reasoning already established.
**Files:** `packages/api-client/src/message.ts` (new), `apps/mobile/src/screens/messages/` (new).
**Steps.** 1. Thread list + a single thread view, reusing the existing pagination shape unchanged. 2. The same rate-limit `429` response `MessageThread.tsx` already treats as an ordinary, legible outcome (not an error state) — ported identically, not reinvented.
**Interfaces:** none new.
**Tests.** `message.test.ts` — client functions only, same "test the client, not the render" position.
**Verification:** a real message round-trips between a patient and clinician session, one on mobile, one on web — cross-client delivery is this task's own proof that the API, not the client, is the shared surface.
**Flag:** `messaging.enabled`, shared with web. **DoD:** a patient and clinician can exchange messages from mobile, interoperably with the web thread — the same conversation, either client. **Rollback:** flip the shared flag off.

### TASK 6.4.2 — Push notification on a new message

**Milestone:** M6.4 · **Requirements:** §5 · **Depends on:** 6.3.2, 6.4.1 · **Size:** S · **Cost:** £0.00 — one more caller of 6.3.2's own channel, no new infrastructure.
**Context.** The smallest task in this phase: messaging has no notification of its own today (`messaging.md` names this as out of scope at TASK 3.6.1, a Phase-6-adjacent concern at the time). Now that 6.3.2 exists, this is a second caller of the same channel, not a second mechanism.
**Files:** `services/api/src/message.ts` (call `PushNotificationSender` on send).
**Steps.** 1. On a successful `POST /messages`, notify the *other* party via 6.3.2's own sender — no email/SMS fallback ladder for this one, deliberately: a missed push here is not the safety-critical kind of miss an appointment reminder is (R-01's own concern), so this task does not inherit that ladder's full weight, just its channel.
**Interfaces:** none new.
**Tests.** Unit: sending a message triggers exactly one push, to the recipient only, never the sender. Negative: a recipient with no registered device — silent no-op, never an error the sender's own request sees.
**Verification:** a real message send against `ndn-prod` produces a real push to the other party's registered device.
**Flag:** `notifications.push.enabled` (6.3.2's own flag) plus `messaging.enabled` — both must be on; neither is new. **DoD:** a new message notifies its recipient in near-real-time when they have a registered device. **Rollback:** flip `notifications.push.enabled` off — messaging itself is unaffected, only the notification.

### TASK 6.5.1 — WebRTC on React Native: the peer connection, over the same signalling relay

**Milestone:** M6.5 · **Requirements:** FR-VID-04 · **Depends on:** 6.1.2, 6.3.1, 4.1.1, 4.2.2 · **Blocks:** 6.5.2 · **Size:** L · **Cost:** £0.00 recurring — the same signalling/TURN infrastructure Phase 4 already built and priced; `react-native-webrtc` itself has no service cost.
**Context.** **A real constraint this task's own Files must accept, not work around: `react-native-webrtc` is a native module.** Expo Go — what every earlier Phase 6 task can be developed and demoed in — cannot load it; this task is the point at which `apps/mobile` moves to an **Expo custom dev client** (`expo-dev-client`, built via EAS Build), still the fully-managed Expo workflow ADR-0013 chose, not a fork into a bare React Native project — EAS's own config-plugin mechanism is exactly what lets a managed app add a native module without giving up CI-buildable, over-the-air-updatable Expo tooling. Worth naming precisely because it is the one point in ADR-0013's own "Expo, not native ×2" reasoning that could look, from a distance, like a walk-back; it is not — Expo with a custom dev client is still one shared codebase, still not two native rewrites, and remains the framework the decision actually named.
**Files:** `apps/mobile/app.json` (the `expo-dev-client`/`react-native-webrtc` config plugins), `apps/mobile/src/video/` (new — `webrtc-client.ts`, a thin native adapter), `packages/api-client/src/connection.ts` (new — the shared, SDK-free signalling-message logic).
**Steps.** 1. `webrtc-signalling-client.ts`'s own parser/message-shape logic (TASK 4.1.1/4.2.2, already SDK-free and unit-tested per its own header) is ported into `packages/api-client` largely as-is — the same offer/answer/ICE-candidate message shapes travel over the same WebSocket API Gateway route, `ws-relay.ts` unchanged, unaware and uncaring which client is on either end. 2. The one genuinely platform-specific layer — `RTCPeerConnection`, `mediaDevices.getUserMedia` from `react-native-webrtc` — is a thin adapter in `apps/mobile/src/video/webrtc-client.ts`, not a fork of the signalling logic itself. 3. STUN-only first, exactly as TASK 4.3.1's own web-side sequencing did; TURN is inherited from Phase 4 unchanged (`TurnCredentialsFunction`), unaware of client platform, and needs nothing new here.
**Interfaces:** none new — the same WebSocket route and message shapes TASK 4.1.1/4.2.2 built.
**Tests.** Ported message-shape/parser tests, identical assertions to `webrtc-signalling-client.test.ts`, proving the shared logic behaves identically regardless of which package imports it. No render/native-module test — `react-native-webrtc` itself is not unit-testable without a real device/simulator, the same "SDK-touching code is proven live, not by a mock" position `apps/web`'s own `VideoCall.tsx` has held since TASK 4.5.1.
**Verification:** a real STUN-only call, one side on a real device/simulator (mobile), one side a browser (web) or a second device — cross-client interoperability is this task's own defining proof, not same-platform-only.
**Flag:** `video.signalling.enabled`, `video.callAuthz.enabled` — shared with web, no new flag. **DoD:** a mobile client and a web client complete a real STUN-only call with each other, over the identical signalling relay. **Rollback:** the app simply doesn't build the video screen's dependency into a release — no server-side rollback needed since nothing server-side changed. **Do NOT:** fork `ws-relay.ts`'s own logic or add a mobile-specific message shape "to make the client easier" — cross-client interoperability is only provable if both clients speak the identical protocol.

### TASK 6.5.2 — Device permissions and the join flow, on mobile

**Milestone:** M6.5 · **Requirements:** FR-VID-04 · **Depends on:** 6.5.1, 4.3.2, 4.3.3, 4.5.1 · **Size:** M · **Cost:** £0.00.
**Context.** The mobile port of `DeviceCheck.tsx` and the join-button state machine (TASK 4.3.2/4.5.1) — the state machine itself (`call-state-machine.ts`) is already SDK-free, platform-agnostic logic per its own design; this task ports the *UI* around it, not the machine.
**Files:** `apps/mobile/src/screens/call/` (new); `call-state-machine.ts` extracted into `packages/api-client` if not already standalone, the same "shared logic, not duplicated" discipline 6.3.1's `calendarWindow` port already established.
**Steps.** 1. Camera/microphone permission via Expo's own permissions API, mapped onto the exact same `classifyMediaError` categories TASK 4.3.2 already defined — a denied permission is legible the same way on both platforms, not a mobile-specific error shape. 2. The join button drives the same state machine, rendering the same named states (`too-early`, `ready`, `in-call`, `call-failed`, `ended`) with mobile-native UI instead of web components. 3. The reachable-link gap TASK 5.5.3 closed on web (a real link from the appointment list into the call) is built here from day one on mobile — 6.3.1's own appointment screen links directly into this one, never repeating the gap TASK 4.5.1 originally left and 5.5.3 had to close after the fact.
**Interfaces:** none new.
**Tests.** `classifyMediaError` reused/re-asserted against Expo's own permission-result shapes. No render test, matching 6.5.1's own position.
**Verification:** a real call joined from a mobile device, camera/mic permission granted and denied paths both exercised live.
**Flag:** `video.call.enabled`'s own non-flag status is unchanged (TASK 4.5.1's own note: never wired to a flag of its own) — `video.turn.enabled` shared with web. **DoD:** a patient or clinician can find, join, conduct and end a call from a phone, every failure state as legible as the web equivalent. **Rollback:** n/a — same non-flagged status the web join button already has.

### TASK 6.6.1 — Store readiness: identity, listings, and the two long-lead items

**Milestone:** M6.6 · **Decisions:** D-26 · **Long-lead:** LL-09, LL-10 · **Depends on:** 6.1.1–6.5.2 (every user-facing screen this phase builds) · **Size:** M · **Cost:** one-off, outside C-01 — **Apple Developer Program, re-verified live 2026-08-29: $99/yr, unchanged since D-26's own original estimate → £81.78/yr at this plan's own £1=$1.2105 planning rate.** **Google Play registration, re-verified live 2026-08-29: $25 one-off, unchanged → £20.65.** Both struck from `09-self-audit.md`'s `UNVERIFIED` list as the third of the original six to move from deferred to resolved (after AWS SMS at 2.3.2, Cloudflare TURN at Gate G3) — the exact gate this plan named for it ("re-verified... before Phase 6").
**Context.** **LL-09 (Apple Developer enrolment, D-U-N-S) and LL-10 (Google Play data-safety declarations) are the owner's own actions, 2–4 weeks and ~1 week respectively, both named "After G5" in `08-long-lead.md` since the plan's own first draft** — this task is the point both actually start, not before. Nothing in Phase 6's own build (6.1.1–6.5.2) needed either: Expo Go/EAS internal builds run without a store account at all. This task is squarely the "reported, outside C-01" store-fee line finally being spent, and the content/compliance work only a real store listing forces into the open (privacy nutrition labels, data-safety declarations, an actual privacy policy URL rather than TASK 1.2.2's own i18n placeholder).
**Files:** `apps/mobile/app.json` (bundle identifiers, versioning), `docs/runbooks/mobile-store-submission.md` (new), `docs/plan/03-cost-model.md` (the two re-verified GBP figures), `docs/plan/09-self-audit.md` (strike the third `UNVERIFIED` price).
**Steps.** 1. **Owner action, this task's own gate, not proceeded past silently:** LL-09 (Apple D-U-N-S/enrolment) and LL-10 (Google Play data-safety questionnaire) both submitted — real lead times, 2–4 weeks and ~1 week, started here because nothing before this point needed them. 2. Store listing content: screenshots (generated from a real build against `ndn-prod`'s own synthetic-patient fixture, never fabricated), the privacy policy URL pointed at the real page TASK 1.2.2 shipped (no longer a placeholder by this point in the plan, per that task's own DoD), Apple's privacy "nutrition label" and Google's data-safety form both completed **honestly against what this codebase actually collects** — a direct, factual answer from `04-data-model-rbac.md`'s own schema, not a template. 3. EAS Build configured for both platforms' release profiles; TestFlight (Apple) and an internal testing track (Google) are this task's own first real distribution, before any public release.
**Interfaces:** none.
**Tests.** No automated test — this task's own deliverable is content and process, the same "the event is the deliverable" shape TASK 5.4.1's restore drill already established for this plan.
**Verification:** both store listings pass their own platform's internal-review stage (TestFlight processing, Google's internal track), reachable by a named tester account, before 6.7.1 attempts a public submission.
**Flag:** none. **DoD:** both developer accounts exist, both store listings are complete and honest against this codebase's real data collection, both apps are installable by a named internal tester. **Rollback:** n/a — no production user-facing change. **Do NOT:** submit for public review before 6.7.1's own gate — internal testing first, the same staged-rollout discipline TASK 0.6.2's canary deploy already applies to every web release.

### TASK 6.6.2 — Mobile observability and staged-rollout configuration

**Milestone:** M6.6 · **Depends on:** 6.6.1 · **Size:** S · **Cost:** ~£0/mo at this account's volume — Expo's own crash/update telemetry has a free tier comfortably inside this app's expected traffic; re-priced against a real bill at Gate G6 if usage says otherwise, the same "don't assume a free tier forever" discipline `09-self-audit.md` already applies to every other line.
**Context.** Phase 5 built this discipline for the web product (load testing, cold-start measurement, a security review, an accessibility sweep, a restore drill) before any real patient reached it; mobile gets the equivalent floor — crash visibility and a rollback mechanism — before its own store release, not after an incident forces it.
**Files:** `apps/mobile/app.json` (EAS Update channels), `docs/runbooks/mobile-store-submission.md` (extended, not duplicated — this repo's own "one runbook per task cluster" convention).
**Steps.** 1. EAS Update's own staged-rollout percentage mechanism configured as this platform's equivalent of TASK 0.6.2's canary — a bad JS-level release rolls back to the previous binary's own OTA channel without an app-store resubmission; a bad *native* change (a new native module, a config-plugin change) cannot roll back this way and needs a real store release instead — named honestly, not conflated. 2. Crash reporting wired (whichever Expo-compatible integration this task's own implementation names, evaluated against the same "no dependency without a concrete need" bar 6.1.2 already applied) so a production crash is visible without waiting for a support conversation, mirroring `03-cost-model.md`'s own CloudWatch-alarm posture on the backend.
**Interfaces:** none.
**Tests.** No automated test — configuration, verified by a deliberate forced-failure drill (a build that crashes on launch, confirmed caught by the reporting pipeline) before this task's own DoD is claimed met, the same "prove the guard fails when it should" discipline `log-retention.ts`'s own Aspect tests already hold themselves to.
**Verification:** a deliberately broken OTA update rolls back via the staged channel without a new store submission; a deliberate crash appears in the reporting dashboard within minutes.
**Flag:** none. **DoD:** a bad JS-level release can be rolled back without a store resubmission; a crash is visible in near-real-time. **Rollback:** the mechanism this task builds *is* the rollback path — no separate rollback of this task itself beyond reverting the EAS channel config. **Do NOT:** treat OTA rollback as covering a native-level regression — it does not, and pretending otherwise is exactly the kind of gap this task exists to name rather than paper over.

### TASK 6.7.1 — Store submission and phased public rollout

**Milestone:** M6.7 · **Depends on:** 6.6.1, 6.6.2 · **Size:** M · **Cost:** £0 beyond 6.6.1's own one-off fees, already spent by this point.
**Context.** The mobile equivalent of TASK 1.6.1's G1 cutover and TASK 5.5.3's go-live sequence — the point real users can install this app, gated the same way every real launch in this plan has been: a named owner decision, not a default this task executes on its own authority.
**Files:** none new — process, not code.
**Steps.** 1. **Owner's own explicit go-ahead, named at execution time, the identical standing rule `go-live.md` states for every production flag flip** — public submission is not this task's to trigger unprompted. 2. Apple App Review and Google Play review submitted from the internal tracks 6.6.1 already proved. 3. Phased rollout on Google Play's own staged-percentage mechanism (Apple's own equivalent, phased release, enabled the same way) — a launch that can be paused mid-rollout if either platform's own review or early real-user telemetry finds a problem, the same canary discipline TASK 0.6.2 established for the web product, applied here for the first time to a store release neither this codebase nor `cdk` controls the deploy mechanics of.
**Interfaces:** none.
**Tests.** No automated test — a store review process is this task's own deliverable, the same "the event is the deliverable" shape 5.4.1/6.6.1 already established.
**Verification:** both apps pass their platform's review and become installable by the general public, at whatever rollout percentage the phased release names.
**Flag:** none — the store's own review/rollout mechanism is the gate here, not an SSM flag; every *feature* flag inside the app is still governed by TASK 5.5.3's own go-live sequence regardless of which client reaches it. **DoD:** both apps are live in their respective stores, phased rollout underway, with the owner's own named go-ahead on record. **Rollback:** pause or halt the phased rollout on either platform; a native-level rollback beyond that needs a new store submission, named honestly per 6.6.2's own caveat. **Do NOT:** submit for public review without the owner's own explicit, dated approval — the identical discipline `go-live.md` already states for every flag flip, extended here to a channel this plan does not otherwise control.

### TASK 6.7.2 — Post-launch monitoring and Phase 6 cost reconciliation

**Milestone:** M6.7 · **Depends on:** 6.7.1 · **Size:** S · **Cost:** reconciliation only — no new spend.
**Context.** The mobile equivalent of TASK 5.5.1 — real usage against the modelled figures, for the first time, now that a real public release exists.
**Files:** `docs/plan/03-cost-model.md` (mobile's own actual-vs-modelled line), `docs/runbooks/mobile-store-submission.md` (extended).
**Steps.** 1. Real push-notification volume checked against Expo's/APNs'/FCM's own free-tier ceilings — none of the three is expected to be threatened at this account's scale, confirmed rather than assumed. 2. Real store-review outcomes and any post-launch crash/rollback events recorded. 3. `03-cost-model.md`'s own C-01 total re-confirmed unaffected — every Phase 6 recurring cost modelled at £0.00, the store fees already reported outside the envelope from the start.
**Interfaces:** none.
**Tests.** None — reconciliation.
**Verification:** a dated, live-checked reconciliation entry, the same shape TASK 5.5.1's own cost-reconciliation pass already set as this plan's standard.
**Flag:** none. **DoD:** real mobile usage and cost are checked against the model at least once after public launch; nothing in the model is invalidated by the check going unrecorded. **Rollback:** n/a.

**Gate G6:** both apps live in their respective app stores, installable by the general public, at least one real cross-client session completed (a patient or clinician on mobile, the other party on web, in the same appointment/message thread/call) — the mobile equivalent of Gate G4's own "prove the mechanism actually works end-to-end for a real user," not merely reviewed and approved by the stores.

## Phase 7 — milestone plans + task stubs

*(Elaborated to full §12 detail at each gate, per D-27. Phases 2–6 are elaborated above; Phase 7 is elaborated at Gate G6.)*

| Phase | Milestones | Stub tasks | Notes |
|---|---|---|---|
| **7 — Post-launch** | Analytics within privacy constraints, cost review, deferred backlog **incl. chatbot (D-05) if solicitor clears scope** | — | |
