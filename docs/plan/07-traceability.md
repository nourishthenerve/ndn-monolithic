# 9. Traceability (summary)

Every FR/NFR maps to ≥1 task and ≥1 test. Full matrix in `docs/plan/07-traceability.md`. Coverage as planned: **FR-PP** 12/12 · **FR-DP** 12/12 · **FR-WEB** 6/7 (FR-WEB-03 deferred by D-05, tracked not dropped) · **FR-VID** 6/6 · **FR-X** 7/7 · **NFR** 9/9.

**Still an open action item, through Gate G2 and Phase 3's elaboration.** This file has pointed at itself for the actual matrix since it was first committed — the source requirements brief it summarises (`ndn-planning-brief.md`, named in `00-index.md`) was never committed to this repo, so the counts above are the plan's own original claim, not something re-derivable from what's in the tree. `09-self-audit.md`'s Gate G1 finding said this should close "before G2 audits coverage"; it has not closed, `05-execution-plan.md`'s Phase 3 elaboration says so again at the point Gate G2 was supposed to fix it, and only `FR-DP-02`, `FR-DP-05`, `FR-PP-10`, `FR-VID-04` and the `FR-X-*`/`FR-WEB-*` IDs Phase 1 established are pinned to a requirement anywhere in the committed docs. Closing it means locating or reconstructing the source brief's own FR/NFR list and building a real per-ID table here — not re-estimating the counts above from what Phases 2–3 happened to cite.
