# 11. Plan self-audit

- **Coverage:** every FR/NFR maps to ≥1 task and ≥1 test; every risk R-01…R-14 has a named mitigating task. FR-WEB-03 is the sole uncovered requirement, deliberately, by D-05.
- **Ordering:** the dependency graph is acyclic — checked by walking every task's `Depends on` and confirming each names only lower-numbered tasks. Guards (0.3.x) precede all data writes; the SMS cap (0.5.3) precedes any send; canary/rollback (0.6.2) precedes user-facing features. After every task the tree builds and production works; incomplete work merges dark behind flags.
- **Cost roll-up:** £8.67/month at M12 against a £12–14 target and £20 cap — **£3–5 headroom to target, £11 to cap.** No line depends on an expiring free tier.
- **`UNVERIFIED` prices:** .com renewal · ~~AWS UK SMS unit price~~ · ~~Cloudflare TURN free-tier period~~ · Vonage UK SMS · MEF lead time · ~~Apple GBP fee~~ · EBS gp3 + IPv4. Each re-verified at G0 and every 90 days (§14.12). **G0 pass (2026-08-13):** `.com` renewal re-priced live via `aws route53domains list-prices` at $16.00/yr (was ~$14 estimate) — cost model corrected, see [03-cost-model.md](03-cost-model.md). **TASK 2.3.2 pass (2026-08-22):** AWS End User Messaging's UK SMS price re-verified live at $0.035/message (vs. Twilio's re-verified $0.056) and chosen — see [ADR-0008](../adr/0008-sms.md); struck from this list, the first of the six to move from deferred to resolved. **Gate G3 pass (2026-08-26), ahead of Phase 4 elaboration (TASK 4.4.1):** Cloudflare TURN's free-tier period re-verified live via `developers.cloudflare.com/realtime/sfu/pricing` — still 1,000 GB/month free, $0.05/GB overage, unchanged since the figure `03-cost-model.md` has modelled against since G0; struck from this list, the second of the six. **Gate G5 pass (2026-08-29), ahead of Phase 6 elaboration (TASK 6.6.1):** Apple Developer Program and Google Play registration both re-verified live — $99/yr and $25 one-off respectively, both unchanged since D-26's own original estimate; converted at this plan's own planning rate to £81.78/yr and £20.65, see [03-cost-model.md](03-cost-model.md); struck from this list, the third of the six, at exactly the gate this note named for it ("Apple/EBS before Phase 6"). The remaining three (Vonage UK SMS, MEF lead time, EBS gp3+IPv4) are all for services no task has provisioned yet (EBS: no task in this serverless design has ever needed one; Vonage/MEF: never chosen over the AWS SMS provider) — re-confirmed at every gate since G3, including this one, that none is silently already relied upon.

**Gate G4 pass (2026-08-27):** Phase 4 (video calling, TASK 4.1.1–4.5.1) audited in full — see [gate-g4-report.md](gate-g4-report.md). Full test suite green (1,753 tests), 82.64% branch coverage held after a same-day fix (a test file's transitive import had pulled an untested component's module graph into the coverage denominator), no known dependency vulnerabilities, no new `private{}`-shaped entity and the authorisation boundary re-audited clean. **One real, unclosed action item carried forward, not absorbed into a clean go/no-go:** Gate G4's own defining criterion — a real cross-network call forced through TURN, with measured relay cost compared against `03-cost-model.md`'s ≈85 GB worst case — has not been demonstrated, because no Cloudflare TURN key is provisioned yet. This is the plan's own first gate to report **NO-GO on its own criterion** while every other checklist item passes; per `06-gate-checklists.md`'s standing structure, elaboration of the next phase's stubs still ran regardless (Phase 5, below), since that item is not conditioned on go/no-go. Phase 5 is now elaborated to full `§12` detail in `05-execution-plan.md`, ten tasks across five milestones — landing exactly on R-10's forward-citation of task 5.1.2 and D-21's forward-citation of milestone M5.4, the same ordering discipline that constrained Phase 4's own M4.4 at the last gate.

**Gate G5 pass (2026-08-29):** Phase 5 (hardening & launch, TASK 5.1.1–5.5.3) audited in full — see [gate-g5-report.md](gate-g5-report.md). Full test suite green (1,879 tests, up from 1,753 at G4), 82.12% branch coverage held, no known dependency vulnerabilities, no new `private{}`-shaped entity since D-29's account-identity work (confirmed against `04-data-model-rbac.md`'s own `Patient profile` row, still no `visible{}`/`private{}` split). **This is the plan's first GO on a gate's own defining criterion since G1** — "restore drill evidence; load test at 10×" — both are real, dated events (TASK 5.4.1, 2026-08-28; TASK 5.1.1/5.1.2, 2026-08-22/28), unlike G4's still-open TURN call. **That is a narrower claim than the stub table's own "G5 = WEB IS DONE" label, deliberately not conflated with it:** TASK 5.5.3's own remaining steps — LL-05/LL-06 closure and the owner's own approval to flip any patient-facing flag for a real patient — are still open, named honestly in that task's own status update and in [go-live.md](../runbooks/go-live.md) (TASK 5.5.3's own step 2, merged this gate). **One periodic-check finding, closed the same day:** the `autoDeleteObjects` log-group residual TASK 5.2.1/Gate G4 security review already named and accepted (`aws-account-baseline.md`'s own prescribed check) had accumulated to 20 orphaned, infinite-retention log groups across every destroyed ephemeral PR stack since — checked live, all 20 deleted under this repo's standing authority to remove unneeded infrastructure, zero patient data involved; the underlying gap itself is unchanged and still named there, not re-litigated here. Phase 6 is now elaborated to full `§12` detail in `05-execution-plan.md`, fourteen tasks across seven milestones — landing exactly on R-01's forward-citation of push notifications relieving the SMS cap, and on LL-09/LL-10's own "Phase 6" / "After G5" citations in `08-long-lead.md`. A new Gate G6 criterion is added to `06-gate-checklists.md` for it, the same way each earlier phase's own elaboration named its gate.

**Red-team — the five likeliest ways this plan fails:**

1. **SMS arithmetic bites at scale.** 500 patients × weekly reminders ≫ any fixed cap; even at the plan's own ~150/month modelled volume, C-02's £5 bought only ~108 messages under Twilio's rate. AWS End User Messaging's re-verified $0.035/msg (chosen at 2.3.2, ADR-0008) raises that to ~172 — closing the modelled shortfall, though a worse-than-modelled reminder volume still exceeds any fixed cap. *Changed:* email-primary from day one, SMS behind a proven hard block, degradation defined before launch (built as a property of the notification abstraction at 2.3.1), push prioritised in Phase 6.
2. **The private-field boundary leaks through a path nobody tested** — an export, a log line, an error message, a cache. *Changed:* enforcement moved to a single repository-layer projection rather than per-handler, 100% coverage on that chokepoint, and a re-audit *from scratch* at every gate rather than only when the code changes.
3. **DynamoDB single-table design meets a §7 query it can't serve** (admin cross-caseload views, keyword matching) and forces a costly migration mid-build. *Changed:* ADR-002 must prove every §7 query against the key schema **before** any table code is written, and names the fallback explicitly.
4. **Zero staging plus a bad merge takes production down** in front of patients. *Changed:* five independent layers — contract tests, ephemeral PR environments, dark merges behind flags, canary with automatic rollback, and post-deploy smoke — with rollback *demonstrated* at G0, not documented.
5. **Cost creeps invisibly** — log volume, an unmetered path, a retained-media surprise — and the cap is breached before anyone notices. *Changed:* budgets and alarms land in Phase 0 *before* anything can spend, 14-day log retention is a CDK default rather than a habit, and every gate reconciles actual spend against this model across the whole envelope, not just AWS.

**Where I'd push back on the brief (§16.8):** §11 places the entire public website (Phase 1) before any identity or authorisation work (Phase 2). That defers the authorisation boundary — the component where a mistake is most catastrophic and most expensive to retrofit — until after five milestones of accumulated momentum, while front-loading the surface with the least clinical risk. I have **not** silently reordered. I recommend pulling M2.3 (RBAC enforcement layer + audit log) forward to sit alongside M1.1, so the spine exists and is exercised for the whole of Phase 1 before real patients exist. Your call at G1.

**Decided at Gate G1 (2026-08-21): accepted, in the form still available.** Phase 1 shipped before the call was made, so *alongside M1.1* had passed; what was taken is the surviving half — **the spine goes first inside Phase 2, ahead of authentication.** Milestones renumbered against the brief: M2.1 is now the authorisation spine and audit log, M2.2 authentication, M2.3 notifications; M2.4/M2.5 unchanged. Consequences recorded where they land: R-09's chokepoint moves from 3.2.x to **2.1.2** (`02-risk-register.md`), LL-02 now blocks **M2.3** (`08-long-lead.md`, ADR-0008), and Phase 2 is elaborated in full at `05-execution-plan.md`. What the reorder does **not** recover is that Phase 1 ran its whole length with no authorisation boundary and no durable audit log — the four admin-gated Phase 1 endpoints stand behind a single shared bearer token to this day, which is why retiring it is an explicit Phase 2 task (2.5.4) rather than an assumed side effect.

**A second gap, found while elaborating Phase 2 rather than by this audit:** the coverage line at the top of this document ("every FR/NFR maps to ≥1 task and ≥1 test") cannot be checked against the repo. `07-traceability.md` holds the summary and points at itself for the matrix, and the source requirements brief it summarises was never committed — so only `FR-DP-02`, `FR-DP-05` and the `FR-X-*`/`FR-WEB-*` IDs Phase 1 established are pinned to anything. The claim may well be true; it is currently unverifiable. Reconstructing the matrix is an open action item on the Gate G1 report.
